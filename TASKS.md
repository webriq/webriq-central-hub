# WebriQ Central Hub — Task Tracker

## Planned

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|

## In Progress

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 048 | Onboarding: Submit-Only Cliq Notification, Form Lock After Submit, PM Responses View & Reopen | HIGH | minor | _docs/task/048-onboarding-submit-lock-pm-responses-view.md | 2026-06-02 |
| 056 | Enhance Hub Create Task Modal — Project + Tasklist Selection | HIGH | minor | _docs/task/056-enhance-create-task-modal-zoho-project-tasklist.md | 2026-06-08 |

## Testing

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 167 | Portfolio Tracker List — Design System v2.0, Consistent Card Height, "+ New Project" CTA, Phase-Agnostic Description, Search/Filter/Pagination | HIGH | enhancement | _docs/task/167-portfolio-tracker-list-design-system-v2-card-height-cta.md | 2026-07-21 |
| 165 | v2 Dashboard — Real Active-Feature Data, Ported Greeting, Marketing Dashboard, Space Grotesk/JetBrains Mono | HIGH | feature | _docs/task/165-v2-dashboard-real-data-greeting-marketing-dashboard-font-swap.md | 2026-07-21 |
| 164 | Service Worker — Push Event + Notification Click Handlers | HIGH | bugfix | _docs/task/164-service-worker-push-notification-handler.md | 2026-07-20 |
| 163 | In-App Notifications — Activate Header Bell + Wire Push Notification Prerequisites | HIGH | feature | _docs/task/163-in-app-notifications-bell-push-prerequisites.md | 2026-07-20 |
| 064 | Push Notifications (web-push + VAPID + subscription API) | HIGH | feature | _docs/task/064-push-notifications.md | 2026-06-19 |
| 162 | PM Dashboard — Wire Unwired Clickable Rows + Add Missing aria-labels | MEDIUM | bugfix | _docs/task/162-pm-dashboard-clickable-rows-aria-labels.md | 2026-07-17 |
| 157 | New Project — Multi-Select Project Type with StackShift Combination Rules | HIGH | feature | _docs/task/157-new-project-multiselect-project-type-stackshift-rules.md | 2026-07-15 |
| 149 | Onboarding Wizard — Drag-and-Drop Multi-File Upload | MEDIUM | enhancement | _docs/task/149-onboarding-wizard-dragdrop-multifile-upload.md | 2026-07-14 |
| 147 | Kickoff Contacts — Auto-Populate From New Project Setup Form | HIGH | bugfix | _docs/task/147-kickoff-contacts-autopopulate-from-new-project.md | 2026-07-14 |
| 146 | Onboarding Timeline & Wizard — PM/Developer Role-Based Access | HIGH | feature | _docs/task/146-onboarding-timeline-wizard-rbac-pm-developer.md | 2026-07-14 |
| 145 | Onboarding List — Stop Hiding Projects Once Phase 1 Is Done (Page Tracks All 5 Phases) | HIGH | bugfix | _docs/task/145-onboarding-list-show-all-phases-not-just-phase1.md | 2026-07-14 |
| 135 | Onboarding Wizard — Client Call, Sign-Off Step (Meeting Notes + Agreement Upload) | HIGH | enhancement | _docs/task/135-client-signoff-step-redesign.md | 2026-07-13 |
| 140 | Storage/KB — Add Credential/Link Assets (Add Asset Modal Pattern) | MEDIUM | feature | _docs/task/140-storage-kb-add-credential-link-assets.md | 2026-07-13 |
| 139 | Storage/KB File Explorer — Real Folder Look + Grid/List View Toggle (Default Grid) | MEDIUM | enhancement | _docs/task/139-storage-kb-explorer-folder-look-grid-list-view.md | 2026-07-13 |
| 138 | Customer Assets — Per-User Sharing (Alongside Existing Role-Based Permissions) | MEDIUM | feature | _docs/task/138-customer-assets-per-user-sharing.md | 2026-07-13 |
| 137 | Customer Assets — Per-Project Folder Separation in Supabase Storage | MEDIUM | refactor | _docs/task/137-customer-assets-per-project-storage-folders.md | 2026-07-13 |
| 136 | Phase 1 Completion — Animated Closing/Transition Screen (2.4 Completion Criteria) | HIGH | feature | _docs/task/136-phase1-completion-animated-closing-screen.md | 2026-07-13 |
| 134 | Onboarding Wizard — Storage Folder + KB Step Redesign (File Explorer / Finder UI with Permissions) | HIGH | feature | _docs/task/134-storage-kb-file-explorer-redesign.md | 2026-07-13 |
| 133 | Onboarding Wizard — HTML Mockup Step (Upload, In-App Preview, Code Editor) | HIGH | feature | _docs/task/133-html-mockup-step-upload-preview-editor.md | 2026-07-13 |
| 132 | Onboarding Wizard — 90-Day Content Map Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/132-content-map-step-redesign.md | 2026-07-13 |
| 131 | Onboarding Wizard — Migration Checklist Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/131-migration-checklist-step-redesign.md | 2026-07-13 |
| 130 | Onboarding Wizard — Outcome Target Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/130-outcome-target-step-rich-text-file-upload.md | 2026-07-10 |
| 128 | Onboarding Wizard — Kickoff Step Redesign, Rich Text Editor & Autosave Indicator | HIGH | enhancement | _docs/task/128-onboarding-kickoff-step-redesign-rich-text-editor.md | 2026-07-10 |
| 125 | Onboarding Project Detail — Horizontal Gantt Timeline Redesign (exact reference functionality + design, WebriQ style guide) | HIGH | feature | _docs/task/125-onboarding-detail-horizontal-timeline-redesign.md | 2026-07-09 |
| 123 | Onboarding Redesign — Dedicated Bert-Gated Phase 1 Module (New Project Intake, Project-Scoped Programme, Sub-Phase Timeline, PM Visibility Gate) | HIGH | feature | _docs/task/123-onboarding-redesign-bert-gated-phase1.md | 2026-07-09 |
| 122 | 120-Day Customer Programme — Timeline, Bert's Day 1-15 Onboarding Wizard, Reminders & v2 Customers Extension | HIGH | feature | _docs/task/122-120-day-programme-bert-wizard-notifications.md | 2026-07-09 |
| 116 | v2 Customers Listing — Search, Filter, Pagination & Skeletons | HIGH | enhancement | _docs/task/116-v2-customers-search-filter-pagination-skeletons.md | 2026-07-08 |
| 115 | v2 Customer Management & Onboarding — Full Port from v1 | HIGH | feature | _docs/task/115-v2-customer-management-onboarding-port.md | 2026-07-08 |
| 105 | Fix Attachments Export Throttle: Bounded Retry + Shared Zoho Retry Helper | HIGH | bugfix | _docs/task/105-fix-attachments-export-throttle-bounded-retry-shared-helper.md | 2026-07-03 |
| 104 | Attachments Export/Import Fix: Multi-File Scan, SSE, Failure Visibility + DB Index/Constraint | HIGH | feature | _docs/task/104-attachments-export-import-fix-index-constraint.md | 2026-07-02 |
| 102 | Timelogs Export: SSE Streaming + 429 Handling | HIGH | patch | _docs/task/102-timelogs-export-sse-streaming.md | 2026-07-01 |
| 101 | Comments Export: SSE Streaming + Pagination | HIGH | patch | _docs/task/101-comments-export-sse-pagination.md | 2026-07-01 |
| 100 | Super Admin: RLS Policies + Application-Layer Access | HIGH | patch | _docs/task/100-super-admin-rls-and-app-layer-access.md | 2026-07-01 |
| 099 | Fix Super Admin 403 on Admin API Routes | HIGH | bugfix | _docs/task/099-fix-super-admin-403-api-routes.md | 2026-07-01 |
| 098 | Add super_admin role to v2 hub (migration, invite, PATCH API, sidebar, users table) | HIGH | minor | _docs/task/098-add-super-admin-role.md | 2026-07-01 |
| 093 | Zoho Users Export + Import | HIGH | feature | _docs/task/093-zoho-users-export-import.md | 2026-06-30 |
| 092 | Task Detail Page — /v2/projects/[projectId]/tasks/[taskId] | HIGH | feature | _docs/task/092-task-detail-page.md | 2026-06-29 |
| 090 | Tasks Import: Multi-File, Full Column Set & Parent Resolution | HIGH | feature | _docs/task/090-tasks-import-multi-file-full-columns.md | 2026-06-29 |
| 089 | Tasks Export: SSE Progress, Project Slice & Date Filter | HIGH | feature | _docs/task/089-tasks-export-sse-progress-slice-date-filter.md | 2026-06-29 |
| 088 | Zoho Tasklists: Batch Import Fix + pg_cron Zoho API Sync | HIGH | feature | _docs/task/088-zoho-tasklists-batch-import-cron-sync.md | 2026-06-29 |
| 085 | Auth Overhaul — Invitation-Only Registration, Forced Password Change & Email OTP Step-Up | HIGH | feature | _docs/task/085-auth-overhaul-invitation-password-change-otp.md | 2026-06-29 |
| 084 | Zoho Milestones Export + Import + Schema Fix | HIGH | feature | _docs/task/084-zoho-milestones-export-import.md | 2026-06-26 |
| 083 | Projects Search: Server-Side Filtering + Real Pagination Total | HIGH | patch | _docs/task/083-projects-search-server-side-pagination-fix.md | 2026-06-26 |
| 082 | Projects Grid Card: Rotating Conic Gradient Border on Hover | NORMAL | patch | _docs/task/082-projects-grid-card-conic-border.md | 2026-06-26 |
| 081 | Projects Listing UI/UX Redesign — Grid/List toggle, status/type colors, owner, tags, completion ring, pagination, skeleton, DB indexes | HIGH | feature | _docs/task/081-projects-listing-ui-redesign.md | 2026-06-25 |
| 080 | Zoho Projects Import — Create New Hub Rows from projects.json | HIGH | feature | _docs/task/080-zoho-projects-import-create-new-rows.md | 2026-06-25 |
| 079 | Zoho Decommission — Full Data Migration to Supabase | HIGH | feature | _docs/task/079-zoho-decommission-data-migration.md | 2026-06-25 |
| 078 | Zoho Projects Export — GET /api/zoho/projects (Full Paginated Raw Dump) | HIGH | patch | _docs/task/078-zoho-projects-export-endpoint.md | 2026-06-25 |
| 077 | Login Redirect → /v2/dashboard + returnTo Deep-Link Restore | HIGH | enhancement | _docs/task/077-login-returnto-redirect.md | 2026-06-21 |
| 076 | Dashboard RBAC + Remove Duplicate "Dashboards" Nav Link | HIGH | enhancement | _docs/task/076-dashboard-rbac-remove-duplicate-nav.md | 2026-06-21 |
| 075 | Ops Chat: Full Task CRUD Tools (Create, Update, Assign, Delete) | HIGH | feature | _docs/task/075-ops-chat-task-crud-tools.md | 2026-06-21 |
| 074 | Ops Chat: update_task_status + update_classification_status Tools | HIGH | feature | _docs/task/074-ops-chat-update-task-status-tool.md | 2026-06-21 |
| 073 | Native PM Core — Projects/Milestones/Tasks/Subtasks + Kanban/List/Calendar | HIGH | feature | _docs/task/073-native-pm-core-projects-tasks.md | 2026-06-21 |
| 072 | Ops Chat Agent Activation (Claude + Vercel AI SDK + Sanity MCP) | HIGH | feature | _docs/task/072-ops-chat-agent-activation.md | 2026-06-21 |
| 071 | v2 Dashboard: Role-Aware PM/Dev/Admin Dashboards (Zoho Decommission + Design Refresh) | HIGH | feature | _docs/task/071-v2-dashboard-redesign.md | 2026-06-19 |
| 070 | Offline PWA Queue (IndexedDB service worker intercept) | NORMAL | feature | _docs/task/070-offline-pwa-queue.md | 2026-06-19 |
| 069 | Health Check + Rollback + Lane 3 Sequencing | HIGH | feature | _docs/task/069-health-check-lane3-sequencing.md | 2026-06-19 |
| 068 | KB Library (lookupKB, saveToKB, saveKBCorrection) | HIGH | feature | _docs/task/068-kb-library.md | 2026-06-19 |
| 067 | Orchestrator Route + KB Embedding Lookup | HIGH | feature | _docs/task/067-orchestrator-route-kb-lookup.md | 2026-06-19 |
| 066 | Sub-task Enumerator (sanity \| code \| both lane tagging) | HIGH | feature | _docs/task/066-subtask-enumerator.md | 2026-06-19 |
| 065 | Sanity MCP (Streamable HTTP) + Global Token + Preview URL | HIGH | feature | _docs/task/065-sanity-mcp-global-token-preview-url.md | 2026-06-19 |
| 064 | Push Notifications (web-push + VAPID + subscription API) | HIGH | feature | _docs/task/064-push-notifications.md | 2026-06-19 |
| 063 | KB Schema Migration (pgvector + kb_entries + task_logs + kb_corrections) | HIGH | feature | _docs/task/063-kb-schema-migration.md | 2026-06-19 |
| 062 | Sprint 0A: RLS Policies v1 + Auth Trigger | CRITICAL | feature | _docs/task/062-rls-policies-auth-trigger.md | 2026-06-11 |
| 061 | Sprint 0A: v2.0 Schema Migration — All New Tables + Rename customer_projects → projects | CRITICAL | feature | _docs/task/061-v2-schema-migration.md | 2026-06-11 |
| 060 | v2 Route Scaffold — Sprint 0A Foundation | CRITICAL | feature | _docs/task/060-v2-route-scaffold.md | 2026-06-11 |
| 059 | Fix Execution Engine 500 — Sanity `getDocuments([])` 404, Circuit Breaker Threshold, `maxTokens` Passthrough | HIGH | patch | _docs/task/059-fix-execution-engine-500.md | 2026-06-09 |
| 058 | Edit Project Modal — Edit Button on Project Cards with Zoho Rename Sync | HIGH | minor | _docs/task/058-edit-project-modal.md | 2026-06-09 |
| 057 | Enhance Hub Create Task Modal — 2-Step Form with AI Classification | HIGH | minor | _docs/task/057-enhance-create-task-modal-2step-ai-classification.md | 2026-06-08 |
| 054 | Fix Progress Percentage Discrepancy — Product Forms vs Customers Table | HIGH | patch | _docs/task/054-fix-progress-percentage-discrepancy.md | 2026-06-04 |
| 053 | Onboarding Responses Edit, File URL Fix & Product Archive Tab | HIGH | minor | _docs/task/053-onboarding-responses-edit-file-url-archive-tab.md | 2026-06-04 |
| 052 | Code Review Fixes — Security, Auth & Correctness | HIGH | patch | _docs/task/052-code-review-security-correctness-fixes.md | 2026-06-03 |
| 051 | In Review Queue — Tasks Ready for Checking Filter Tab | HIGH | feature | _docs/task/051-in-review-queue-filter-tab.md | 2026-06-03 |
| 050 | PM Create Task from Hub — Manual Task Creation + Immediate Zoho Push | HIGH | feature | _docs/task/050-pm-create-task-from-hub.md | 2026-06-03 |
| 049 | PM Assign Developer to Task (Zoho API) | HIGH | feature | _docs/task/049-pm-assign-dev-to-task.md | 2026-06-03 |
| 048 | Onboarding: Submit-Only Cliq Notification, Form Lock After Submit, PM Responses View & Reopen | HIGH | minor | _docs/task/048-onboarding-submit-lock-pm-responses-view.md | 2026-06-02 |
| 047 | Customer Profile Redesign: Left Nav Layout, Metadata, Product Data & Assets | HIGH | minor | _docs/task/047-customer-profile-redesign-layout-assets-metadata.md | 2026-06-02 |
| 046 | Onboarding: Company Info Sections + PM Section-Gap View | HIGH | minor | _docs/task/046-onboarding-company-info-sections-pm-gap-view.md | 2026-06-02 |
| 045 | Fix "+ New Customer" Button Route (404) | HIGH | bugfix | _docs/task/045-fix-new-customer-button-route.md | 2026-06-02 |
| 041 | Route Architecture Restructure — /auth/login, /dashboard/*, /customers/new | HIGH | minor | _docs/task/041-route-architecture-restructure.md | 2026-06-02 |
| 042 | RBAC: Sidebar & Route Enforcement — Role-based Nav + Guards + Admin Hub Users | HIGH | minor | _docs/task/042-rbac-sidebar-and-route-enforcement.md | 2026-06-02 |
| 039 | Customer Profile Restructure — Settings Panel + Activity Tab | HIGH | minor | _docs/task/039-customer-profile-restructure.md | 2026-06-02 |
| 038 | Pipeline Page Redesign — Rename + Stage Filters + Task Card CTAs | HIGH | minor | _docs/task/038-pipeline-page-redesign.md | 2026-06-02 |
| 040 | Customer ID Format: WRQ-CLIENT- → WRQ-CUST- | NORMAL | patch | _docs/task/040-customer-id-format-rename.md | 2026-06-02 |

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 037 | Code Review Fixes — Security, Performance & Maintenance | HIGH | patch | _docs/task/037-code-review-fixes-security-performance.md | 2026-06-02 |
| 036 | Zoho Portal User: Single-User Lookup by ZPUID or Email | NORMAL | patch | _docs/task/036-zoho-portal-user-by-id.md | 2026-06-01 |
| 035 | Zoho Portal Users: Library Function + API Endpoint | NORMAL | patch | _docs/task/035-zoho-portal-users-endpoint.md | 2026-06-01 |
| 034 | Execution Engine — DB Write Error Visibility + Circuit Breaker Threshold | HIGH | patch | _docs/task/034-execution-engine-bug-fixes.md | 2026-05-29 |
| 033 | Classification: Low-Confidence Review Gaps | NORMAL | patch | _docs/task/033-classification-low-confidence-review-gaps.md | 2026-05-29 |
| 032 | Zoho V3: Fix 3 write functions + parseZohoDate | HIGH | patch | _docs/task/032-zoho-v3-write-functions-parseddate.md | 2026-05-29 |
| 031 | GitHub PR Execution Mode (CODE_CHANGE_MINOR) | HIGH | minor | _docs/task/031-github-pr-execution-mode.md | 2026-05-29 |
| 027 | Sprint 5 — Execution Engine (M6) | HIGH | feature | _docs/task/027-sprint-5-execution-engine.md | 2026-05-27 |
| 028 | Sprint 5 — Reply Generation (M8) | HIGH | feature | _docs/task/028-sprint-5-reply-generation.md | 2026-05-27 |
| 029 | Zoho API V3 Migration (/restapi → /api/v3) | HIGH | chore | _docs/task/029-zoho-api-v3-migration.md | 2026-05-28 |
| 030 | Classification Review: Code Quality + Audit Trail | NORMAL | patch | _docs/task/030-classification-review-code-quality.md | 2026-05-29 |

## Completed

- [168] Portfolio Tracker Detail Page — Design System v2.0 (Timeline + Wizard Shell), "Jump to Today" Z-Index Fix — fixed the reported z-index bug (`notification-bell.tsx`'s two `z-99999` classes were unbracketed, so Tailwind silently emitted no CSS for them at all — the drawer rendered at default stacking while the floating button had a real `z-40`; bracketed to `z-[99999]`), and restyled the Timeline (`_onboarding-detail.tsx`, 1750 lines, read in full) to v2.0 as a strictly visual-only pass — `PHASE_VISUALS`/`PHASE_HEX` remapped to the app's fixed phase-hue vocabulary (Onboard=orange/Migrate=blue/Publish=violet/AI Visibility=teal/Optimize=green, correcting an unrelated old blue/violet/teal/amber/slate mapping), all `isDark` ternaries in the Timeline's own chrome collapsed to fixed-light while the `isDark` value itself stays threaded into the Wizard unchanged for its still-dark-mode-aware step content, and every v1 hex swapped to its v2.0 token. Wizard shell (7-circle step indicator, 2 lines) restyled light-branch-only; all 7 step contents (~5000 lines) explicitly untouched, matching this codebase's established per-step-task precedent. `DeliverableCard` drag/resize logic, phase-jump/access-gating/membership logic all verified unchanged. Six user-driven follow-up rounds in the same session, all logged in the task doc's Implementation Notes: (2) 7 buttons across the Timeline fixed to pill radius per DESIGN.md's button spec, "Onboarding Wizard" dropped its gradient fill for flat blue; (3) a full compliance audit against `_final_design/guide/central-hub-style-guide.html` found and fixed the list page's status-filter pills (were a segmented control, rebuilt as individual floating pills per spec), 6 more buttons at the wrong radius, and 5 form inputs missing their documented focus ring; (4) the shared `OnboardingStatusPill` (list page cards, PM dashboard's Clients table, Marketing dashboard) was rebuilt on the already-compliant `Chip` primitive — was `rounded-full`/500-weight/no-dot/blue, now 5px radius/700-weight/dot/`ok`-or-`warn`-or-`neutral` tone; (5) added a real `"completed"` project status end-to-end (`GET /api/onboarding/projects` never detected Phase-5 completion before this — a finished 120-day programme showed "In progress" forever; now checks Phase 5's `customer_phases` row), colored `ok`-green same as "In progress" (by design — `--ok` and `--ph-optimize` are the identical hex in this system) but distinguished by a checkmark instead of the status dot, plus a card-spacing fix; (6)+(7) the member-avatar stack on both the list page and the Timeline (`AvatarStack`/`CollaboratorAvatars`) — raised the "+N" overflow threshold from 3/4 to 5, replaced native `title` tooltips with the real shadcn/Base UI Tooltip (`AvatarCircle` converted to `forwardRef` on the Timeline page since Base UI's trigger needs to attach a ref a plain function component can't receive), and added a hover slide-up animation exclusively for multi-member stacks (single-member cards/rows get the tooltip only, no motion, deliberately skipped the tooltip on 2 call sites that already show the name as adjacent visible text). `npx tsc --noEmit`/`pnpm lint` clean after every round (repo-wide count held at 1 pre-existing, unrelated error throughout); manual in-browser QA not run this session (no test credentials) — flagged for the user's live-testing pass → `_docs/task/168-portfolio-tracker-detail-design-system-v2-zindex-fix.md` (completed: 2026-07-21)
- [166] V2 Dashboard — WebriQ Design System v2.0 (Navy/Blue/Orange) + Real 120-Day Programme Data — replaced `DESIGN.md`'s v1 dark-first oklch "Night Ops Console" system with Design System v2.0 (`#071133` navy / `#007BFF` blue / `#FB914E` orange, sourced from `_final_design/guide/central-hub-design-system.md`), and rebuilt `pm-dashboard.tsx` to match the mockup's full section set (`_final_design/dashboard/webriq-central-hub-dashboard.html`) with real Supabase data throughout, no fabricated numbers. Removed the old ticket/plan cards (`DecisionCard`/`TasksTable`/`DeskPulse`/`DailyDigestCard`); added 4 real stat tiles, the phase-colored 120-Day Programme board, a Clients table, Reminders (`GET /api/notifications`), a Developer queue (Phase 2 deliverables), a Phase 1 intake checklist (soonest-Day-15-gate client, `onboarding_internal_deliverables`), and Publish-phase progress (Phase 3 deliverables, honestly labeled as coarser than the mockup's page-level "weekly waves" since no real per-page data exists). New shared `Chip`/`PhaseChip`/`ProgrammeTrack`/`OnboardingStatusPill` primitives in `dashboard-shared.tsx`; `globals.css`'s `.pm-light` tokens updated to v2.0 hex so Dev/Marketing/Admin dashboards inherit the new palette automatically (none hardcode brand colors outside the `--c-*` token system, confirmed by reading all three in full — zero code changes needed there). Found and corrected a stale research premise mid-implementation: migration 070 (task 146, applied after the 060 the planning research cited) had already re-widened `customer_phases`/`customer_deliverables`/`onboarding_internal_deliverables` read access to `pm`/`developer`, so the plan's original "PM-limited vs. Admin-only-full-fidelity" split was unnecessary — corrected in the task doc before writing code, giving PM's dashboard the full section set and reducing `admin-dashboard.tsx` to a 10-line pass-through (previously also home to an "Admin-only extras" LLM-spend/event-bus block, since removed per live-testing feedback below). Live-testing round 2: user caught that the rewrite dropped the mockup's page-head (greeting stats subline + "Export weekly report" button) — restored with real `inProgress.length`/`lateProjects.length` counts and a working client-side CSV export of the loaded programme roster (no new API); user also asked to remove `WorkspaceCard` ("Your workspace") and Admin's LLM Spend/Event Bus Health block "for now" — both deleted along with their now-orphaned queries/imports. Round 3: user reported the greeting still invisible — traced to `use-greeting.ts`'s single app-wide `sessionStorage` key (`hub_greeting_ts`) already having faded from earlier same-tab testing, not a code bug; user ran the suggested `sessionStorage.removeItem` reset and confirmed it renders correctly. `npx tsc --noEmit`/`pnpm lint` clean throughout every round (1369 problems/77 errors baseline unchanged from before this task, all pre-existing and outside its touched files) → `_docs/task/166-v2-dashboard-design-system-v2-real-programme-data.md` (completed: 2026-07-21)
- [150] Portfolio Tracker — Nav Rename, `project_id` Route Param, Query-Param Wizard Step Addressing — retitled in scope partway through from the original "120-Day Programme" naming after user feedback during Testing; original plan + first revision both completed together in this doc. Original scope (3 parts, all shipped): (a) sidebar/page-title rename off "Onboarding," (b) `[projectId]` route now keyed by the human-readable `projects.project_id` instead of the UUID (a documented, scoped exception to CLAUDE.md's UUID-routing-key convention), (c) wizard step state made URL-addressable instead of pure `useState`. Found and fixed two latent bugs along the way not named in the original task doc: `GET`/`POST /api/onboarding/projects` both mis-labeled the UUID as `project_id` in their response shape (same bug category, only the GET route was originally flagged) — left unfixed, the "New Project" success screen's View button would have 404'd once the detail route required the real `project_id`; and `[projectId]/page.tsx`'s `phase_members`/`project_members` queries had to be switched from the raw route param to the fetched `project.id`, since those child tables are still UUID-keyed and the route param stopped being the UUID. Follow-up revision (post-Testing, same doc): user asked for a different URL shape than first shipped — route segment/label `programme` → `portfolio-tracker`, and the nested `/wizard/[stepKey]` route replaced with `?phase=&deliverable=` query params (1-based deliverable index into `getPhaseByNumber(phase).deliverables`, not the string key, so future deliverable additions/reorders in `customer-phases.ts` don't need another URL redesign) — one shared `stepIdx`-watching effect in the Wizard keeps Continue/Back/Steps-indicator navigation and the URL in sync without touching each of the 6 internal step-change call sites individually. Also added a breadcrumb-map entry (the route previously fell through to the header's default "WebriQ / Hub" since no entry existed for it) and relabeled the detail page's back-link "Back to Onboarding" → "Back to Projects" (label only, same destination — no new target was specified). Implemented the new path as `/v2/portfolio-tracker/[projectId]` rather than a literal root-level `/portfolio-tracker/[projectId]` as the request's example URLs showed, since everything in this app's v2 build (sidebar, header, auth guard) lives under `/v2/*` and a true root-level route would need its own duplicated layout — flagged to the user rather than assumed silently. Final round: user confirmed the one item flagged as still open — the list page's `<h1>🚀 Onboarding</h1>` heading now reads "🚀 Portfolio Tracker" too. `npx tsc --noEmit` passes clean throughout every round; `pnpm lint` has one pre-existing, unrelated failure (`_onboarding-wizard.tsx`'s `checklistValidationError`-clearing effect, `react-hooks/set-state-in-effect`) that predates this task and was never touched by it → `_docs/task/150-onboarding-programme-rename-route-project-id-wizard-routes.md` (completed: 2026-07-17)
- [161] Onboarding Wizard — Bert Label Removal, Checklist Redesign, shadcn Tooltips, Clickable Steps Indicator & Preview Fixes — retitled from the original "Remove Static 'Bert' Owner Label, Checklist Checkbox Redesign & 'Mark All as Done' Button," which shipped as originally scoped, but the same session kept surfacing follow-up work on the same Wizard file across 7 rounds. Round 1 (original scope): removed the static "Bert"/"PM + Bert" owner pill from `WizardDeliverableRow` (Phase 1 step header); redesigned internal-checklist items from `Circle`/`CheckCircle2` icons to a custom checkbox (outlined `rounded` square unchecked, `bg-green-500` filled square + white `Check` when done — sized `h-5 w-5` since this project's `--radius-lg` token is 12px and fully circularizes a 16px box); added a hover tooltip right of each item's label ("Mark as Done"/"Uncheck"); added a per-step "Mark All as Done" button reusing the existing incomplete-items/force-confirm validation modal via a new `markAllAdvance` flag so it never auto-advances the step (unlike the Continue-gate's own use of the same modal). Round 2: swapped the hand-rolled hover-reveal tooltip for the real shadcn Tooltip (`npx shadcn add tooltip`, Base UI-backed per this project's `base-nova` style; `TooltipProvider` added to the root layout) — found and fixed it rendering as a near-invisible white-on-white pill, since `<html>` carries a permanent `dark` class app-wide while v2 pages theme independently via a JS `isDark` prop that defaults to light; hardcoded `src/components/ui/tooltip.tsx`'s Popup/Arrow to a solid dark pill instead of the theme tokens. Round 3: rolled the same Tooltip out across the entire Wizard — the 7-circle step indicator (bottom placement, shows the full untruncated step name) and ~25 icon-only Add/Remove/X/View/Edit/Actions buttons across every step and the Storage/KB file explorer, via a small local `IconTip` helper, replacing native `title` attributes. Round 4: fixed two unrelated visual bugs found along the way — the active step's `ring-4` glow was clipped at the top because the steps-indicator scroll container only padded horizontally (`px-1 -mx-1`; `overflow-x-auto` forces `overflow-y` non-visible per spec too), fixed to `p-1 -m-1`; and CSV/Excel file-preview columns were unreachable because the modal's preview flex item had `min-h-0` but not `min-w-0`, letting it silently grow past the modal width instead of its own `overflow-auto` div ever registering overflow — added `min-w-0`. Round 5: made that now-functional CSV/Excel scrollbar actually visible at rest — discovered Tailwind v4 generates zero CSS for `[&::-webkit-scrollbar-thumb]:bg-*` arbitrary-variant syntax (confirmed via the page's own compiled stylesheet rules), so the fix from Round 4 was scrollable but still invisible (the app-wide thumb color is a translucent white tuned for the Hub's permanently-dark surfaces); replaced with a plain `.scrollbar-light` class hand-written in `globals.css`, matching how the app's existing scrollbar styling is already raw CSS there, not a Tailwind utility. Round 6: made the step indicator clickable — already-reached steps always navigate; jumping to an unreached step is blocked with a custom modal ("'{step}' needs to be completed first before continuing to the other step") unless the currently-viewed step is done or overdue (`currentDay > step.dayEnd`); pm role bypasses the gate entirely, mirroring `handleContinueClick`'s own existing pm bypass. Round 7: restored a real pre-existing validation gap the user caught while re-testing — `handleValidatedInternalToggle` only ever gated 4 of the 5 field-backed checklist keys; `outcome-target-filed` (Outcome Target's "Agreed measurable outcomes…" item) could be checked with the field completely empty, no error, since it fell through to the function's own documented no-op passthrough for unlisted keys (predates this session, never touched by rounds 1-6). Added the missing `outcome-target-filed && !isOutcomeFilled` branch (mirroring the existing 4), plus the same key to `handleMarkAllDone`'s force-confirm check and `handleReview`'s field-highlight reset, so all three entry points (direct checkbox click, "Mark All as Done", the Continue-gate's own modal) now correctly block/highlight instead of silently completing. `npx tsc --noEmit`/`pnpm lint` pass clean throughout all 7 rounds; every round live-tested in-browser (checklist toggle/tooltip/Mark-All-as-Done including the force-confirm bypass path, step-indicator and Add/Remove tooltips matching reference screenshots pixel-for-pixel, ring-glow fix via zoomed screenshot comparison, CSV overflow+visible-scrollbar fix via a real uploaded wide test file, clickable-steps blocking/allowed cases, Round 7's restored validation on a fresh empty Outcome Target step) — no console errors → `_docs/task/161-onboarding-wizard-remove-bert-label-checklist-redesign-mark-all-done.md` (completed: 2026-07-17)
- [160] Onboarding Timeline — Fix Jump-to-Phase "Day N" Indicator + Read-Only Previous Phases — fixed two related bugs in the manual "Jump to phase" override (`PATCH .../programme/phase` + `OnboardingWizard`). Bug 1: the already-started branch of the phase route left `projects.programme_started_at` untouched, so the calendar-day-driven "Day N" marker/header text/progress bar never moved after jumping an in-progress project to a different phase — now backdates it the same way the not-started branch already does (`adminClient`, matching `seedProgrammeAtPhase`'s convention for that table). Bug 2: `OnboardingWizard` had no concept of whether the phase it renders is still the DB's active phase, so a jumped-past Phase 1 stayed fully editable/uploadable — added a required `isPhaseActive` prop folded into `isStepReadOnly`/`canEditChecklist` (now `|| !isPhaseActive`, extending storage-kb's existing PM-only carve-out to also require an active phase, no carve-out once inactive) and into the completion-flow gating (banners, error text, Complete-Phase-1 button/message, new "Phase 1 is no longer active" copy). The Timeline's "Onboarding Wizard" CTA button now stays visible after a jump (relabels to "View Onboarding Wizard") instead of disappearing once Phase 1 is no longer active; `isPhaseActive` is computed inline from `phases` state rather than the later `phaseStatusMap` to avoid a `const` temporal-dead-zone error in the wizard's early-return render branch. `npx tsc --noEmit`/`pnpm lint` pass clean; no live browser verification run this session. Marked complete for now at the user's request — a follow-up task is planned for tomorrow covering further Onboarding Phase/jump-to-phase work, including an explicit in-Wizard message telling the user *why* a phase is read-only (currently fields are just disabled with no explanatory copy) → `_docs/task/160-onboarding-jump-to-phase-fix-active-indicator-readonly-previous.md` (completed: 2026-07-16)
- [159] Onboarding List — "Import Project" CSV/Excel Bulk Intake — bulk CSV/Excel import for onboarding projects, completed together with 5 rounds of live user testing/iteration all bundled into this one doc. Initial build: full-page wizard (`/v2/onboarding/import`, replacing an earlier modal design after round 1 feedback that its columns clipped and rows couldn't be fixed) with an Upload → editable Review & Fix table → Import flow; new `POST /api/onboarding/projects/import` route (partial-failure-tolerant, per-row customer/contact/phase seeding) reusing a newly-extracted `seedProgrammeAtPhase` helper shared with `PATCH .../programme/phase`'s not-started branch. Round 2: converted the review table's static preview into fully editable rows (`Customer`/`Project Type`/`Primary Contact`/`Kickoff Date`/`Current Phase`), reusing the New Project wizard's exact StackShift-grouping rule for Type selection. Round 3: renamed `Account`→`Customer`/`Type`→`Project Type`, replaced the 6-chip Type picker with a searchable multi-select combobox (portaled dropdown to escape the table's scroll-container clipping), added placeholder-value detection for Primary Contact ("To be confirmed"/"Unknown" clear the field but show the original text below). Round 4: quoted the Primary Contact placeholder hint, added a per-pill remove `×` button, fixed invisible search-box input text. Round 5: fixed a real Kickoff Date bug — SheetJS was silently reinterpreting day-first `DD-MM-YYYY` sheet text as US month-first, corrupting dates like "09/07/2026" into September instead of July; rewrote the parser to explicitly handle day-first strings and genuine Excel date-serial numbers. Confirmed the Timeline's existing `getCurrentProgrammeDay()` math already derives the correct active day/sub-phase once `programme_started_at` is set correctly (no new logic needed there beyond the date-parsing fix), and generalized `seedProgrammeAtPhase`'s per-deliverable seed status to mark whichever deliverable's day range contains the current elapsed day as "in_progress". Added an "Overdue" warning (Phase 1 still active past its 15-day window) in both the import review table and the Timeline's existing reminders-strip system. Verified end-to-end via a real completed import (not just previewed) matching a user-supplied worked example (Kickoff July 9, Day 8, Migration Checklist active) and an overdue case (Kickoff June 1, Day 46, "Phase 1 Overdue" banner). Also found and fixed one unrelated schema bug as a byproduct of cleaning up test data: `customer_asset_folders_project_id_fkey` (migration 065) was the only `project_id` foreign key in the schema without `ON DELETE CASCADE`, blocking test-row deletion — added migration 081 to match every sibling FK's convention (not yet applied by the user). `npx tsc --noEmit`/`pnpm lint` pass clean throughout all rounds → `_docs/task/159-onboarding-import-project-csv-excel.md` (completed: 2026-07-16)
- [158] New Project — Scheduled Start Redesign: Date/Time Picker, Phase-Aware Scheduling, QStash + Cron Reliability — retitled from the original "Move 'Scheduled Start' from Project Details to Review & Create," which shipped as originally scoped, but the same live-testing thread kept surfacing follow-up work on the exact same feature across 9 rounds, all bundled into this one doc rather than opened as separate tasks (same convention as task 155). Round 1: fixed a pre-existing duplicate "Company name is required" error (a leftover standalone error span meant only for the existing-company search view rendering unconditionally). Round 2: selecting an existing company now fetches and pre-fills its primary contact (new `GET /api/customers/[customerId]/primary-contact`, adminClient-backed since marketing isn't covered by `contacts_staff_read` RLS) with a loading state on the three contact fields. Round 3: server-side duplicate-name validation for company (new-company only) and project name (new `check-name` endpoints), blocking `goNext()` with an inline error. Round 4: added a "Start at phase" dropdown (admin/super_admin/marketing only, mirroring `_onboarding-detail.tsx`'s `canManagePhases` exactly) so immediate starts can jump straight to Phase 2-5 via the Timeline's existing "Jump to phase" route, plus a first-pass date-range constraint on the scheduled-start field. Round 5: hid "Start Now" while scheduling, restyled "Confirm & schedule" to match "Start Now"'s design, and made "Just save" clear the schedule state. Round 6: replaced the native `datetime-local` input (inconsistent across browsers/OS) with a custom `react-day-picker`-based `DateTimePicker` matching the app's own design tokens, plus auto-flip positioning so it never opens off-screen. Round 7: scheduled starts became phase-aware end-to-end (migration 076's `scheduled_start_phase`, `seedAndStartProgramme` generalized to accept a target phase, unifying the Phase-1 and Jump-to-phase seeding paths) and the Timeline's not-started card got a matching scheduled-state redesign ("Start Phase N Anyway" / "— OR —" / "Select Phase" + "Proceed"). Round 8: root-caused a real production bug — **all 5** cron-triggered jobs in the app (not just this one) had been sitting on unfilled `REPLACE_WITH_APP_URL`/`REPLACE_WITH_DIGEST_SECRET` placeholders since their original migrations and had never actually fired; fixed via Supabase Vault-based secret lookup (migrations 077/078, so real values never touch a git-tracked migration) plus an app-wide rename of the shared secret (`DIGEST_SECRET`→`CRONJOB_SECRET_KEY`, `x-digest-secret`→`x-cron-secret`) and a tightened 5-minute interval for just this job (migration 079); live-diagnosed and fixed a second bug where the Vault secrets themselves hadn't been created yet, which nulled out `net.http_post`'s url/headers and violated a `NOT NULL` constraint. Round 9: added Upstash QStash as a one-shot exact-time trigger (new `src/lib/qstash/index.ts`, migration 080's `qstash_message_id`, a signature-verified idempotent callback route) with the cron poll deliberately kept as a fallback safety net; fixed a build-breaking bug where `verifySignatureAppRouter` throws at module-load time without signing keys configured, and a live region-routing bug where QStash's global endpoint resolved to the wrong region for the user's account (fixed via `QSTASH_URL` env var, no code change). Verified via schema inspection that a QStash/cron race can't corrupt data (`project_id`-scoped unique constraints on all three seeded tables turn a losing racer into a caught, harmless Postgres error). `npx tsc --noEmit`/`pnpm lint`/`pnpm build` pass clean throughout all 9 rounds; user confirmed live in production that both the cron fallback and QStash successfully auto-started real scheduled test projects → `_docs/task/158-new-project-move-scheduled-start-to-review-step.md` (completed: 2026-07-16)
- [156] New Project — Optional Primary Contact + Horizontal Contact Email/Phone Layout — dropped step 1's client-side requirement that Primary contact name/email be filled in, since the same contact can be entered later at Kickoff (`ContactsField`, task 129/151's `upsertPrimaryContact` sync) — confirmed via code reading that the server (`POST /api/onboarding/projects`) already treated the whole `contact` object as optional, so this was purely a client-side validation change (`goNext()`'s step-1 check, `_content.tsx`). Kept the email-format regex check but gated it on non-empty input; removed the `required` prop/asterisk from Primary contact and Contact email; wrapped Contact email + Phone in a `grid grid-cols-1 sm:grid-cols-2 gap-3` row so they sit side-by-side on desktop and stack on mobile, Primary contact staying its own full-width row above. Confirmed by reading the code (not live-tested in browser, extension unavailable this session) that leaving contact fields blank at intake doesn't lose data — the Kickoff step's `ContactsField` pre-fill reads live from the `contacts` table (`page.tsx`'s `is_primary` query), and its own autosave (`wizard-data/route.ts`) calls `upsertPrimaryContact` again when the PM fills it in there instead, so the redundant early-entry point is simply removed, not a data path. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/156-new-project-primary-contact-optional-horizontal-layout.md` (completed: 2026-07-16)
- [155] Project Ownership Transfer, Wider Collaborator Pool & Search-to-Add Picker — third task in the 153→154→155 project/phase-membership arc, completed together after 4 rounds of live user testing/iteration on this doc (informally tracked as "task 156"/"task 157" in code comments — no separate task doc files exist for those, everything landed here). Round 1 (task 156): relocated Phase 1's member-management UI out of the Timeline page's `AccessPanel` and into `_onboarding-wizard.tsx` itself (new `PhaseAccessPanel`, toggled via an "Access" button in the Wizard's persistent header), since managing who has Phase 1 access belongs inside that phase's own workspace — `_onboarding-detail.tsx` still owns the `phase1Members` state and the Wizard-entry restriction gate, only the management UI moved; adding a phase member now also auto-adds them as a `project_members` row (non-owner), so anyone with phase access can find the project on the list too. Round 2 (task 157): added a real, transferable project-level owner (`project_members.is_owner`, mirroring `phase_members.is_owner` from task 153, migration 074) — the project creator becomes owner at creation, Super Admin/Admin/creator can transfer it afterward (including on legacy pre-153 projects with zero members); replaced the static "Owner: Bert" header text (a hardcoded per-phase config label never connected to any real user) with the actual owner's avatar, falling back to the project creator's name for legacy projects; added a "Collaborators: [avatars]" row (includes phase-added members automatically); replaced the "Access" text button with a Gear "Project Settings" icon opening a 2-item dropdown ("Set Project Owner" / "Add Collaborators"), each expanding its own focused panel — `canManageProjectMembers`/new `canSetProjectOwner` were re-keyed off "is this caller the project creator" rather than plain membership, per the user's explicit final role list (Super Admin/Admin/PM/creator can add collaborators; Super Admin/Admin/creator can set the owner) — this deliberately dropped `marketing` from collaborator-adding rights, flagged clearly to the user since it had those rights in every earlier round; also fixed an unrelated wizard step-indicator clipping bug (active step's `ring-4` glow was being cut by its `overflow-x-auto` container's edge). Round 3: user's first live test surfaced a real `PGRST201` PostgREST error — `project_members`/`phase_members` each have two FKs into `profiles` (`user_id` and `added_by`), so a bare `profiles(...)` embed was ambiguous everywhere either table joined to `profiles` (4 queries across `page.tsx` and both members API routes, including code originally from task 153) — fixed by qualifying every embed with its explicit FK constraint name. Round 4: minor layout polish (search field above collaborator chips instead of beside them, added bottom margin so the expanded panel doesn't touch the progress bar below it). `npx tsc --noEmit`/`pnpm lint` pass clean throughout all rounds; user live-tested each round in the browser before approving → `_docs/task/155-project-owner-search-picker-collaborator-refinement.md` (completed: 2026-07-15)
- [154] Onboarding List — Member Avatar Chips on Project Cards — second task in the 153→154→155 arc, completed together. `GET /api/onboarding/projects` now returns a deduped `members` array per project (union of `project_members` + Phase 1 `phase_members`, resolved to `profiles.full_name` via one batched lookup), and each list card renders up to 3 overlapping initials avatars + a `+N` overflow chip in its bottom metadata row, reusing `OwnerChip`'s (Projects module) exact initials/color derivation for visual consistency but as a smaller overlapping stack with overflow — behavior `OwnerChip` itself doesn't have. No changes were needed to this task's own files during the 4 rounds of follow-up work under task 155 — its query pattern (`profiles` queried standalone via `.in("id", ids)`, not embedded through `project_members`/`phase_members`) was never subject to the `PGRST201` ambiguity the other two tasks in this arc hit. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/154-onboarding-list-member-avatar-chips.md` (completed: 2026-07-15)
- [153] Project & Phase 1 Membership — Ownership, Access Gating & Project Visibility — foundation task for the 153→154→155 arc, completed together. Built a two-tier membership model: `project_members` (gates Onboarding-list visibility for marketing/pm; admin/super_admin always see everything) and `phase_members` (gates entry into Phase 1's Wizard, with a single transferable owner via a partial unique index, migration 073) — both tables use a "zero membership rows = unrestricted" backward-compatibility rule so shipping this never locked out any already-in-progress onboarding. Widened project creation and "Start Onboarding" to include `pm` (previously admin/super_admin/marketing only, in three separate `CREATE_ROLES`/`WRITE_ROLES` lists); `seedAndStartProgramme` (shared by 3 callers, including a session-less cron discovered mid-implementation) gained an optional starter-user parameter to seed Phase 1 ownership. Read the requirements as meaning `marketing` — not just `pm` — is also phase-membership-gated (today any marketing user has unrestricted Wizard access; flagged as the one non-literal interpretation in the task doc for the user to confirm). Split the planned single membership-logic file into `membership-rules.ts` (pure, client-safe) + `phase-membership.ts` (adminClient-backed mutations) after discovering `_onboarding-detail.tsx` (a Client Component) needed the pure permission checks — CLAUDE.md forbids importing `adminClient` client-side. User live-tested this task together with 154/155 across 4 follow-up rounds (see task 155's Implementation Notes) and found one real bug that traced back to this task's original code: a `PGRST201` PostgREST error from `project_members`/`phase_members` each having two FKs into `profiles`, fixed under task 155's Follow-Up 3 by qualifying the ambiguous embeds. Also superseded later: this task's original `AccessPanel`/`AddPersonSelect` Phase 1 UI was relocated into the Onboarding Wizard under task 155's Follow-Up 1 — the restriction gate and `phase1Members` state this task built remain unchanged, only the management UI moved. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/153-project-phase-membership-access-control.md` (completed: 2026-07-15)
- [152] Onboarding Wizard — Real-Time Upload Progress Bars + Kickoff/Business Facts File Preview — follow-on to task 149. Added a shared `uploadFileWithProgress` helper (`XMLHttpRequest` wrapped in a Promise, since `fetch` exposes no upload-byte progress) and wired it into all 6 `FileUploadBox`/`HtmlMockupFileList` upload handlers (Business Facts, Outcome Target, Migration Checklist, Content Map, Client Sign-Off, HTML Mockup), each tracking a per-file progress array rendered as a live bar in the two shared components; as a side effect this also fixed a pre-existing race from task 149 where concurrent uploads shared one step-level `uploading` boolean (now derived from the progress array's length instead). Also closed a real gap: Kickoff/Business Facts was the one `FileUploadBox` call site never wired to the shared `onView`/`FileViewerModal` preview every other step already had — added `handleViewBusinessFactsFile`, an exact mirror of the existing pattern. Three rounds of live user testing surfaced and fixed real issues beyond the original plan: (1) the `finishing` state meant to bridge the gap between byte-upload completion and full server response was itself set too late (only after the second DB-insert request resolved), so it never covered the actual XHR-to-response wait — fixed by moving the transition into the progress callback itself, flipping the instant `upload.onprogress` hits 100%; (2) the pulsing-bar "Finishing…" treatment was replaced with a spinning `Loader2` icon (this codebase's established loading-state convention, per `_milestone-panel.tsx`/`_task-drawer.tsx`/etc.) after the user pointed out a bar visually implies measured progress that doesn't exist for that phase. Also fielded and declined (with reasoning, no code change) a question about switching to Server-Sent Events for that phase — traced the upload route (`assets/upload/route.ts`) to confirm the server has no granular progress to stream in the first place under its current buffer-then-atomic-`supabase-js`-upload approach; real end-to-end percentage would require a chunked/resumable protocol (e.g. TUS) first, a materially bigger change flagged as a possible future task rather than bundled in here. `npx tsc --noEmit`/`pnpm lint` pass clean throughout all 4 rounds (initial implementation + 3 follow-up fixes); every fix was live-tested by the user in the browser (including real 7.8MB and 25.4MB file uploads) before being marked complete → `_docs/task/152-onboarding-wizard-upload-progress-bars-kickoff-preview.md` (completed: 2026-07-15)
- [151] Primary Contact — Migrate from customers Columns to contacts.is_primary — moved the write-side source of truth for a customer's primary contact off the two loose `customers.contact_name`/`contact_email` columns (plus phone, previously stashed in `projects.source_meta.primary_contact_phone` since `customers` never had a phone column) onto a proper `contacts.is_primary` flag, since a customer can have many contacts and `contacts` (task 117) already models that. This directly reverses task 120's earlier decision to reject exactly this approach — revisited because phone finally needed a real home and task 147 (this session) added a third awkward `source_meta` read path. Kept `customers.contact_name`/`contact_email` as a synced read cache so the 15+ existing read-only call sites (search, list views, public onboarding pre-fill, `reply.ts`) needed zero changes; only the four write paths (v1/v2 create, v1/v2 edit, New Project form, Kickoff autosave) route through one new shared helper (`src/lib/customers/primary-contact.ts`), plus task 147's Kickoff pre-fill switched from reading `source_meta` to reading the customer's `is_primary` `contacts` row. New migration 072 adds `full_name`/`is_primary` columns, a DB-enforced partial unique index (one primary per customer), and backfills existing `customers` contact data. Also fixed two real bugs found only by tracing actual role access (not visible from the plan alone): the `marketing` role can call two of the affected write/read routes but isn't covered by `contacts` table RLS, which would have silently dropped its writes/reads — fixed with `adminClient` for just those specific operations, no RLS policy touched; and a uuid-vs-empty-string comparison bug in the demote-primary query that Postgres would have rejected. Also caught and fixed a duplicate-insert risk this migration would otherwise have introduced in the existing Phase-1 hand-off bulk-insert (task 129). `npx tsc --noEmit`/`pnpm lint` pass clean. User applied migration 072 live; first re-test still showed a blank Kickoff pre-fill on a reused test project — not a code bug, but the pre-fill's by-design "never overwrite an already-autosaved Kickoff" behavior colliding with a stale blank snapshot from earlier testing (task 147). Resolved with a one-line SQL fix clearing just that project's stale `wizard_data.kickoff.contacts` key; user confirmed the pre-fill then worked correctly → `_docs/task/151-primary-contact-migration-contacts-table.md` (completed: 2026-07-15)
- [148] 120-Day Timeline — Real Day-Span Scheduling + Drag Resize/Move — widened every Phase 2-5 `DeliverableConfig` day span in `customer-phases.ts` from the old single-day pins to real multi-day ranges (Phase 1 untouched); added migration 071 (nullable `day_start_override`/`day_end_override` on `customer_deliverables`, check-constrained, no RLS change needed) for per-project schedule edits that never mutate the shared static config; new role-gated (`admin`/`super_admin`/`marketing`) `PATCH .../deliverables/[deliverableKey]/schedule` route mirroring the sibling status-PATCH route's shape, validated against the deliverable's own phase day range. `_onboarding-detail.tsx`'s `Swimlane` merges `override ?? default` before rendering; `DeliverableCard` gained a custom pointer-capture drag implementation (no new library) with resize-left/resize-right/move modes, whole-day snapping, phase-range clamping, and a 4px movement threshold to distinguish a Phase-1 click (opens the wizard, unchanged) from a drag (resizes/moves, suppresses the click) on the same card body; drag-end optimistically updates state and calls the new PATCH, rolling back via the page's existing error banner on failure. `npx tsc --noEmit`/`pnpm lint` pass clean; migration 071 created but not applied, manual/browser verification skipped (no live environment this session). One flagged pre-existing limitation (not introduced or fixed by this task): the override lookup is keyed only by `deliverable_key`, matching the established `deliverableStatusMap` convention — but that key repeats across Phase 4 and Phase 5 (`updated-publishing-plan`, `gap-publishing`), so an override on one could currently apply to its same-keyed sibling in the other phase → `_docs/task/148-timeline-day-span-drag-resize-move.md` (completed: 2026-07-15)
- [144] Storage/KB File Explorer — Folder Permissions, Folder Rename/Delete, File Rename — added `allowed_roles`/`allowed_user_ids` to `customer_asset_folders` (migration 068) with server-side visibility filtering via a new `canSeeFolder()` mirroring the existing asset-level `canSeeAsset()`; new `PATCH`/`DELETE /api/customers/[customerId]/assets/folders/[folderId]` route (rename allowed on any folder including the 6 system folders + 3 pre-seeded sub-folders — cosmetic, folder membership is `folder_id`-based since task 141; delete blocked on system folders and on any non-empty folder, both confirmed defaults from the user rather than silent assumptions, to avoid the `ON DELETE CASCADE`/`ON DELETE SET NULL` FK orphaning edge cases); extended `PATCH .../assets/[assetId]` for file rename (display-name only, `file_path`/Storage object untouched). Folder kebabs gained Permissions/Rename/Delete, file kebabs gained Rename, both sharing one rename modal and the `renderPersonPicker` search-to-add UI (task 141). Live-run follow-ups after the user applied migration 068 and tested: rename modal's Save button now shows "Saving…" while busy; the new folder Permissions panel got the same explicit ✕ close button added to task 141's file panel, so neither requires re-clicking "Permissions" to close → `_docs/task/144-storage-kb-folder-permissions-rename-delete-file-rename.md` (completed: 2026-07-14)
- [143] Storage Bucket Path — Use Public project_id Instead of Project UUID — swapped the Supabase Storage path's project segment from the raw `project.id` UUID to the new human-readable `project_id` (task 142), so new uploads land at `{customerId}/{project_id}/{timestamp}_{filename}` instead of a UUID-shaped path; existing files keep their old path unchanged (`file_path` is always read as-is, per task 137's established precedent). Required widening the `project` prop type through all three files in the render chain (`page.tsx` → `_onboarding-detail.tsx` → `_onboarding-wizard.tsx`) — the middle one wasn't anticipated in the original file list, added as a necessary pass-through dependency. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean; the specific Storage-path change itself wasn't independently re-verified against the Supabase dashboard this session → `_docs/task/143-storage-path-human-readable-project-id.md` (completed: 2026-07-14)
- [142] Projects — Rename zoho_project_id → external_project_id + New Public-Facing project_id — confirmed with the user before proceeding: task 117 had deliberately kept `zoho_project_id`'s Zoho-specific name since it was still live-synced, but with Zoho being decommissioned that reasoning no longer applies, so the rename went ahead across all 90 genuine references in 30 application files + `database.ts`, carefully preserving the unrelated `_zoho_project_id` (underscore-prefixed) convention — a completely different, still-untouched thing used only for tagging raw Zoho IDs onto exported records for import correlation (verified via whole-tree greps before/after: 40 occurrences, unchanged). Migration 066 also added a new `project_id` column (`<last 4 chars of customer_id>-PROJ-<4 random chars>`, auto-generated by a `BEFORE INSERT` trigger + one-time backfill for existing rows) — display-only, the UUID `id` stays the routing key everywhere. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean. User applied the migration live; the one `NOTICE` in the `supabase db push` output (`trigger ... does not exist, skipping`) was the migration's own defensive `drop trigger if exists` running before first creation — expected, not an error. The generated `project_id` format was subsequently confirmed live via a real value (`2EBA-PROJ-04BA`) visible in a screenshot the user shared while testing task 141/144's File Explorer → `_docs/task/142-projects-external-project-id-rename-public-project-id.md` (completed: 2026-07-14)
- [141] Storage/KB File Explorer — Drive-Style Folders/Files, Custom Folder Tree, Bulk Actions — replaced the fixed, label-derived-only folder model (tasks 134/139) with a real folder tree (`customer_asset_folders`, migration 065, arbitrary depth via `parent_folder_id`) confirmed with the user to span the "whole explorer" (not just under Business Files), auto-provisioning the six former hardcoded folders plus three new `Branding`/`Proposals`/`Collateral` sub-folders that replace the removed free-text "Documents (branding / proposals / collateral)" note field. Redesigned folder tiles to a border-less pill+kebab look and files to Google-Drive-style cards with a kebab-only actions dropdown (View/Permissions/Move to folder/Remove), added multi-select with a Drive-style bulk toolbar (Share/Move/Delete — no Download, confirmed with the user since this app has no download feature anywhere by deliberate design), and a "New folder" control at every level. Live-run bug found immediately after the user applied migration 065: `customer_asset_folders` had no RLS policy (this Supabase project auto-enables RLS on new tables), so every insert 500'd — fixed with migration 067, an identically-shaped permissive policy to its sibling `customer_assets` (migration 021). Several rounds of live UI feedback followed and were applied directly: breadcrumb iterated from "hidden at root" to "always visible with the project's public ID + a 'Loading folders…' state" to "non-clickable and greyed specifically at root"; multi-select switched from checkboxes to Google-Drive-style highlight-on-select (card became a `<button>`, kebab repositioned as a sibling to avoid HTML's no-nested-`<button>` rule); a real bug where the bulk Share panel never closed (a standalone flag no other bulk action reset) was fixed with a `closeBulkSharePanel()` helper wired into every other action plus a `selectedIds.size > 0` gate as a second line of defense; "Share with specific people" switched from listing the entire staff directory to a search-to-add picker (`renderPersonPicker`, mirroring the Credentials & Links "Add" modal's existing pattern) shared by both the file Permissions panel and the bulk Share panel; and the file Permissions panel gained an explicit close button. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean throughout → `_docs/task/141-storage-kb-drive-style-folders-bulk-actions.md` (completed: 2026-07-14)
- [129] Kickoff Step Finalization — Multi-Contact Field, Business Facts Attachments, Validated Completion Checklist & Two-Column Layout — reworked the Kickoff step's data shape and completion mechanics on top of task 128's styling pass. Removed `seniorContact`/`customerData`; added a repeatable `ContactsField` (primary contact non-removable, per-row email/phone validation, icon-only outline add/remove controls) syncing the first contact to `customers.contact_name`/`contact_email` on every autosave; renamed `directAccess` → `additionalNotes` with a read-time fallback so already-saved content isn't lost; added URL validation to Website/Competitor fields; gave Business Facts a file-attachment capability (with a remove button, reusing the existing `DELETE /api/customers/[customerId]/assets` endpoint already used elsewhere) required alongside its rich text content. The 3-item completion checklist reuses task 127's `onboarding_internal_deliverables` mechanism verbatim (zero changes to its auto-derive-parent-status logic) via two new migrations (`061_contacts_external_id_nullable.sql`, `062_backfill_kickoff_internal_deliverables.sql` — not yet applied, user opted to apply personally) plus a `seedAndStartProgramme` change so Kickoff starts "in_progress" automatically at onboarding start. Checklist items are a direct pending↔done toggle (a mid-task correction from the initial 3-state cycle, since "in_progress" is reserved for the parent step). Continue now gates on any incomplete checklist item via a two-step confirmation flow (list incomplete items → "Mark all as done" → if that would bypass Kickoff's contacts/business-facts validation, a force-bypass "Yes, proceed" vs. "Review" — which red-glows the invalid field(s), self-clearing once fixed) built from scratch this task, including catching and fixing a real bug where the red-glow border was being visually overridden by the blue focus ring on the same element. On Phase-1 hand-off, kickoff contacts now bulk-insert into the `contacts` table as `match_method: 'manual'`. Verified extensively live in the browser across many rounds of user feedback, including creating a throwaway test project ("QA Test Co 129") to verify seed-time behavior unaffected by the still-unapplied migrations, and confirming the primary-contact sync against real DB data. `npx tsc --noEmit` and `pnpm lint` clean throughout; migrations still need to be applied by the user before the checklist/hand-off-contacts paths work on already-in-progress projects → `_docs/task/129-kickoff-contacts-checklist-two-column-redesign.md` (completed: 2026-07-10)
- [127] DeliverableCard — Progress-Ring Redesign, Auto-Status from Checklist, Click-to-Wizard-Step & Hover Detail Popover — redesigned the 120-Day Programme Gantt's `DeliverableCard` (task 125's view) from a status-icon pill into a progress-bar card: filled pie/ring indicator (outer ring + gap + wedge, checkmark at 100%) + title + `%`, two-tone solid-fill/diagonal-stripe-track background in the card's phase color. Clicking a Phase-1 card no longer cycles status — it opens the Onboarding Wizard directly on that deliverable's step (`initialStepKey` prop); non-Phase-1 cards stay inert. For sub-phases with an internal checklist, status is now derived automatically (new side effect on `PATCH .../internal-deliverables/[deliverableKey]`, which recomputes and returns the parent deliverable alongside the internal row) instead of manually toggled — `WizardDeliverableRow` goes read-only for those steps. Added a hover popover (title/description/owner/calendar dates/%, no chart) separate from the existing click-triggered internal-checklist badge popover, which itself was changed post-launch (live user feedback) to navigate to the owning wizard step on click instead of toggling status directly. Iterated the progress-ring visual through 6 rounds of live feedback (stroke ring → filled pie with outer ring, matched in-progress/completed lightness, per-character split text color at the fill boundary, fixed an invisible-ring bug from an unfilled transparent gap exposing the card's own same-hue background) plus a scroll regression fix: task 126's wheel-to-horizontal-pan listener was attached via a `useEffect` keyed to state unrelated to the scroll container's DOM lifecycle, so "Jump to phase" (or any re-render not touching that state) could leave it permanently unattached — moved attach/detach into the scroll container's ref callback so it's self-healing on every render. `npx tsc --noEmit` and `pnpm lint` both pass clean; every change was exercised live in the browser against the real "Acme Testing Co Website" project throughout, not just typechecked, with no console errors at any point → `_docs/task/127-deliverable-card-progress-ring-wizard-redirect.md` (completed: 2026-07-10)
- [126] Onboarding Detail Gantt — Badge Clipping Fix, Wheel-to-Horizontal-Scroll, Calendar-Day Programme Day Fix — fixed three bugs in the 120-Day Programme Gantt (task 125's view): (1) the Internal Deliverables `ListChecks` badge was clipped on track-0 cards because the Swimlane row was `overflow-scroll` with no top clearance — switched to `overflow-visible` plus an 8px `LANE_TOP_PADDING` (adjusts `laneHeight` to match); (2) horizontal scroll only worked via scrollbar drag, since nothing converted vertical wheel/trackpad input into `scrollLeft` — added a native (non-passive) `wheel` listener on `scrollRef` that pans the grid on wheel input while hovered, skips interception on `ctrlKey` (pinch-zoom) or when there's no horizontal overflow, and leaves the page's normal vertical scroll untouched outside the grid; (3) the "Day N" indicator was stuck a day behind because `getCurrentProgrammeDay` floor-divided the raw millisecond gap from a full `programme_started_at` timestamp — since that timestamp carries a time-of-day, the day only ticked over once today's clock caught up to the original start time, not at calendar midnight. Fixed by diffing midnight-normalized calendar dates instead. `npx tsc --noEmit` and `pnpm lint` both pass clean; manual browser verification of the wheel-scroll/badge-visibility/Day-N behavior was left to the user rather than independently re-verified this session → `_docs/task/126-onboarding-detail-badge-clipping-wheel-scroll-day-fix.md` (completed: 2026-07-10)
- [124] New Project — Full-Page Setup Wizard (replaces onboarding intake modal) — replaced the onboarding module's "New Project" intake modal (task 123) with a full-page, 3-step animated wizard at `/v2/onboarding/new` (Company & Contact → Project Details → Review & Create), styled to match `_design/customers/CustomerOnboarding.tsx`'s exact palette/font/Motion animations but reimplemented with Tailwind arbitrary-value classes instead of inline `style={{}}`, scoped Space Grotesk/Inter/JetBrains Mono fonts to this one route only (rest of v2 hub stays Sora/`bg-brand`). All form state, validation, debounced customer search, and the `POST /api/onboarding/projects` submit contract (`save`/`save_scheduled`/`start` modes) were ported verbatim from the deleted modal — no business-logic changes. Deleted the now-dead `fetchProjects` `useCallback` in `_onboarding-list.tsx` as a direct consequence of removing the modal's `onCreated` callback. Browser-verified live end-to-end (new-company creation → "Just Save" → success screen → project detail → "Draft" card on the list); `Save + Set Schedule`/`Start Onboarding` share the same submit code path but weren't independently live-submitted to avoid extra writes against what turned out to be live-looking Supabase data → `_docs/task/124-new-project-wizard-full-page-redesign.md` (completed: 2026-07-09)
- [121] Fix Lint Errors + Warnings (`pnpm lint`) — cleared all 44 original problems (8 errors, 36 warnings) plus a few more surfaced mid-implementation; `pnpm lint` and `npx tsc --noEmit` both pass clean. Ignored the PWA-generated custom-worker file in `eslint.config.mjs` instead of hand-editing generated code. Fixed all 6 `react-hooks/set-state-in-effect` errors (a newly-strict rule) with three different patterns depending on cause: `useSyncExternalStore` for hydration-safe one-time reads (theme, greeting, date), derived state instead of a stored loading flag (KB page), and an inlined fetch effect for the users page — one attempt (`TimerButton`) needed a second pass after tripping a *different* rule (`react-hooks/purity`, calling `Date.now()` during render). Found and removed a real bug in passing: `zohoPortalId`, a server-only env var per CLAUDE.md, was being serialized into a Client Component prop for no reason across three separate page.tsx callers (one of which — a cross-route-group import — wasn't in original scope, caught by `tsc`). Several unused-prop removals cascaded further up their call chains than originally planned (`v2-hub-shell.tsx`/`layout.tsx`'s `email`; `admin-dashboard.tsx`/`dashboard-view.tsx`'s `displayName` forwarding to `PMDashboard`; `tasks-tab.tsx`'s `customers` prop, which traced all the way back to a now-removed dead Supabase query in `dashboard/tasks/page.tsx`) — each verified fully dead before removal. Browser-verified the theme toggle (`useSyncExternalStore` rewrite, the riskiest change since it touches hydration) at `/auth/login`: toggles correctly, persists across reload, zero console errors. The remaining behavior-sensitive changes (Ops Chat greeting, `/kb` loading transition, `/v2/dashboard/users` load+refresh, project list view task timer) live behind the auth guard and were not independently browser-verified in this session (no test credentials available) → `_docs/task/121-fix-lint-errors-warnings.md` (completed: 2026-07-09)
- [120] v2 Customer Profile — Select/Deselect Primary Contact from Desk Contacts + Back-to-List Link — added Set/Remove Primary controls to each Desk Contacts row (task 119), writing straight into the existing `customers.contact_name`/`contact_email` fields rather than a new schema concept — resolved by research: those fields are already read by `src/lib/ai/reply.ts` for AI reply personalization and already power the Edit modal, so a parallel `is_primary` flag on `contacts` would have forked the source of truth for no benefit. Primary Contact block now shows Phone/Title when the current contact matches a Desk contact, resolved client-side with no extra fetch. Found and fixed a real pre-existing bug in passing: `validateCustomerUpdate` crashed (`null.trim()`) whenever `contact_email` was explicitly `null` — a path the existing Edit modal already hits on every save with a blank email field (true for the vast majority of customers before this feature existed), and one this task's "Remove Primary" action depends on directly. Also added a "← Back to Customers" link above the profile header for faster navigation. User-confirmed working in browser → `_docs/task/120-v2-customers-select-primary-contact.md` (completed: 2026-07-08)
- [119] v2 Customers — Show Matched Zoho Desk Contacts (List + Profile) — new `GET /api/customers/[customerId]/contacts` route (session-scoped, relies entirely on task 117's `contacts_staff_read` RLS policy, no manual role-filtering needed unlike the Assets route); v2 Customers list gets a scoped `contacts` count query alongside the existing `projects`/`customer_products` lookups, rendering a plain "+N Desk contact(s)" line under the Contact column; profile page's Contact tab gets a new "Desk Contacts" block (name, title, email, phone/mobile) below the existing Primary Contact section, lazy-fetched on tab-open mirroring the Assets tab's exact fetch-once pattern — no new nav tab or `NavSection` value needed. v1 untouched (list view is a structurally different, pre-task-116 component) and the 1,427 unmatched Desk contacts stay out of UI scope, per user decision. User-confirmed working in browser against real data → `_docs/task/119-v2-customers-show-desk-contacts.md` (completed: 2026-07-08)
- [117] Zoho Desk Contacts — Export, Import & Customer Matching — new Zoho Desk API client (`src/lib/zoho/desk.ts`, separate base URL + required `orgId` header, reuses `fetchZohoWithRetry`'s throttle handling) plus two export routes (Desk Accounts, Desk Contacts) and an import route into a new `contacts` table, matching each contact to a `customers` row by normalizing the Desk account name against `company_name`; unmatched contacts import anyway with `customer_id: null` as a review queue, by design (data-only scope — no UI this round). Post-review fix: renamed `zoho_desk_contact_id`/`zoho_desk_account_id` to generic `external_id`/`external_account_id` (migration 058) to match the `issues`/`milestones`/`tasklists` convention for one-time-import dedupe keys, since baking "zoho_desk" into a table meant to outlive the Zoho decommission was inconsistent (unlike `projects.zoho_project_id`, which stays live-synced and keeps its explicit name). That rename migration was originally numbered 057 and collided with an unrelated, concurrently-created migration (task 118's) claiming the same version — resolved by renumbering to 058 after confirming via a direct schema check that the rename hadn't actually applied yet. Live run on real data: 6,143 Desk accounts and 1,627 Desk contacts exported; import matched 200/1627 by account name (1,427 unmatched — mostly Desk's years of prospect/historical accounts beyond the Hub's 203 active customers, plus contacts with no Desk `accountId` at all), 0 errors — independently re-verified against the live table via direct PostgREST reads, not just the route's own log line → `_docs/task/117-zoho-desk-contacts-export-import-matching.md` (completed: 2026-07-08)
- [118] Customer Assets — Real File Upload, Multi-Field Credentials & Role Permissions — replaced the broken "File" type (previously just a mislabeled URL text box) with a real upload flow into a new private `customer-assets` storage bucket, viewed via on-demand short-lived signed URLs; "Credential" assets now support multiple `{label,value}` sub-fields (username/password/email/etc.) instead of one; every asset type gets an `allowed_roles` visibility pill group (All/Super Admin/Admin/PM/Developer), enforced via `profiles.role` API-level filtering on both GET and DELETE — chosen over introducing this codebase's first per-row RLS policy, after resolving a discovered conflict between the `hub_users.role` and `profiles.role` systems in favor of the latter (matches all existing RLS/CLAUDE.md convention). Applied identically to both the v1 and v2 (task 115) copies of the 2174-line customer profile client component, verified byte-identical throughout via direct file diffing. Post-handoff follow-up (user-tested, requested one refinement) made the modal's helper text dynamic per asset type: accepted file types + 25MB limit for File, example use cases for Link/Credential → `_docs/task/118-customer-assets-file-upload-multifield-role-permissions.md` (completed: 2026-07-08)
- [114] Zoho Issue Attachments Import: `issue` Entity Type + Manual-Match Upload Route — extended the `attachments` table's polymorphic entity_type constraint to include `'issue'` and built an issue-scoped sibling of task 106's manual-match uploader, matching on a `(name, size)` compound key (a real improvement over task 106's name-only matching, since the 629-file Issue dataset has genuine duplicate filenames the 40-file Task dataset never did). Live migration surfaced a chain of real bugs beyond the original implementation: `next.config.ts`'s upload body limit (50MB → 2GB) was far too small for the 1.6GB combined dataset; a matching-key bug where Chrome's local `" (N)"` dedup suffix broke lookups against bare Zoho names (fixed by stripping it) — followed by a second bug where the fix was *too* aggressive and mangled genuinely parenthetical real filenames like `"1 (6).jpg"` (fixed by trying the raw name first, falling back to stripped only if needed); the `project-assets` bucket's 50MB file-size limit (task 106's own setting) rejected several 60-120MB files, requiring both a bucket-level migration (055) and — discovered only after that still didn't work — a separate Supabase Dashboard-level "Global file size limit" project setting that migrations can't touch at all. The ~43 files originally scoped as an "accepted, unresolvable gap" (identical name *and* size across multiple issues) turned out to be fully resolvable by hand once the user identified which real issue each belonged to — three one-off scripts (`scripts/import-ambiguous-issue-attachments.ts`, `fix-line-card-electrical-linked-mixup.ts`, `import-batch2-fixes.ts`) inserted all of them directly, including catching and correcting a self-inflicted bug where the first script assumed same-name-within-a-group always meant same-bytes and briefly corrupted one already-correctly-imported file. Final state: 623 of 629 Issue Attachments fully correct end-to-end; 6 large files have DB rows but are pending a user-deferred manual re-upload once the Dashboard size limit is raised → `_docs/task/114-zoho-issue-attachments-import.md` (completed: 2026-07-08)
- [113] Zoho Issue Attachment Metadata Export: Per-Issue SSE Streaming (entity_type: "bug") — new export route, issue-scoped sibling of the Task attachment-meta export; the one functional delta (`entity_type: "bug"` instead of `"task"`) was user-directed and not independently doc-confirmed beforehand, so the spec required proving it against real data before trusting it. Live run across two slices (0-500, 500-1049 issues) confirmed it: 629 attachment records across 308 of 1,049 issues, every raw record's own `entity_type` field reading back `"bug"`, zero overlap between slices, 100% tagged with `_zoho_issue_id`/`_zoho_project_id`. Also surfaced the same known Zoho quirk task 106 already had to handle (sentinel `attachment_id: "-1"` on 50/629 records, real uniqueness via `third_party_file_id`) — flagged for whichever future task builds the Issue Attachments import/upload follow-up → `_docs/task/113-zoho-issue-attachment-meta-export.md` (completed: 2026-07-07)
- [112] Zoho Issue Time Logs Import: `issue_id` Column + Chunked SSE Upsert — reused the existing `time_logs` table with a new nullable `issue_id` column (migration 053) instead of a new dedicated table, since `time_logs.task_id` was already nullable and 0 `external_id` collisions were confirmed between the 14,533 Task time-log rows and 2,185 Issue time-log rows. New import route mirrors task 103's Task Timelogs importer exactly (multi-file scan, `stripHtml`/notes-fallback, chunked SSE upsert); caught during implementation (by reading the actual shipped Task Timelogs route rather than trusting the spec's sketch) that the `issues` lookup query needed the same `PAGE = 1000` pagination loop as `tasks`/`taskMap`, since `issues` has 1,049 rows — over Supabase's 1000-row default cap, the same bug class task 103/110 hit. Live run: 2,185 imported, 0 skipped, 0 errors → `_docs/task/112-zoho-issue-timelogs-import.md` (completed: 2026-07-07)
- [111] Zoho Issue Time Logs Export: Per-Issue SSE Streaming — near-literal mirror of the Tasks Timelogs export (task 102), diffed line-by-line against `zoho-export/timelogs/route.ts` to confirm byte-identical `fetchZohoWithRetry`/`sleep(700)`/`sleep(100)` throttle pacing, with the one functional delta (`module.type: "issue"`) confirmed via official Zoho API docs. No `log_hours` pre-filter exists on issues, so all 1049 issues get queried per run. Live run (two project-slice runs, `from=0&to=50` and `from=0&to=100`) completed cleanly with no unhandled errors: 2,185 total time-log entries across 73 of 78 projects (the other 5 simply logged zero time on issues), zero project-slice overlap between the two downloaded files, 100% tagged with `_zoho_project_id` → `_docs/task/111-zoho-issue-timelogs-export.md` (completed: 2026-07-07)
- [110] Zoho Issue Comments Import: New `issue_comments` Table + Import Route — dedicated table (migration 052, `issue_id` FK to `issues` not `task_comments`), `resolveIssueId()` helper, chunked-upsert import route with a first-of-its-kind `upsertChunkWithRetry` (bounded retry for transient Supabase write failures, unrelated to Zoho's throttle). Live run initially returned 2185 imported/100 skipped/100 errors ("no Hub issue found") — investigated by cross-checking a failing issue against the raw export (confirmed it existed, ruling out data corruption); root cause was an unpaginated `.select()` on the 1049-row `issues` table silently truncated by Supabase's 1000-row default cap, the same bug class already fixed once in task 103. Fixed by copying that established paginated-loop pattern; re-run imported all 2285 comments cleanly → `_docs/task/110-zoho-issue-comments-import.md` (completed: 2026-07-06)
- [109] Zoho Issue Comments Export: Per-Issue SSE Streaming — new SSE export route mirroring Task Comments export's per-entity/pagination mechanism, scoped to issues via `_from_zoho/issues-*.json`; upgraded to `fetchZohoWithRetry` instead of Task Comments' older inline 429-only retry. Live run tripped Zoho's rolling throttle at issue 200/1049 twice (first at `sleep(200)`, then again at `sleep(700)` after a proactive pacing fix that made no measurable difference to the trip point) — proved the "200 req/2 min" window assumption inherited from tasks 104/105 doesn't hold for this endpoint. Resolution required no further code change: `fetchZohoWithRetry`'s existing bounded backoff (9/12/15min) carried the run through the throttle automatically. Re-run: 2285 comments exported, 0 errors → `_docs/task/109-zoho-issue-comments-export-sse.md` (completed: 2026-07-06)
- [108] Zoho Issues Import: New `issues` Table + Import Route — new `issues` table (migration 051, `task_id` FK reserved for future linkage) + import route mirroring Tasks import's lookup-map/chunking pattern. Live run exposed an N+1 query bug (per-issue `resolveProjectId`+`upsert`, 282 sequential round-trips → 2.4min with zero UI feedback, looked hung); fixed by pre-building a project lookup Map (1 query) + chunked upserts (matches `tasks/route.ts`, not the `milestones/route.ts` pattern it was first modeled on). Also caught and fixed a missing `super_admin` RLS grant before shipping (same bug class migration 048 patched elsewhere). Re-run: 1049 issues imported, 0 errors → `_docs/task/108-zoho-issues-import.md` (completed: 2026-07-03)
- [107] Zoho Issues Export: Per-Project SSE Streaming (with From/To/Since) — added "Issues" export card mirroring Tasks; corrected mid-build from an assumed portal-wide endpoint to the real per-project `/projects/{projectId}/issues` shape (page/per_page required), added `fetchZohoWithRetry` + `failed_project_ids` visibility, then added From/To/Since inputs matching the Tasks card. Live run: 141 issues across 50 projects exported cleanly, verified same array+`_zoho_project_id` shape as `tasks-0-50-2025.json` → `_docs/task/107-zoho-issues-export-per-project-sse.md` (completed: 2026-07-03)
- [106] Attachments Bulk Upload: Manual-Match Uploader — replaced broken Zoho auto-fetch (OAuth-scope/CORS/IAM-ticket blocked, confirmed via live diagnostics) with file-picker + filename-match import; fixed external_id bug (third_party_file_id vs always-"-1" attachment_id); live run surfaced + fixed a missing proxyClientMaxBodySize config and a never-provisioned project-assets Storage bucket (migration 050) — 40/40 real attachments imported and verified linked → `_docs/task/106-attachments-bulk-upload-manual-match.md` (completed: 2026-07-03)
- [103] Timelogs Import: Field-Mapping Fixes, Multi-File Scan & SSE Upsert — fixed log_hour/module_detail/billing_status/notes mapping bugs, pre-built lookup maps (fixed Supabase 1000-row pagination gap), chunked SSE upsert + migrate page UI → `_docs/task/103-timelogs-import-multi-file-field-mapping-sse.md` (completed: 2026-07-02)
- [097] List View: Inline Subtask Expand/Collapse — tree rendering, depth indentation, pre-expand on load, count badge + import Pass 2 pagination fix → `_docs/task/097-list-view-subtask-inline-expand.md` (completed: 2026-06-30)
- [096] Tasks List View Redesign — Zoho-style table, inline pickers, timer, real name chips + assignees backfill migration → `_docs/task/096-tasks-list-view-redesign.md` (completed: 2026-06-30)
- [095] v2 HR > Users Tab: List Users, Manage Role/Status, Send Invite + Register Page → `_docs/task/095-v2-hr-users-tab-management-invite.md` (completed: 2026-06-30)
- [094] hub_users Schema: external_id rename, first_name/last_name split, role system, invite flow, trigger fix → `_docs/task/094-hub-users-schema-rename-roles.md` (completed: 2026-06-30)
- [091] List View: Status Normalization, Priority Fix & Zoho-Style UI Overhaul → `_docs/task/091-list-view-status-priority-fix-ui-overhaul.md` (completed: 2026-06-29)
- [087] Project Detail Overhaul: Primary Tabs, Tasklist Grouping, Milestone Panel & Skeleton → `_docs/task/087-project-detail-list-default-milestone-panel.md` (completed: 2026-06-29)
- [086] Fix profiles.role for Existing Staff Users — Seed from hub_users via migration 040 → `_docs/task/086-fix-profiles-role-seed-from-hub-users.md` (completed: 2026-06-29)
- [055] New `customer_projects` Table — Move Project Fields Off `customer_products` + Fix Auto-Zoho-Creation → `_docs/task/055-customer-projects-table.md` (completed: 2026-06-08)
- [044] Zoho Employee Pending Approval Flow → `_docs/task/044-zoho-employee-pending-approval-flow.md` (completed: 2026-06-02)
- [043] Auth Callback: Zoho Portal User Role Determination → `_docs/task/043-auth-callback-zoho-role-determination.md` (completed: 2026-06-02)
- [026] Sprint 4 Part 2 — Full Zoho Sync (M7 complete) → `_docs/task/026-sprint-4-full-zoho-sync.md` (completed: 2026-05-27)
- [025] Sprint 4 — Plan Generation (M5) → `_docs/task/025-sprint-4-plan-generation.md` (completed: 2026-05-27)
- [024] Sprint 3 Digest Gaps — Clarification Flag, Automation Queue, Unassigned Tasks → `_docs/task/024-sprint-3-digest-gaps-clarification-automation-queue-unassigned.md` (completed: 2026-05-27)
- [023] Dev Digest — Type-Aware Queries, Dev Prompt & Cliq Dev Channel → `_docs/task/023-dev-digest-type-aware-queries-cliq-channel.md` (completed: 2026-05-27)
- [022] Sprint 3 — Requirements Assessment (M3) + Daily Digest (M4) → `_docs/task/022-sprint-3-requirements-assessment-daily-digest.md` (completed: 2026-05-27)
- [021] Onboarding Submission Flow — PM Notification, Zoho Project Dialog & Status Transitions → `_docs/task/021-onboarding-submission-pm-notification-zoho-project-dialog.md` (completed: 2026-05-27)

- [019] CiteForge Badge — Customers Table + Customer Details Add-On Card → `_docs/task/019-citeforge-badge-display.md` (completed: 2026-05-25)
- [018] Fix Public Onboarding — CiteForge Filter + Product Sub-Routes → `_docs/task/018-public-onboarding-product-subroutes.md` (completed: 2026-05-25)
- [017] CiteForge as StackShift Add-On → `_docs/task/017-citeforge-stackshift-addon.md` (completed: 2026-05-25)
- [016] M1 Onboarding Completion — Missing Fields View, Product Metadata Edit, Add Product → `_docs/task/016-m1-onboarding-completion.md` (completed: 2026-05-25)
- [015] Wire Cliq Webhook Token into sendCliqNotification → `_docs/task/015-wire-cliq-webhook-token.md` (completed: 2026-05-25)
- [014] Sprint 2 — Live Stats, Customer Profile Classifications & Pipeline Classify Column → `_docs/task/014-sprint-2-live-stats-customer-classifications-pipeline.md` (completed: 2026-05-25)
- [013] Sprint 2 — Classification Engine, PM Tasks UI, Cliq & Zoho Stubs → `_docs/task/013-sprint-2-classification-engine-pm-ui.md` (completed: 2026-05-25)
- [012] PM Dashboard: Sub-Routes, Tailwind Cleanup & Active Indicator → `_docs/task/012-pm-routes-tailwind-cleanup.md` (completed: 2026-05-25)
- [011] Sidebar Cleanup, Inline Style Migration & Dynamic Greeting → `_docs/task/011-sidebar-cleanup-dynamic-greeting.md` (completed: 2026-05-25)
- [009] PM Dashboard: Apply WebriQ Design System → `_docs/task/009-pm-dashboard-design-system.md` (completed: 2026-05-25)
- [005] Per-Product Progress Bars (Real-Time) on PM Dashboard → `_docs/task/005-per-product-progress-realtime.md` (completed: 2026-05-25)
- [004] Fix: Public Onboarding Route Auth Wall → `_docs/task/004-fix-public-onboarding-route.md` (completed: 2026-05-25)
- [003] Sprint 1: Customer Creation & Onboarding (M1) → `_docs/task/003-sprint-1-customer-onboarding.md` (completed: 2026-05-25)
- [020] Auto-Transition Customer to Active + Create Zoho Project on Full Onboarding Completion → `_docs/task/020-auto-active-status-zoho-project.md` (completed: 2026-05-21)
- [010] Sidebar Nav Restructure + Header User Menu + AI Chat → `_docs/task/010-sidebar-nav-restructure-header-menu-ai-chat.md` (completed: 2026-05-15)
- [008] Admin Force-Logout (All Users Except One) → `_docs/task/008-admin-force-logout.md` (completed: 2026-05-15)
- [007] Fix Zoho Auth: Display Name & Zoho User ID → `_docs/task/007-fix-zoho-auth-display-name-user-id.md` (completed: 2026-05-15)
- [006] Sprint 1.1: Zoho OAuth + Email/Password Auth → `_docs/task/006-sprint-1-1-zoho-auth.md` (completed: 2026-05-07)
- [002] Resolve Sprint Plan Gaps Before Sprint 0 Kickoff → `_docs/task/002-resolve-sprint-plan-gaps.md` (completed: 2026-05-04)
- [001] Sprint 0: Infrastructure Foundation → `_docs/task/001-sprint-0-infrastructure.md` (completed: 2026-04-27)

## Cancelled
