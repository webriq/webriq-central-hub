# WebriQ Central Hub — Task Tracker

## Planned

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 194 | Task & Issue Detail Pages — Design System v2.0 Redesign + Proper HTML Title/Description Rendering (fixes raw `&amp;` entities and unrendered HTML in descriptions across detail pages + listing/board/calendar views) — task-detail scope shipped via 206; issue-detail scope shipped via 234; remaining listing/board/calendar/milestone-detail scope now covered by 258 | HIGH | enhancement | _docs/task/194-task-issue-detail-design-system-v2-html-rendering.md | 2026-07-28 |
| 177 | Security Sweep #2 — #4-9: Remaining Authorization Gaps (products PATCH, reopen-onboarding, primary-contact, reply/send, assets POST, customers GET) | HIGH | security | _docs/task/177-security-sweep-2-remaining-authorization-gaps.md | 2026-07-22 |
| 170 | Zoho Issue (Bug) Comment Attachment Metadata Export | MEDIUM | feature | _docs/task/170-zoho-issue-comment-attachment-meta-export.md | 2026-07-21 |

## In Progress

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 048 | Onboarding: Submit-Only Cliq Notification, Form Lock After Submit, PM Responses View & Reopen | HIGH | minor | _docs/task/048-onboarding-submit-lock-pm-responses-view.md | 2026-06-02 |
| 056 | Enhance Hub Create Task Modal — Project + Tasklist Selection | HIGH | minor | _docs/task/056-enhance-create-task-modal-zoho-project-tasklist.md | 2026-06-08 |

## Testing

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 258 | Fix Unrendered HTML Entities (`&amp;` etc.) in Task/Issue/Milestone Listing Views — closes task 194's remaining Requirement A scope (listing/board/calendar/milestone-detail title-decoding; task-detail and issue-detail halves already shipped via 206/234) | MEDIUM | bug | _docs/task/258-listing-views-html-entity-decode.md | 2026-08-17 |
| 257 | Issue Detail Page — Description Enhancement, Accordion Sections, Image Preview, Comment/Attachment Fixes, Tab Redesign, Quick Access Panel | HIGH | enhancement | _docs/task/257-issue-detail-description-accordion-quick-access-redesign.md | 2026-08-17 |

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 255 | v2 → Root Migration — Retire Legacy `(hub)`/`(auth)` (rename to private folders excluded from routing), Promote `v2/(hub)`/`v2/(auth)` to App Root (`/v2/dashboard` → `/dashboard` etc.), `/v2/*` Redirects, Fix Cross-Route-Group Imports/Hardcoded `/v2/` Path Strings | HIGH | refactor | _docs/task/255-v2-to-root-migration-legacy-hub-retirement.md | 2026-08-17 |
| 252 | Portfolio Tracker — Skip-Phase Timeline Compression (StackShift I, shipped) + Generic-Engine Swimlane/Overview Parity (Access/Access Plus/Discrete Development/StackShift II w/o opt-in) — day-range migration on milestones/tasklists, free-form intake day-range UI, day-based Gantt rebuild of GenericSwimlane, Jump-to-phase backdating | MEDIUM | enhancement | _docs/task/252-portfolio-tracker-skip-timeline-compression-generic-swimlane-parity.md | 2026-08-14 |
| 251 | New Project Form — Scheduled Start Validation, StackShift I Phase Collapse-by-Default, Generic-Engine Scheduled/Draft Screen Parity | MEDIUM | enhancement / bug fix | _docs/task/251-new-project-form-schedule-validation-phase-collapse-generic-not-started-screen.md | 2026-08-14 |
| 249 | New Project Form — Per-Phase Custom Duration Days + Auto-Distributed Deliverable Day Ranges (largest-remainder distribution, insert-in-middle cascade); depends on 248 | MEDIUM | feature | _docs/task/249-new-project-form-per-phase-duration-deliverable-distribution.md | 2026-08-14 |
| 248 | Portfolio Tracker "Not Started"/"Scheduled" Screen — Skip-Aware Dynamic Start CTA + Jump-to-Phase (closes a confirmed data-loss gap where task 244/246's skip/custom-phase intake config is silently discarded for Draft/Scheduled saves) | HIGH | enhancement | _docs/task/248-portfolio-tracker-not-started-screen-skip-aware-dynamic-cta.md | 2026-08-14 |
| 247 | Portfolio Tracker — Non-StackShift I Dynamic Phase View (not-started state, Jump-to-phase, progress bar/overview/swimlane driven by `milestones`/`tasklists`/`tasks` instead of the StackShift I `customer_phases` engine), Delete Rename (`_deleted_<date>` suffix on soft delete), Default Sort by Creation Date | HIGH | enhancement | _docs/task/247-portfolio-tracker-non-stackshift-dynamic-phase-view-delete-rename-sort.md | 2026-08-14 |
| 243 | Fix Phase 2-5 Milestone/Tasklist `external_id` Collision Across Projects (unscoped `external_id` on `milestones`/`tasklists` causes `23505` duplicate-key errors for every project after the first to start its programme, silently dropping Phase 2-5 deliverable→tasklist links) | HIGH | bug | _docs/task/243-programme-phase-2-5-external-id-collision-fix.md | 2026-08-13 |
| 242 | Projects Module — Milestone Swimlane View + Deliverable-to-Tasks Navigation (lanes = milestones, cards = tasklists, click-through to Tasks tab); depends on 239 | MEDIUM | feature | _docs/task/242-projects-milestone-swimlane-view-tasks-navigation.md | 2026-08-13 |
| 241 | StackShift Timeline — Phase 2-5 Deliverable Cards Become Clickable, Redirect to Projects > Tasks (Phase 1/Onboard keeps its existing Onboarding Workspace destination); depends on 239 | MEDIUM | enhancement | _docs/task/241-stackshift-phase-2-5-swimlane-projects-tasks-redirect.md | 2026-08-13 |
| 240 | New Project Wizard — PipelineForge Add-on Section, Multi-Type Selection, Duration & Phase Builder (StackShift I/II/Access/Access Plus/Discrete Development as distinct trackers); depends on 239 | HIGH | feature | _docs/task/240-new-project-wizard-pipelineforge-addon-multi-type-builder.md | 2026-08-13 |
| 239 | New Project Backend — Multi-Type Creation Support, Configurable StackShift I Duration, Generic Phase Seeding (`milestones`/`tasklists`/`tasks` seeding for non-StackShift-I classifications) | HIGH | feature | _docs/task/239-new-project-backend-multi-type-duration-generic-phases.md | 2026-08-13 |
| 237 | Issue Detail — Time Logs Tab (View/Add/Edit Manual Entries); follow-up to 234, part 3/3 | MEDIUM | feature | _docs/task/237-issue-detail-time-logs-tab.md | 2026-08-13 |
| 236 | Issue Detail — Live Comments Tab (Rich Text + Attachments, new `issue_comments` insert/delete RLS); follow-up to 234, part 2/3 | MEDIUM | feature | _docs/task/236-issue-detail-comments-tab.md | 2026-08-13 |
| 234 | Issue Detail Page — Design System v2.0 Redesign, RBAC (Creator/Assignee via new `created_by`/`assignee_id` columns), Timer Support (extends the task-only timer subsystem to issues); closes task 194's remaining issue-detail scope | HIGH | enhancement | _docs/task/234-issue-detail-design-system-redesign-rbac-timer.md | 2026-08-13 |
| 235 | Issue Detail — Attachments Tab (Live Upload, Grid Viewer); follow-up to 234, part 1/3 of full Task-Detail parity | MEDIUM | feature | _docs/task/235-issue-detail-attachments-tab.md | 2026-08-13 |
| 226 | Dedicated Time Logs Page (`/v2/dashboard/timelogs`) — Day/Week/Month/Range Filters, Role-Scoped View-All vs Own-Only, Add/Edit Own Entries, Export to PDF | HIGH | feature | _docs/task/226-dedicated-time-logs-page.md | 2026-08-12 |
| 225 | Portfolio Tracker Owner Transfer Doesn't Sync Phase 1 Owner (Stale "Danessa" Assignee on Listing/Status Report/Status Summary After Setting Project Owner to "Dannea Moneva") + Status Summary Tooltip Behind Drawer (z-index) | HIGH | bugfix | _docs/task/225-portfolio-tracker-owner-transfer-phase-sync-tooltip-zindex.md | 2026-08-10 |
| 224 | Portfolio Tracker & Status Report — Filter UI Parity with `/v2/projects` (checkbox-dropdown `FilterMultiSelect` replacing single-select pill rows) + New Project Classification Filter (StackShift I/II/Access/Access Plus, PipelineForge, Discrete Development) | MEDIUM | enhancement | _docs/task/224-portfolio-tracker-status-report-filter-classification.md | 2026-08-10 |
| 221 | Portfolio Tracker Status Report — Fold Overdue Into Used/Allotted (color-coded overdue/remaining/ahead), Completed-Only Date Column, Early-Completion Health Status (green) | HIGH | enhancement | _docs/task/221-portfolio-tracker-status-report-page.md | 2026-08-10 |
| 222 | Swimlane Items Redirect to Onboarding Workspace (Not Wizard) + URL Params for Tabs/Folders (Kickoff→Business Info, Outcome Target/Migration Checklist/Content Map/HTML Mockup/Storage+KB/Client Sign-off→Files w/ folder, Migration Checklist folder rename) | HIGH | enhancement | _docs/task/222-swimlane-redirect-to-onboarding-workspace-url-params.md | 2026-08-07 |
| 220 | Files Tab Fixes — Nested-Button Hydration Errors, In-App Preview on View, Invisible Search/New-Folder Text, Enter-to-Save + Duplicate-Name Confirm, Equal Folder Tile Heights, Sub-Folders | HIGH | bugfix | _docs/task/220-files-tab-hydration-preview-input-visibility-subfolder-fixes.md | 2026-08-06 |
| 218 | Task Detail Page — Start Timer Button (Parity with List View) | MEDIUM | enhancement | _docs/task/218-task-detail-page-start-timer-button.md | 2026-08-06 |
| 217 | Onboarding Workspace v2 (`/v2/portfolio-tracker/[projectId]/v2`) — Design Redesign From `_final_design/Onboarding Workspace Design` (underline tabs, programme track, business-info field polish, file mgmt search/sort/version badges, upload progress queue, access masked values + undo-delete, checklist stats) | HIGH | enhancement | _docs/task/217-onboarding-workspace-v2-design-redesign.md | 2026-08-06 |
| 216 | Fix Task Comments Showing "Unknown" Author — Profiles RLS Gap | HIGH | bugfix | _docs/task/216-fix-comment-author-unknown-profiles-rls-gap.md | 2026-08-05 |
| 214 | Task Detail — "Time Logs" Tab (Check, Edit, Add Manual Entries) | HIGH | feature | _docs/task/214-task-detail-time-logs-tab-check-edit-add.md | 2026-08-05 |
| 212 | Task Comments — Rich Text Editor (Paste Images), File Attachments, Loading-Skeleton Verification | HIGH | enhancement | _docs/task/212-comment-rich-text-attachments-skeletons.md | 2026-08-05 |
| 211 | Task Detail Page — Column Swap, Attachments/Comments Tabs, Grid+Modal Attachment Viewer, Assignee Avatar Fix, Required Creation Dates | HIGH | enhancement | _docs/task/211-task-detail-layout-tabs-attachment-modal-assignee-avatars-required-dates.md | 2026-08-05 |
| 210 | Fix "Unnamed" Project Avatars & Raw-UUID Task Assignees — Profiles RLS Gap + Tooltip/Animation Parity | HIGH | bugfix | _docs/task/210-fix-unnamed-avatars-assignee-uuid-tooltip-parity.md | 2026-08-05 |
| 207 | Developer Dashboard — Design System v2.0 Redesign (Fix Theme Bug: Washed-Out Stats/Dark Task Cards) + Hide Tracker/Desk/Orchestration Nav Tabs (Developer Role Only) | HIGH | enhancement | _docs/task/207-developer-dashboard-design-system-v2-redesign-hide-nav-tabs.md | 2026-08-05 |
| 206 | Task Detail Page — Design System v2.0 Redesign, HTML Description Rendering, Comments, Attachments Viewer (Remove Subtasks/Labels; supersedes task 194's task-detail scope) | HIGH | enhancement | _docs/task/206-task-detail-page-redesign-comments-attachments.md | 2026-08-04 |
| 205 | "New Task" Modal — Rich Text Description (Image Paste), File/Image Attachments, Task List Select-or-Create, Developer Assignee | HIGH | enhancement | _docs/task/205-new-task-modal-richtext-attachments-tasklist-assignee.md | 2026-08-04 |
| 204 | Portfolio Tracker v2 Sandbox — "Proceed to Phase 2" Button on Checklist Completion (top-right CTA once all Phase 1 deliverables checked, reuses existing `complete-phase` API + PM notification) | MEDIUM | feature | _docs/task/204-portfolio-v2-sandbox-proceed-to-phase-2-button.md | 2026-08-04 |
| 202 | Onboarding Wizard v2 Sandbox — Tabbed Redesign (Business Info / Files / Access / Checklist) at `/v2/portfolio-tracker/[projectId]/v2` (new isolated route, zero edits to the existing wizard/task 200 gating) | HIGH | feature | _docs/task/202-onboarding-wizard-tabbed-file-management-redesign.md | 2026-08-04 |
| 198 | Storage Folder + KB Step — File Explorer Redesign (Tabs, Bigger Folders, Drive-Style Thumbnails, Contextual Drag & Drop) | HIGH | enhancement | _docs/task/198-storage-kb-step-file-explorer-redesign.md | 2026-07-29 |
| 193 | Project Detail — Real Sub-Routes for Tasks/Issues/Milestones (`/tasks`, `/issues`, `/milestones` + `/issues/[issueId]` & `/milestones/[milestoneId]` detail pages) + Empty-State Polish | HIGH | feature | _docs/task/193-project-tasks-issues-milestones-route-restructure.md | 2026-07-27 |
| 190 | Task Detail Route — `[taskId]` Segment UUID → `display_id` (mirrors task 188's `[projectId]` change; amends task 188/189's "taskId stays UUID" decision) | MEDIUM | refactor | _docs/task/190-task-detail-route-taskid-display-id.md | 2026-07-24 |
| 189 | Human-Readable `display_id` for Tasks & Issues (`<customer-hex><project-seq>-T####` / `-I####`, new column + trigger, no routing change) | MEDIUM | feature | _docs/task/189-task-issue-display-id-format.md | 2026-07-24 |
| 188 | Project Detail & Task Detail Routes — UUID → `project_id` (expands the portfolio-tracker routing-key exception to `/v2/projects/[projectId]`) | MEDIUM | refactor | _docs/task/188-project-task-detail-routes-project-id.md | 2026-07-24 |
| 187 | Customer ID 8-Hex Backfill (All Existing Rows, FK Cascade) + Incremental Per-Customer `project_id` Format (`<8-char>-PROJ-01,02,...`) | HIGH | refactor | _docs/task/187-customer-id-8hex-backfill-incremental-project-id.md | 2026-07-24 |
| 186 | Notification Drawer Stacking-Context Fix (Header z-10 Traps Drawer's z-99999) + Portfolio Tracker Sticky Header Parity | MEDIUM | bugfix | _docs/task/186-notification-drawer-zindex-fix-portfolio-tracker-sticky-header.md | 2026-07-24 |
| 183 | "Import Project" & "New Project" Wizards — Design System v2.0 Redesign (drop isDark, v2.0 tokens, fix classification-hue collision, remove gradients, CTA/blue/ghost button roles) | HIGH | enhancement | _docs/task/183-import-new-project-design-system-v2-redesign.md | 2026-07-24 |
| 181 | Remote MCP Server — OAuth 2.1/PKCE Scaffold + Two Read-Only Tools (get_project_status, list_open_tasks) | HIGH | feature | _docs/task/181-remote-mcp-server-oauth-scaffold.md | 2026-07-23 |
| 180 | OTP/2FA Verification Redesign — Digit-Box UI, Attempt Lockout (4 tries / 1hr), Forgot Password Flow, Super Admin Unlock | HIGH | feature | _docs/task/180-otp-2fa-redesign-attempt-lockout-forgot-password.md | 2026-07-23 |
| 178 | `/v2/(auth)` Audit — DRY Refactor, Error Handling & Guard Rails (shared shell/password-input/strength-meter, gate-cookie helpers, silent OTP failure fixes) | MEDIUM | refactor | _docs/task/178-v2-auth-audit-dry-error-handling-guardrails.md | 2026-07-22 |
| 173 | Notification Drawer — Design System v2.0 Restyle, Paginated Scroll, Read/Unread Emphasis, Bell Badge Polish | MEDIUM | enhancement | _docs/task/173-notification-drawer-design-system-v2-pagination-read-unread.md | 2026-07-22 |
| 172 | Onboarding Wizard — Per-Step Uploaded Files Vanish on Refresh (State Not Rehydrated From Server) | HIGH | bugfix | _docs/task/172-onboarding-wizard-per-step-file-persistence-refresh.md | 2026-07-22 |
| 169 | Zoho Task Comment Attachment Metadata Export | MEDIUM | feature | _docs/task/169-zoho-comment-attachment-meta-export.md | 2026-07-21 |
| 167 | Portfolio Tracker List — Design System v2.0, Consistent Card Height, "+ New Project" CTA, Phase-Agnostic Description, Search/Filter/Pagination | HIGH | enhancement | _docs/task/167-portfolio-tracker-list-design-system-v2-card-height-cta.md | 2026-07-21 |
| 165 | v2 Dashboard — Real Active-Feature Data, Ported Greeting, Marketing Dashboard, Space Grotesk/JetBrains Mono | HIGH | feature | _docs/task/165-v2-dashboard-real-data-greeting-marketing-dashboard-font-swap.md | 2026-07-21 |
| 164 | Service Worker — Push Event + Notification Click Handlers | HIGH | bugfix | _docs/task/164-service-worker-push-notification-handler.md | 2026-07-20 |
| 163 | In-App Notifications — Activate Header Bell + Wire Push Notification Prerequisites | HIGH | feature | _docs/task/163-in-app-notifications-bell-push-prerequisites.md | 2026-07-20 |
| 064 | Push Notifications (web-push + VAPID + subscription API) | HIGH | feature | _docs/task/064-push-notifications.md | 2026-06-19 |
| 162 | PM Dashboard — Wire Unwired Clickable Rows + Add Missing aria-labels | MEDIUM | bugfix | _docs/task/162-pm-dashboard-clickable-rows-aria-labels.md | 2026-07-17 |
| 157 | New Project — Multi-Select Project Type with StackShift Combination Rules | HIGH | feature | _docs/task/157-new-project-multiselect-project-type-stackshift-rules.md | 2026-07-15 |
| 149 | Onboarding Wizard — Drag-and-Drop Multi-File Upload | MEDIUM | enhancement | _docs/task/149-onboarding-wizard-dragdrop-multifile-upload.md | 2026-07-14 |
| 147 | Kickoff Contacts — Auto-Populate From New Project Setup Form | HIGH | bugfix | _docs/task/147-kickoff-contacts-autopopulate-from-new-project.md | 2026-07-14 |
| 146 | Onboarding Timeline & Wizard — PM/Developer Role-Based Access | HIGH | feature | _docs/task/146-onboarding-timeline-wizard-rbac-pm-developer.md | 2026-07-14 |
| 145 | Onboarding List — Stop Hiding Projects Once Phase 1 Is Done (Page Tracks All 5 Phases) | HIGH | bugfix | _docs/task/145-onboarding-list-show-all-phases-not-just-phase1.md | 2026-07-14 |
| 135 | Onboarding Wizard — Client Call, Sign-Off Step (Meeting Notes + Agreement Upload) | HIGH | enhancement | _docs/task/135-client-signoff-step-redesign.md | 2026-07-13 |
| 140 | Storage/KB — Add Credential/Link Assets (Add Asset Modal Pattern) | MEDIUM | feature | _docs/task/140-storage-kb-add-credential-link-assets.md | 2026-07-13 |
| 139 | Storage/KB File Explorer — Real Folder Look + Grid/List View Toggle (Default Grid) | MEDIUM | enhancement | _docs/task/139-storage-kb-explorer-folder-look-grid-list-view.md | 2026-07-13 |
| 138 | Customer Assets — Per-User Sharing (Alongside Existing Role-Based Permissions) | MEDIUM | feature | _docs/task/138-customer-assets-per-user-sharing.md | 2026-07-13 |
| 137 | Customer Assets — Per-Project Folder Separation in Supabase Storage | MEDIUM | refactor | _docs/task/137-customer-assets-per-project-storage-folders.md | 2026-07-13 |
| 136 | Phase 1 Completion — Animated Closing/Transition Screen (2.4 Completion Criteria) | HIGH | feature | _docs/task/136-phase1-completion-animated-closing-screen.md | 2026-07-13 |
| 134 | Onboarding Wizard — Storage Folder + KB Step Redesign (File Explorer / Finder UI with Permissions) | HIGH | feature | _docs/task/134-storage-kb-file-explorer-redesign.md | 2026-07-13 |
| 133 | Onboarding Wizard — HTML Mockup Step (Upload, In-App Preview, Code Editor) | HIGH | feature | _docs/task/133-html-mockup-step-upload-preview-editor.md | 2026-07-13 |
| 132 | Onboarding Wizard — 90-Day Content Map Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/132-content-map-step-redesign.md | 2026-07-13 |
| 131 | Onboarding Wizard — Migration Checklist Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/131-migration-checklist-step-redesign.md | 2026-07-13 |
| 130 | Onboarding Wizard — Outcome Target Step (Rich Text + Optional File Attachment) | HIGH | enhancement | _docs/task/130-outcome-target-step-rich-text-file-upload.md | 2026-07-10 |
| 128 | Onboarding Wizard — Kickoff Step Redesign, Rich Text Editor & Autosave Indicator | HIGH | enhancement | _docs/task/128-onboarding-kickoff-step-redesign-rich-text-editor.md | 2026-07-10 |
| 125 | Onboarding Project Detail — Horizontal Gantt Timeline Redesign (exact reference functionality + design, WebriQ style guide) | HIGH | feature | _docs/task/125-onboarding-detail-horizontal-timeline-redesign.md | 2026-07-09 |
| 123 | Onboarding Redesign — Dedicated Bert-Gated Phase 1 Module (New Project Intake, Project-Scoped Programme, Sub-Phase Timeline, PM Visibility Gate) | HIGH | feature | _docs/task/123-onboarding-redesign-bert-gated-phase1.md | 2026-07-09 |
| 122 | 120-Day Customer Programme — Timeline, Bert's Day 1-15 Onboarding Wizard, Reminders & v2 Customers Extension | HIGH | feature | _docs/task/122-120-day-programme-bert-wizard-notifications.md | 2026-07-09 |
| 116 | v2 Customers Listing — Search, Filter, Pagination & Skeletons | HIGH | enhancement | _docs/task/116-v2-customers-search-filter-pagination-skeletons.md | 2026-07-08 |
| 115 | v2 Customer Management & Onboarding — Full Port from v1 | HIGH | feature | _docs/task/115-v2-customer-management-onboarding-port.md | 2026-07-08 |
| 105 | Fix Attachments Export Throttle: Bounded Retry + Shared Zoho Retry Helper | HIGH | bugfix | _docs/task/105-fix-attachments-export-throttle-bounded-retry-shared-helper.md | 2026-07-03 |
| 104 | Attachments Export/Import Fix: Multi-File Scan, SSE, Failure Visibility + DB Index/Constraint | HIGH | feature | _docs/task/104-attachments-export-import-fix-index-constraint.md | 2026-07-02 |
| 102 | Timelogs Export: SSE Streaming + 429 Handling | HIGH | patch | _docs/task/102-timelogs-export-sse-streaming.md | 2026-07-01 |
| 101 | Comments Export: SSE Streaming + Pagination | HIGH | patch | _docs/task/101-comments-export-sse-pagination.md | 2026-07-01 |
| 100 | Super Admin: RLS Policies + Application-Layer Access | HIGH | patch | _docs/task/100-super-admin-rls-and-app-layer-access.md | 2026-07-01 |
| 099 | Fix Super Admin 403 on Admin API Routes | HIGH | bugfix | _docs/task/099-fix-super-admin-403-api-routes.md | 2026-07-01 |
| 098 | Add super_admin role to v2 hub (migration, invite, PATCH API, sidebar, users table) | HIGH | minor | _docs/task/098-add-super-admin-role.md | 2026-07-01 |
| 093 | Zoho Users Export + Import | HIGH | feature | _docs/task/093-zoho-users-export-import.md | 2026-06-30 |
| 092 | Task Detail Page — /v2/projects/[projectId]/tasks/[taskId] | HIGH | feature | _docs/task/092-task-detail-page.md | 2026-06-29 |
| 090 | Tasks Import: Multi-File, Full Column Set & Parent Resolution | HIGH | feature | _docs/task/090-tasks-import-multi-file-full-columns.md | 2026-06-29 |
| 089 | Tasks Export: SSE Progress, Project Slice & Date Filter | HIGH | feature | _docs/task/089-tasks-export-sse-progress-slice-date-filter.md | 2026-06-29 |
| 088 | Zoho Tasklists: Batch Import Fix + pg_cron Zoho API Sync | HIGH | feature | _docs/task/088-zoho-tasklists-batch-import-cron-sync.md | 2026-06-29 |
| 085 | Auth Overhaul — Invitation-Only Registration, Forced Password Change & Email OTP Step-Up | HIGH | feature | _docs/task/085-auth-overhaul-invitation-password-change-otp.md | 2026-06-29 |
| 084 | Zoho Milestones Export + Import + Schema Fix | HIGH | feature | _docs/task/084-zoho-milestones-export-import.md | 2026-06-26 |
| 083 | Projects Search: Server-Side Filtering + Real Pagination Total | HIGH | patch | _docs/task/083-projects-search-server-side-pagination-fix.md | 2026-06-26 |
| 082 | Projects Grid Card: Rotating Conic Gradient Border on Hover | NORMAL | patch | _docs/task/082-projects-grid-card-conic-border.md | 2026-06-26 |
| 081 | Projects Listing UI/UX Redesign — Grid/List toggle, status/type colors, owner, tags, completion ring, pagination, skeleton, DB indexes | HIGH | feature | _docs/task/081-projects-listing-ui-redesign.md | 2026-06-25 |
| 080 | Zoho Projects Import — Create New Hub Rows from projects.json | HIGH | feature | _docs/task/080-zoho-projects-import-create-new-rows.md | 2026-06-25 |
| 079 | Zoho Decommission — Full Data Migration to Supabase | HIGH | feature | _docs/task/079-zoho-decommission-data-migration.md | 2026-06-25 |
| 078 | Zoho Projects Export — GET /api/zoho/projects (Full Paginated Raw Dump) | HIGH | patch | _docs/task/078-zoho-projects-export-endpoint.md | 2026-06-25 |
| 077 | Login Redirect → /v2/dashboard + returnTo Deep-Link Restore | HIGH | enhancement | _docs/task/077-login-returnto-redirect.md | 2026-06-21 |
| 076 | Dashboard RBAC + Remove Duplicate "Dashboards" Nav Link | HIGH | enhancement | _docs/task/076-dashboard-rbac-remove-duplicate-nav.md | 2026-06-21 |
| 075 | Ops Chat: Full Task CRUD Tools (Create, Update, Assign, Delete) | HIGH | feature | _docs/task/075-ops-chat-task-crud-tools.md | 2026-06-21 |
| 074 | Ops Chat: update_task_status + update_classification_status Tools | HIGH | feature | _docs/task/074-ops-chat-update-task-status-tool.md | 2026-06-21 |
| 073 | Native PM Core — Projects/Milestones/Tasks/Subtasks + Kanban/List/Calendar | HIGH | feature | _docs/task/073-native-pm-core-projects-tasks.md | 2026-06-21 |
| 072 | Ops Chat Agent Activation (Claude + Vercel AI SDK + Sanity MCP) | HIGH | feature | _docs/task/072-ops-chat-agent-activation.md | 2026-06-21 |
| 071 | v2 Dashboard: Role-Aware PM/Dev/Admin Dashboards (Zoho Decommission + Design Refresh) | HIGH | feature | _docs/task/071-v2-dashboard-redesign.md | 2026-06-19 |
| 070 | Offline PWA Queue (IndexedDB service worker intercept) | NORMAL | feature | _docs/task/070-offline-pwa-queue.md | 2026-06-19 |
| 069 | Health Check + Rollback + Lane 3 Sequencing | HIGH | feature | _docs/task/069-health-check-lane3-sequencing.md | 2026-06-19 |
| 068 | KB Library (lookupKB, saveToKB, saveKBCorrection) | HIGH | feature | _docs/task/068-kb-library.md | 2026-06-19 |
| 067 | Orchestrator Route + KB Embedding Lookup | HIGH | feature | _docs/task/067-orchestrator-route-kb-lookup.md | 2026-06-19 |
| 066 | Sub-task Enumerator (sanity \| code \| both lane tagging) | HIGH | feature | _docs/task/066-subtask-enumerator.md | 2026-06-19 |
| 065 | Sanity MCP (Streamable HTTP) + Global Token + Preview URL | HIGH | feature | _docs/task/065-sanity-mcp-global-token-preview-url.md | 2026-06-19 |
| 064 | Push Notifications (web-push + VAPID + subscription API) | HIGH | feature | _docs/task/064-push-notifications.md | 2026-06-19 |
| 063 | KB Schema Migration (pgvector + kb_entries + task_logs + kb_corrections) | HIGH | feature | _docs/task/063-kb-schema-migration.md | 2026-06-19 |
| 062 | Sprint 0A: RLS Policies v1 + Auth Trigger | CRITICAL | feature | _docs/task/062-rls-policies-auth-trigger.md | 2026-06-11 |
| 061 | Sprint 0A: v2.0 Schema Migration — All New Tables + Rename customer_projects → projects | CRITICAL | feature | _docs/task/061-v2-schema-migration.md | 2026-06-11 |
| 060 | v2 Route Scaffold — Sprint 0A Foundation | CRITICAL | feature | _docs/task/060-v2-route-scaffold.md | 2026-06-11 |
| 059 | Fix Execution Engine 500 — Sanity `getDocuments([])` 404, Circuit Breaker Threshold, `maxTokens` Passthrough | HIGH | patch | _docs/task/059-fix-execution-engine-500.md | 2026-06-09 |
| 058 | Edit Project Modal — Edit Button on Project Cards with Zoho Rename Sync | HIGH | minor | _docs/task/058-edit-project-modal.md | 2026-06-09 |
| 057 | Enhance Hub Create Task Modal — 2-Step Form with AI Classification | HIGH | minor | _docs/task/057-enhance-create-task-modal-2step-ai-classification.md | 2026-06-08 |
| 054 | Fix Progress Percentage Discrepancy — Product Forms vs Customers Table | HIGH | patch | _docs/task/054-fix-progress-percentage-discrepancy.md | 2026-06-04 |
| 053 | Onboarding Responses Edit, File URL Fix & Product Archive Tab | HIGH | minor | _docs/task/053-onboarding-responses-edit-file-url-archive-tab.md | 2026-06-04 |
| 052 | Code Review Fixes — Security, Auth & Correctness | HIGH | patch | _docs/task/052-code-review-security-correctness-fixes.md | 2026-06-03 |
| 051 | In Review Queue — Tasks Ready for Checking Filter Tab | HIGH | feature | _docs/task/051-in-review-queue-filter-tab.md | 2026-06-03 |
| 050 | PM Create Task from Hub — Manual Task Creation + Immediate Zoho Push | HIGH | feature | _docs/task/050-pm-create-task-from-hub.md | 2026-06-03 |
| 049 | PM Assign Developer to Task (Zoho API) | HIGH | feature | _docs/task/049-pm-assign-dev-to-task.md | 2026-06-03 |
| 048 | Onboarding: Submit-Only Cliq Notification, Form Lock After Submit, PM Responses View & Reopen | HIGH | minor | _docs/task/048-onboarding-submit-lock-pm-responses-view.md | 2026-06-02 |
| 047 | Customer Profile Redesign: Left Nav Layout, Metadata, Product Data & Assets | HIGH | minor | _docs/task/047-customer-profile-redesign-layout-assets-metadata.md | 2026-06-02 |
| 046 | Onboarding: Company Info Sections + PM Section-Gap View | HIGH | minor | _docs/task/046-onboarding-company-info-sections-pm-gap-view.md | 2026-06-02 |
| 045 | Fix "+ New Customer" Button Route (404) | HIGH | bugfix | _docs/task/045-fix-new-customer-button-route.md | 2026-06-02 |
| 041 | Route Architecture Restructure — /auth/login, /dashboard/*, /customers/new | HIGH | minor | _docs/task/041-route-architecture-restructure.md | 2026-06-02 |
| 042 | RBAC: Sidebar & Route Enforcement — Role-based Nav + Guards + Admin Hub Users | HIGH | minor | _docs/task/042-rbac-sidebar-and-route-enforcement.md | 2026-06-02 |
| 039 | Customer Profile Restructure — Settings Panel + Activity Tab | HIGH | minor | _docs/task/039-customer-profile-restructure.md | 2026-06-02 |
| 038 | Pipeline Page Redesign — Rename + Stage Filters + Task Card CTAs | HIGH | minor | _docs/task/038-pipeline-page-redesign.md | 2026-06-02 |
| 040 | Customer ID Format: WRQ-CLIENT- → WRQ-CUST- | NORMAL | patch | _docs/task/040-customer-id-format-rename.md | 2026-06-02 |

| ID | Title | Priority | Type | Doc | Date |
|----|-------|----------|------|-----|------|
| 037 | Code Review Fixes — Security, Performance & Maintenance | HIGH | patch | _docs/task/037-code-review-fixes-security-performance.md | 2026-06-02 |
| 036 | Zoho Portal User: Single-User Lookup by ZPUID or Email | NORMAL | patch | _docs/task/036-zoho-portal-user-by-id.md | 2026-06-01 |
| 035 | Zoho Portal Users: Library Function + API Endpoint | NORMAL | patch | _docs/task/035-zoho-portal-users-endpoint.md | 2026-06-01 |
| 034 | Execution Engine — DB Write Error Visibility + Circuit Breaker Threshold | HIGH | patch | _docs/task/034-execution-engine-bug-fixes.md | 2026-05-29 |
| 033 | Classification: Low-Confidence Review Gaps | NORMAL | patch | _docs/task/033-classification-low-confidence-review-gaps.md | 2026-05-29 |
| 032 | Zoho V3: Fix 3 write functions + parseZohoDate | HIGH | patch | _docs/task/032-zoho-v3-write-functions-parseddate.md | 2026-05-29 |
| 031 | GitHub PR Execution Mode (CODE_CHANGE_MINOR) | HIGH | minor | _docs/task/031-github-pr-execution-mode.md | 2026-05-29 |
| 027 | Sprint 5 — Execution Engine (M6) | HIGH | feature | _docs/task/027-sprint-5-execution-engine.md | 2026-05-27 |
| 028 | Sprint 5 — Reply Generation (M8) | HIGH | feature | _docs/task/028-sprint-5-reply-generation.md | 2026-05-27 |
| 029 | Zoho API V3 Migration (/restapi → /api/v3) | HIGH | chore | _docs/task/029-zoho-api-v3-migration.md | 2026-05-28 |
| 030 | Classification Review: Code Quality + Audit Trail | NORMAL | patch | _docs/task/030-classification-review-code-quality.md | 2026-05-29 |

## Completed

- [256] Fix `/auth/verify` Lottie Animation — `proxy.ts` Redirecting Static Asset Requests — reported bug: the hero-column Lottie animation on `/auth/verify` never rendered, with a console error "Invalid Lottie JSON string: The provided string does not conform to the Lottie JSON format." Traced past the obvious suspect (the `.lottie` file itself — confirmed valid via `file`/hex-dump and a plain `curl`, which returned the correct 27,101-byte `PK` zip payload) to the real root cause: `src/proxy.ts`'s matcher only excluded a short hardcoded list of paths (`_next/static`, `_next/image`, `favicon.ico`, `icons`, `manifest.json`, `sw.js`, `workbox-*.js`) from the proxy, so requests to `/assets/team-work.lottie` ran through `proxy()`'s `isHubRoute` check, matched none of `nonHubPrefixes` (`/auth/`, `/api/`, `/callback`, `/onboarding`), and — since the `mfa_pending` cookie is set by design on that exact page — got redirected to the `/auth/verify` page's own HTML instead of the asset. Confirmed live in-browser via `fetch()`: pre-fix, the response was 60,850 bytes of `<!DOCTYPE...>` HTML; post-fix, 27,101 bytes starting with the correct `PK\x03\x04` zip header. Fixed by extending `proxy.ts`'s matcher with a generic `.*\.[a-zA-Z0-9]+$` exclusion (any path whose last segment has a file extension bypasses the proxy), rather than a narrow one-off fix for just this file — the same bug was silently exposed for every other raw-fetched static asset under `public/` (e.g. `company_logo.webp`, fetched via `timelogs/_export-pdf.ts`) under the right cookie/route combination. Verified end-to-end via Claude in Chrome: no console errors, correct byte-for-byte fetch response, animation rendering confirmed by screenshot → `_docs/task/256-auth-verify-lottie-proxy-static-asset-redirect-fix.md` (completed: 2026-08-17)

- [254] Portfolio Tracker Swimlane — Hide Skipped-Phase Collapse Toggle/Task Count (StackShift I), Generic-Engine Gantt Scroll + Jump-to-Today Parity — three independently-reported UI bugs. (1) A skipped StackShift I phase's Swimlane header still showed a collapse `+`/`−` toggle and an `N/N` task count even though its lane is always empty (no non-skipped content to reveal by expanding it) — `Swimlane` (`_onboarding-detail.tsx`) now branches: a skipped phase renders as an inert `<div>` (icon, name, "Skipped" pill only, no toggle, no count, not clickable); non-skipped phases are byte-identical to before. A stale nearby comment referencing a PM's ability to "manually expand" a skipped row for historical reference was also corrected, since that's no longer possible. (2) Discrete Development (and other non-`customer_phases`-engine project types') Gantt grid technically had `overflow-x-auto` but no wheel-to-horizontal-pan handler, so a normal mouse-wheel/trackpad scroll did nothing — the exact `handleGridWheel` logic from StackShift I's identical grid (`_onboarding-detail.tsx`) was ported into `_generic-swimlane.tsx` verbatim (native `addEventListener`, `{ passive: false }`, ignores `ctrlKey` to preserve pinch-zoom). (3) The orange "Jump to today" floating button, `scrollToToday`, and first-load auto-scroll-to-today that StackShift I's page has were entirely missing from the generic engine's page — ported into `_generic-swimlane.tsx` as well (self-contained, no props added, `_generic-phase-view.tsx` untouched — the component already owns its own scrollable div and the button's `fixed` positioning doesn't depend on DOM placement). `npx tsc --noEmit`/`pnpm lint` clean (only the 2 pre-existing unrelated warnings in `_checklist-tab.tsx`). Full browser verification (skipped-phase header, generic-engine wheel-scroll, the new Jump-to-today button) was not run in this session — no live browser/Supabase session available — the wheel-scroll/button code is copied line-for-line from the already-shipped StackShift I reference implementation, which lowers risk relative to novel code, but a live click-through is still recommended → `_docs/task/254-portfolio-tracker-swimlane-skipped-header-generic-scroll-jump-parity.md` (completed: 2026-08-17)

- [253] Portfolio Tracker (StackShift I / `customer_phases` Engine) — Fix Reference/Real Day-Scale Mismatch, Suppress Skipped-Phase Day Ranges, Backfill Legacy Overrides — reported bug: a project's header read "69-Day Programme Progress" while its own last phase showed "Day 46–75" (75 > 69) and the Gantt's calendar-date columns didn't match either. Root cause traced (not `day_start_override`/`day_end_override` being `NULL`, which is documented, intended fallback-to-static behavior; the skip-compression math itself was verified correct by hand): `scaleDay()` — which converts the static 1–120 reference schedule to a project's actual `programme_duration_days` — was applied only to the header's total (`visibleDurationDays`), never to the Gantt grid's column count/calendar dates, the Swimlane's phase-bar labels, the Jump-to-phase dropdown, or deliverable date badges, all of which stayed on the raw (skip-)compressed-but-unscaled reference scale. Added a `displayPhases` layer (`_onboarding-detail.tsx`) that additionally runs every phase's/deliverable's day range through `scaleDay`, repointed the Gantt's axis (day-column count, "today" marker bounds, calendar dates) from the unscaled `visibleTotalDays` onto the already-scaled `visibleDurationDays`, and made `handleScheduleChange` (drag-resize persistence) invert that scaling via `unscaleDay` before writing to the DB so stored overrides stay on the same reference scale `seed.ts` already uses. Hand-verified against the exact reported numbers: pre-fix reproduced "Day 46–75 vs. 69-day total" precisely; post-fix, the last phase's displayed end (69) now exactly equals the header total (69), and the grid's last calendar-date column now agrees with the page's own pre-existing footer text. Also fixed `resolveEffectiveDeliverable` (`customer-phases.ts`), which never read a deliverable's own `day_start_override`/`day_end_override` at all — every caller but one silently ignored a PM's manual drag-reschedule; removed the now-redundant ad-hoc `deliverableOverrideMap` overlay this gap had required. Added `JumpToPhaseMenu`'s missing skip-aware day-range suppression (Swimlane's phase-row header already had it; the dropdown didn't). New migration `108_customer_phases_deliverables_default_day_backfill.sql` backfills `NULL` overrides on `customer_phases`/`customer_deliverables` for pre-existing rows with the static `PROGRAMME_PHASES` reference values (scoped to phases 1–5 only, custom phases untouched) — **written and reviewed but not applied**, since it's a production data backfill requiring explicit go-ahead, not additive DDL. `npx tsc --noEmit`/`pnpm lint` clean throughout (2 pre-existing unrelated warnings only). Two disclosed gaps remain open: the migration hasn't been run, and no live browser session was available to re-confirm the hand-computed arithmetic against the actual reported project → `_docs/task/253-portfolio-tracker-stackshift-day-scale-fix-override-backfill.md` (completed: 2026-08-17)

- [250] New Project Form — Duration/Skip-Phase Validation, Collapsible Phases, Name+Type Chip Header, Empty-Name Guards — six chat-driven follow-ups to task 249 in one session, all scoped to the New Project wizard's Step 3 (`/v2/portfolio-tracker/new`). (1) Day-range-vs-duration cross-validation: any included phase's `dayEnd` running past the card's `durationDays` now shows a red inline error on that phase's own Day-to input (not just the last phase — phases can run synchronously/overlap by design, so it's a pure ceiling check); the reciprocal (duration set lower than the last phase's own end day) shows its own message on the duration field. (2) Raising duration past the last phase's end day now auto-stretches that phase to match (deliverable sub-ranges recompute for free, a pure derivation) and shows an amber "last phase was stretched" notice — new `extendLastPhaseToDuration` in `_new-project-types.ts`. (3) Real bug caught and fixed mid-session: the "skip this phase" checkbox's day-range repack cascade could leave the new last phase short of duration (e.g. skipping a 15-day middle phase opens an unaccounted 15-day gap at the tail) without ever triggering the same auto-extend-and-warn behavior, since that only fired from the duration field's own `onChange` — fixed by composing the same extend logic into `PhaseBuilder`'s `toggleIncluded`, with a new `onLastPhaseExtended` callback (or `null` to clear a stale notice) bubbling the result up, unified wording via a shared `lastPhaseExtendedNotice()` helper. (4) Phases are now individually collapsible, matching the wizard's existing per-type sections — header (drag handle, name, Included/Skipped badge) always visible, only the day-range editor/deliverable list collapse, a one-line summary shows while collapsed (`Day 76–105 (30 days) · 4 deliverables · Included`), and "Insert custom phase after this one" stays clickable regardless of collapse state. (5) Each section header now reads `{project name} · [type]` instead of the bare classification string, the type rendered as a chip reusing the wizard's existing light-blue pill token (`bg-[#E5F1FF] text-[#007BFF]`, from `_onboarding-wizard.tsx`) rather than a new color — resolved name reused from `_content.tsx`'s existing `displayedNameForCard`, threaded down as a new `getDisplayName` prop. (6) New `phasePlanEmptyNameErrors`/`emptyNameFieldId` block advancing past Step 3 on any blank phase name (free-form + custom fixed-phases only — default fixed-phase names are static, never flagged), deliverable name, or checklist-item title — a blank row previously submitted silently wrong, since `phasePlanDraftToInput`/`defaultPhaseOverridesFromDraft`/`customPhasesFromDraft` all just skip blank rows. `InlineRow` (shared by all three field types) gained `id`/`error` props for the red-border-plus-message treatment; a deliverable's checklist now auto-expands on mount if one of its own items already has an error. The Step-3 validation gate (previously fixed-phases-only for the day-range check) now runs both checks in both modes, and `scrollToField` was upgraded to accept a fallback id chain (exact field → phase container → type container) so a validation target hidden behind a manually collapsed phase/type section still scrolls to the nearest visible ancestor instead of silently no-op'ing. Deliberately did not lift collapse state up into `_content.tsx` to force sections open on failure — the scroll fallback chain is a lighter-weight fix for a case that only arises if a PM manually collapses something first (both collapse layers default to fully expanded). `npx tsc --noEmit`/`npx eslint` clean after every round, re-verified at the end with no further fixes needed. `pnpm build` and full manual/browser acceptance (live cross-validation, the skip-checkbox fix, collapse/expand, the name+chip header, and the empty-name block-and-scroll flow end-to-end) were not run in this session — no live dev server/browser session available, same documented gap as sibling tasks 239/240/244/245/246 — recommended before shipping → `_docs/task/250-new-project-form-phase-validation-collapsible-chip-header.md` (completed: 2026-08-14)

- [246] StackShift I/II Timeline — Custom Phases Beyond the Fixed 5 (Implementation) — implements task 245's resolved research/design, letting a PM add phases beyond StackShift I's fixed 5 (Onboard/Migrate/Publish/AI Visibility/Optimize) at intake. New migration `103_customer_phases_custom.sql`: `customer_phases` gains `custom_name`/`day_start_override`/`day_end_override`/`sort_order` (backfilled `= phase_number`), `customer_deliverables` gains `custom_name`/`custom_description`/`custom_owner`; both tables' `phase_number` CHECK widened from `between 1 and 5` to `> 0`. Key design decision: `phase_number` stays a stable, never-renumbered per-project identity (every existing FK — `phase_members`, `customer_assets`, etc. — keeps working unchanged) while a new `sort_order` column carries display order — inserting a custom phase "between Publish and AI Visibility" is a `sort_order` change, not a renumbering cascade. New `resolveEffectivePhase`/`resolveEffectiveDeliverable` project-aware lookups in `customer-phases.ts` (override columns win when set, else fall back to the static `PROGRAMME_PHASES`/`DeliverableConfig` — byte-identical behavior for any phase with every override left null). Both `seedAndStartProgramme`/`seedProgrammeAtPhase` (`seed.ts`) now merge `PROGRAMME_PHASES`' 5 defaults with any PM-added custom phases into one list, sorted then re-indexed to a dense integer `sort_order` sequence (a custom phase's position arrives as a fractional value — e.g. 2.5 = "between phase 2 and 3" — computed by the wizard's new `customPhasesFromDraft`, interpolating between neighboring phase numbers); the before/target/after cascade ternary now compares `sort_order` instead of `phase_number`. ~10 API routes had hardcoded `phase_number between 1 and 5` bounds relaxed to `> 0`; `programme/reminders/route.ts` and the status-report route now iterate each project's actual phase set instead of the static 5, so a custom phase gets late-reminders/report rows/dashboard treatment for the first time. New Project wizard (`_phase-builder.tsx`): fixed-phases mode gained an "Insert custom phase after this one" affordance per row (not full drag-reorder — the 5 defaults' relative order stays fixed by design, only where a custom phase lands is flexible) plus editable name/day-range inputs for customs. `_phases-step.tsx`: per task 245's Confirmed UI Requirements, Duration/Schedule/"Start at phase" now render *below* the phase builder instead of above it (task 244 Round 2's order), the "Start at phase" dropdown's options come from the project's own entered phases instead of a hardcoded 1-5, and picking a non-first phase auto-excludes every phase before it in the draft. Requirement G (StackShift II re-pointing): new `use_default_phase_engine` flag lets StackShift II's "Generate default phases" path seed into `customer_phases` via the same engine as StackShift I, instead of the generic `milestones` model it used before this task — turned out simpler than the task doc anticipated once tracing `page.tsx` showed `/v2/portfolio-tracker/[projectId]` already renders the Timeline unconditionally for every classification, so re-pointing the seeding alone was sufficient (no separate rendering-layer fix needed). Two real bugs caught and fixed during implementation, neither explicitly flagged in the task doc's own Code Context: `buildReminders()` called `getPhaseByNumber()` directly on the active phase number, which **throws** for any custom phase (would have crashed the whole Timeline page on render) — regeneralized to resolve safely from the project's own ordered phase list; and the Status Report page's phase filter would have silently and permanently hidden any project active in a custom phase once a PM touched any filter checkbox (no checkbox exists for a number the filter doesn't know about) — fixed by only applying the filter to phase numbers it actually lists as options. Also caught a real type-naming collision before it shipped: the new `resolveEffectivePhase`'s row types were initially named `CustomerPhaseRow`/`CustomerDeliverableRow`, already taken by `src/types/database.ts`'s generated Row types — renamed to `PhaseOverrideRow`/`DeliverableOverrideRow`. Deliberately left unchanged after review (not bugs): `onboarding/projects/import/route.ts`'s CSV phase-matching (only ever resolves for a not-yet-existing project, so no custom-phase gap is possible), `pm-dashboard.tsx`'s `PROGRAMME_PHASES[1]`/`[2]` dashboard tiles (specifically about the default Phase 2/3, correct regardless of any project's customs), and `qstash-start/route.ts`'s hardcoded 1-5 bound (widening it wouldn't help — `custom_phases` isn't persisted for the scheduled-start path, a known gap in the same category as task 244's own documented `skip_phase_numbers` gap, not closed here since it needs a new `projects` column outside this task's scope). `npx tsc --noEmit`/`npx eslint` (all 22 changed files)/`pnpm build` all PASS, zero new warnings — re-verified clean immediately before closing this task with no further fixes needed. Full manual/browser acceptance (new-custom-phase creation and mid-sequence insertion, the auto-skip cascade, StackShift II's re-pointed seeding, an existing pre-migration project's unchanged behavior, and confirming the migration's inferred constraint-drop names against a live schema) was not run in this session — no live Supabase/browser session available, same documented gap as tasks 239/240/244 — recommended before shipping → `_docs/task/246-stackshift-timeline-custom-phases-implementation.md` (completed: 2026-08-14)

- [244] New Project Form UX Overhaul — Dedicated Phase/Deliverable Step, Per-Project Duration & Schedule, StackShift I Default-Phase Skip — restructured the New Project wizard (`/v2/portfolio-tracker/new`, built on tasks 239/240) from 3 steps to 4, moving the phase/deliverable/checklist builder into its own dedicated "Phases & Deliverables" step so "Project Setup" stays a short, quick-to-scan config form instead of stacking a full builder tree per selected type — the primary source of the wizard's excessive scroll. Round 1: StackShift I gained a per-project default-phase skip (a PM can deselect e.g. "AI Visibility" for one client) reusing the codebase's existing `customer_phases.status = "skipped"` value already produced by the admin "jump to phase" override — no schema change; `seedAndStartProgramme`/`seedProgrammeAtPhase` gained a `skipPhaseNumbers` param and a shared `resolveEffectivePhaseNumber()` helper (new in `customer-phases.ts`) so the backdate math never targets a phase the PM excluded. Found and fixed a real bug during its own quality gate: the all-5-phases-skipped edge case was granting the project creator `phase_members` "ownership" of Phase 1 even though that phase's own row was seeded `"skipped"` — gated the insert on `!allSkipped`. Duration and per-card scheduling (Draft/Now/Scheduled) were generalized from StackShift-I-only to every classification. Round 2, after visual QA against reference screenshots: relocated Duration/Schedule/"Start at phase N"/StackShift II's "generate default phases" checkbox from Step 2 onto Step 3 (setting a programme's schedule before its phases were even configured didn't make sense as a field order); rebuilt `StepIndicator` as a CSS grid with dedicated `1fr` connector columns (was a flex row where each connector shared leftover space with its own label's width, so "Company & Contact" left almost no room for its connector while "Project Setup" stretched its long — visibly unbalanced); rebuilt the Review step's footer to match steps 1-3's row layout and Back-button styling exactly (was a stacked full-width CTA over a bare text link); unified every phase-builder input (`InlineRow` et al.) with `Field`'s own rounded/border/focus-shadow treatment; added `@dnd-kit` sortable drag-and-drop for phases (free-form mode)/deliverables/checklist items, reusing the same pattern already established in `_board-view.tsx`; added scroll-to-and-focus-first-invalid-field on Step 1/Step 2 validation failures and the submit-error banner. Deliberately deferred out of this task: letting a PM add genuinely new phases to StackShift I/II's Timeline — the user was offered a lower-risk "parallel generic milestones" alternative and explicitly chose to extend the Timeline itself instead, which reopens task 239's locked closed `phase_number` model across a blast radius later found to be 41 files (not the ~20 originally estimated), including 3 migrations with RLS policies keyed on phase number — scoped to its own task (245, then implemented as 246) rather than bolted onto this UI-polish pass. Closing note: between this task's Round 2 landing and closure, task 246 extended several files this task owns (`_new-project-types.ts`'s `PhaseDraft`/`TypeCardState.startPhase`, `_phase-builder.tsx`'s custom-phase UI, `_phases-step.tsx`'s field ordering) — re-verified after those changes landed that every task-244-specific deliverable (the 4-step wizard, phase skip, per-card duration/schedule, sortable reordering, scroll-to-error, the rebalanced step indicator, the matched Step 4 footer) is still present and functioning; `tsc`/`eslint`/`pnpm build` all clean. Full manual/browser acceptance was not run in this session (no live Supabase/browser session available, same documented gap as tasks 239/240/245/246) — recommended before shipping, along with a known gap flagged but not closed: `skip_phase_numbers` isn't yet persisted for the deferred/scheduled-start cron paths (`qstash-start`/`scheduled-autostart`), since closing it needs a new `projects` column outside this task's scope → `_docs/task/244-new-project-form-ux-overhaul-phase-skip-per-project-duration-schedule.md` (completed: 2026-08-14)

- [245] StackShift I/II Timeline — Support Custom Phases Beyond the Fixed 5 (Research/Design) — the deliberately-deferred, high-risk half of a request that also asked for phase-*skip* (delivered in task 244) and phase-*addition* (this task); task 244's Round 2 notes record the user explicitly choosing to extend the Timeline itself over a lower-risk generic-milestones alternative. Resolved all 5 originally-open design questions rather than leaving them for implementation to improvise: (1) a custom phase's name/day-range/deliverables live in new nullable override columns on `customer_phases`/`customer_deliverables`, not a new table or the static config; (2) `phase_number` becomes open-ended but stays a stable per-project identity, decoupled from a new `sort_order` display-order column — the design choice that makes inserting a phase mid-sequence safe without a renumbering cascade; (3) a new phase's day range is placed manually (no automatic reflow of existing phases); (4) RLS needs zero changes — confirmed by reading migrations 059/060/073 in full, every policy on the affected tables is role-only, never conditioned on `phase_number`'s value (only two DB CHECK constraints actually block open-ended numbering, both droppable in one migration) — this significantly lowered the task's original "RLS is especially high-risk" risk framing; (5) confirmed with the user that this applies to both StackShift I and StackShift II's default-phases mode, with a scope correction surfaced during research: StackShift II never actually rendered the fixed-5 Timeline to begin with (it seeds once into the generic `milestones` model and diverges permanently), so "applies to StackShift II too" concretely meant re-pointing that seeding onto the same engine, not extending an already-shared render path. Also classified all 38 files touching `phase_number`/`PROGRAMME_PHASES` (15 no-change, ~20 needing changes) and captured three UI requirements confirmed directly with the user (Duration/Schedule/"Start at phase" render below the phase builder, not above; the "Start at phase" dropdown is driven by the project's actual entered phases; selecting a non-first phase auto-cascades exclusion to every phase before it). No code changes in this task by design — it produced the resolved data model, blast-radius classification, and confirmed UI requirements that task 246 (also Completed) implemented from directly, without re-deriving or re-litigating any of these decisions → `_docs/task/245-stackshift-timeline-custom-phases-beyond-fixed-five.md` (completed: 2026-08-14)

- [238] Issue Detail — Zoho Description Line-Break/Image Fix, Attachments/Comments/Time Logs Tabs, Comment Timestamp Hover — four fixes reported against real Zoho-imported data. (1) Description field rendered with large phantom gaps: Zoho's exported HTML wraps every line in `<div>...<br/></div>`, and left as-is Tiptap turned each `<div>` into a `<p>` with the trailing `<br/>` surviving as a real hardBreak, so *every* line got one extra blank line appended, not just Zoho's genuinely-blank divider divs. Fixed with a new regex-only, SSR-safe `normalizeZohoDescriptionHtml()` in `_pm-shared.tsx` — strips the trailing `<br/>` only when the `<div>`/`<li>`/`<td>` has real content before it, leaving genuinely-blank divider divs untouched (their trailing hardBreak still gets ProseMirror's own "trailingBreak" companion `<br>`, which is precisely the double-height blank line Zoho's source intends there). Paired with a `:has()`-scoped CSS rule in `_description-field.tsx` that gives that exact blank-marker paragraph signature (`<br><br class="ProseMirror-trailingBreak">`) a deterministic `min-h-[1em]` instead of relying on how a browser happens to lay out two stacked `<br>`s — added after testing feedback flagged the first cut as visually too tight, then again after feedback flagged the double-`<br>` reliance as architecturally fragile. Applies to both Task Detail and Issue Detail since `_description-field.tsx` is shared. (2) Zoho inline description images (`/portal/viewInlineAttachment/image?file=...`) don't resolve — same normalizer prepends `https://crmplus.zoho.com`. (3) Attachments/Comments/Time Logs (tasks 235/236/237) were three stacked block `Card`s instead of a tab switcher — replaced with `_issue-attachments-comments-panel.tsx`, a pill-tab panel adapted 1:1 from Task Detail's existing `_task-attachments-comments-panel.tsx` (all three tabs stay mounted; switching only toggles `hidden`, no refetch on return). (4) Comment timestamps had no exact date/time and `formatRelativeTime()` (`src/lib/utils.ts`) had no week/month/year tiers (e.g. `92d ago` instead of `3mo ago`) — added the missing tiers, and after testing feedback clarified the intended UX (inline reveal on hovering the whole comment row, not a floating tooltip — the first cut used the existing `Tooltip` primitive, the wrong shape for this spot), replaced it with a `max-w-0 → group-hover:max-w-[200px]` inline expansion in both `_task-comments.tsx` and `_issue-comments.tsx` (zero footprint at rest, smooth `transition-[max-width]` reveal, matching this codebase's existing width-transition convention rather than a new animation pattern; rejected a plain opacity reveal since it would permanently reserve blank space even when not hovering). `npx tsc --noEmit`/`pnpm lint` clean after every round (only the same 2 pre-existing, unrelated warnings in `_checklist-tab.tsx`); every new Tailwind arbitrary-variant selector was confirmed to actually compile by inspecting the already-running local dev server's live CSS output (never started or stopped that server). Full browser acceptance testing (live Zoho-imported description/comment rendering, pill-tab switching, hover-reveal timing) was not run in this session — no test credentials/browser access — recommended before shipping → `_docs/task/238-issue-detail-zoho-html-tabs-comment-timestamp-fixes.md` (completed: 2026-08-13)

- [233] Portfolio Tracker Listing — Card Context Menu with Delete Action — ported task 232's Grid-card kebab menu to the Portfolio Tracker listing (`ProjectCard` in `_onboarding-list.tsx`), gated to admin/pm/super_admin independently of the existing `roleEditable`/`canOpenProject` membership gate (different role sets: pm can delete but isn't roleEditable; marketing is roleEditable but must not see Delete). Real structural difference from task 232: this card's clickable wrapper is an actual `<button onClick={...}>`, not a `<Link>` — nesting a kebab `<button>` inside it would be invalid HTML (browsers force-close the outer button), so `PortfolioCardMenu` is rendered as a sibling overlay outside the button/div entirely, with a same-size invisible spacer in the header keeping `OnboardingStatusPill` from being covered. New `_portfolio-card-menu.tsx` reuses task 231's `useDeleteProject`/`ConfirmDialog` verbatim. Quality-gate pass caught a real bug carried over from copying task 232's component: its success handler called `router.refresh()`, but this listing fetches its own data client-side (`useEffect(..., [retryKey])`, not server-rendered props like `/v2/projects`) — `router.refresh()` was a no-op here, so a deleted card would have silently stayed visible until a manual reload. Fixed by threading an `onDeleted` callback down to bump the listing's own `retryKey` (reusing the exact expression its pre-existing "Try again" button already used). `npx tsc --noEmit`/`pnpm lint` clean after the fix. Full authenticated browser verification (kebab visibility by role, click-away close, delete + listing refresh, no nested-button console warning, marketing/developer role checks) was not run in this session — no test credentials/browser access, same documented gap as recent tasks — recommended before shipping → `_docs/task/233-portfolio-tracker-listing-card-context-menu-delete.md` (completed: 2026-08-13)

- [232] Projects Grid View — Card Context Menu with Delete Action — added a kebab dropdown to each Grid-view project card on `/v2/projects` (List view untouched, by explicit scope decision — Projects is the only module with a Grid/List toggle), reusing task 231's soft-delete endpoint/hook/dialog with zero API/migration changes. New `_project-card-menu.tsx`: fixed-position dropdown anchored to the trigger's own rect with a full-viewport click-away overlay (mirrors `onboarding-workspace/_file-tile.tsx`'s `ActionsMenu` technique so adjacent cards can't paint over it), gated by a new `canDeleteProjects` prop threaded `page.tsx` → `ProjectsIndex` → `GridView` only. Unlike the detail-page trigger (task 231), this one calls `router.refresh()` and stays on the listing instead of redirecting, since `page.tsx`'s query already excludes `status = "deleted"`. Quality-gate pass confirmed the double-submit fix from task 231's own gate (`confirmDisabled={deleting}`) was correctly included from the start this time — no repeat defect. One self-disclosed deviation: kebab placed inline in the card's title/status header row instead of the doc's sketched absolute-positioned corner, then repositioned again post-gate per direct user feedback to sit to the right of `ProjectStatusChip` (status first, then actions) — a one-line reorder, documented as a Post-QA Adjustment. Full authenticated browser verification not run in this session — same documented gap as sibling tasks — recommended before shipping → `_docs/task/232-projects-grid-card-context-menu-delete.md` (completed: 2026-08-13)

- [231] Delete Project Action (Soft Delete) — Portfolio Tracker & Projects — added a "Delete Project" action to both modules' detail pages (Portfolio Tracker's existing Settings-gear dropdown; a new header action on Projects), soft-deleting via a new `status = "deleted"` value on `projects.status` (new migration mirroring the existing `customer_products.status = "archived"` pattern) rather than ever removing the row — tasks/issues/time logs/onboarding data stay intact. Repurposed a previously-dead-code, unused `DELETE /api/v2/projects/[projectId]` handler (was a hard delete with no frontend caller) into the role-gated (admin/pm/super_admin) soft-delete endpoint; hardened its `GET` to exclude deleted rows; deliberately left `PATCH`'s `VALID_STATUS` allow-list untouched so "deleted" can never be set through the general edit endpoint. Excluded deleted projects from every named listing/detail surface (Projects list, Portfolio Tracker list + status report, both detail-page loaders — 5 query sites) so a stale link 404s and deleted rows never reappear anywhere browsable. Promoted the timelogs page's page-scoped `ConfirmDialog` to `src/components/ui/confirm-dialog.tsx` (now shared by 3+ pages, crossing this codebase's own stated threshold for extraction) and added a shared `useDeleteProject` hook. Quality-gate pass found and fixed a real gap: the Delete button in `ConfirmDialog` wasn't disabled while the request was in flight (only its label changed), so a rapid double-click could fire two DELETE requests — added a `confirmDisabled` prop, now the pattern every later trigger in tasks 232/233 correctly included from the start. Explicitly out of scope by design: bulk delete, an undo/restore UI, and auditing the ~65 other files that query `projects` without a status filter (dashboard tiles, Kanban, MCP tools, etc. — flagged as a known, accepted follow-up gap, not fixed here). Full authenticated browser verification not run in this session — no test credentials/browser access — recommended before shipping → `_docs/task/231-delete-project-soft-delete-portfolio-tracker-projects.md` (completed: 2026-08-13)

- [230] Time Logs Page & Forms — Full UX Overhaul (Progressive Reveal, Recently-Accessed Projects, Tasks/Issues Search Picker + General Log Mode, Rich Text Notes, Validation, Future Date/Time Guards, Hover Actions, Inline Table Editing, Detail Deep-Link) — fifteen-point follow-up to tasks 226–229. Two structural additions the data model didn't yet support: logging time against an Issue (not just a Task), and a task-less/issue-less "General Log" entry — both representable with zero migration (`time_logs.task_id`/`issue_id`/`note` were already nullable), since the write path was the actual gap. Added a new unified, non-nested API (`POST /api/v2/time-logs`, `PATCH`/`DELETE /api/v2/time-logs/[id]`) alongside the untouched task-scoped nested routes still used by the task-detail page's own Time Logs tab; extended `GET /api/v2/time-logs` with `entry_kind`/`log_title`/`project_public_id`/task-and-issue `display_id` so the table's inline editors and a new detail deep-link icon don't need a second fetch. New components: `_task-issue-picker.tsx` (chevron-less search trigger → tabbed Tasks/Issues panel, newest-first, client-side scroll-reveal windowing for 100+-item projects, with an "Enter General Log" free-text switch), `_confirm-dialog.tsx` (styled delete confirmation replacing the native `confirm()`), `_time-log-notes-editor.tsx` (Tiptap rich text, no image upload), `_time-period-inline-editor.tsx` (inline Start/End tile editor with live auto-calculated hours). Reworked the Add/Edit modal (progressive field reveal until a project is picked, required-field `*` markers + inline errors, Add/Save disabled until filled, helper tooltips, future-date/time tile disabling, Edit mode now supports reassigning task/issue/General Log where it previously locked the assignment) and the table (hover-only row actions, click-to-edit Log Title/Time Period/Date cells, hover detail-link icon for task/issue-linked rows). `SearchableSelect` gained an additive `recentValues` prop for "Recently Accessed"/"Others" grouping (localStorage-backed, per-user); `TimeFieldPicker` gained an additive `maxTime` prop and had its Hour/Minute/AM-PM grid extracted into a reusable `HourMinuteAmPmGrid` so the new inline Time Period editor doesn't duplicate it. Quality-gate pass found and fixed a real duplicated-logic issue (`nowHHmm()`/`combineDateTime()`/an ISO→"HH:mm" formatter existed under two names across two files — extracted to `_time-logs-shared.ts`) and a real bug (a `<Link>` nested inside a `<button>` in the table's Log Title cell — invalid HTML/hydration risk, restructured into siblings). Three rounds of direct user feedback after that: (1) validation errors were appearing the instant a field was touched instead of only after a save attempt — split "field has a value" (gates the button) from "field is semantically wrong, e.g. end before start" (only revealed by an actual click); (2) every popover in the feature area anchored purely below its trigger with no viewport-boundary awareness, overflowing off-screen near the bottom of the modal — added a shared `_use-popover-position.ts` hook that measures the panel's real height and flips it above the trigger when there's not enough room below, applied to all five popovers in the directory for consistency, removing five more duplicated positioning effects in the same pass; (3) reassigning an inline Log Title from a General Log to a task/issue silently did nothing (the reverse direction worked) — root cause was the inline editor's own outside-click-to-cancel listener misreading a click inside the Task/Issue picker's `document.body`-portaled dropdown as "outside," canceling the edit before the click's pick handler could fire; fixed with a shared `data-popover-root` marker the outside-click check now recognizes as "inside." `npx tsc --noEmit`/`pnpm lint` clean after every round (only the same 2 pre-existing, unrelated warnings in `_checklist-tab.tsx` throughout). Full authenticated browser verification (100+-item scroll-reveal, General Log/Issue-linked entries end-to-end including PDF export, every future-date/time guard, the Edit-mode reassignment path) was not run in this session — no test credentials/browser access, same documented gap as tasks 226–229 — recommended before shipping → `_docs/task/230-timelogs-form-table-full-ux-overhaul.md` (completed: 2026-08-12)

- [229] Time Logs — Searchable Project/Task Fields + Redesigned Date/Time Pickers in Add Time Log Form + Source/Type Column (Follow-up to Tasks 226/227/228) — in the Add Time Log modal (`_time-log-entry-modal.tsx`), replaced the native Project/Task `<select>`s with the existing searchable `SearchableSelect` combobox (task 228), added an additive `disabled` prop to it for the Task field's pre-project-selection/loading states, and replaced the native Date/Start Time/End Time inputs with two new popover-based pickers: `_date-field-picker.tsx` (reuses `DayPanel` from `_time-period-panels.tsx` — the exact calendar the page's own date filter already uses) and `_time-field-picker.tsx` (new hour/minute/AM-PM tile popover, with a bounded numeric input so exact non-quarter-hour minutes stay enterable — no precision regression from the old native input). Both new pickers are portaled off the trigger's bounding rect (mirroring `SearchableSelect`'s own mechanism) so they don't clip inside the 420px modal card. Added a "Type" column to `_time-logs-table.tsx` showing a Manual/Timer pill (styling copied verbatim from the task-detail page's existing `_task-time-logs.tsx` pill) — no API change needed since `/api/v2/time-logs` already returned `source` unconditionally. Quality-gate pass caught and fixed one repeated-logic issue: `_date-field-picker.tsx` had reimplemented `_time-logs-shared.ts`'s own `toISODate()` inline instead of calling it. Four rounds of direct user feedback after that: (1) Project/Task changed from a squeezed two-column row to a full modal-width vertical stack; (2) future dates disabled in the Add Time Log calendar specifically via a new optional `disabled` prop threaded through `DayPanel` → `react-day-picker`'s own `disabled` prop (the page's date *filter* stays untouched, still allows any date); (3) discovered the Project/Task "full-width" stack wasn't actually full-width because `SearchableSelect`'s trigger button was `inline-flex` (sized to content, not container) — added an optional `fullWidth` prop (`flex w-full justify-between`) and applied it there; a same-round misfire that had also stacked Date/Start/End Time onto separate rows was reverted back to their original three-column row once the user clarified the "full-width" ask was about Project/Task only; (4) restructured the toolbar (`_time-logs-content.tsx`) from a two-region `justify-between` row into three explicit regions — title left, Date/Project/Employee filters centered in a `flex-1 justify-center` middle section, Export PDF/Add Time Log grouped on the right — and added an optional `label` prop to `SearchableSelect` so the Project/Employee filter triggers read "Project: All Projects"/"Project: <name>" and "Employee: All Employees"/"Employee: <name>" (copied from `_filter-multi-select.tsx`'s existing "Status: All" trigger convention, including its blue-highlight-when-filtered styling), renaming that filter's "All Users"/"Search users…" copy to "All Employees"/"Search employees…" to match. `npx tsc --noEmit`/`pnpm lint` clean after every round. Full authenticated browser verification (search/select/clear interactions, calendar/time-tile popovers not clipping, future-date disabling, centered-toolbar responsiveness at narrow widths) was not run in this session — no test credentials/browser access, same documented gap as tasks 226/227/228 — recommended before shipping → `_docs/task/229-timelogs-add-form-searchable-fields-date-time-picker-source-column.md` (completed: 2026-08-12)

- [228] Time Logs Page — Searchable Project & User Filters (Follow-up to Tasks 226/227) — replaced the Project and User filters on `/v2/dashboard/timelogs` (`_time-logs-content.tsx`) from native `<select>`s to a new single-select searchable combobox (`_searchable-select.tsx`), mirroring the portal-positioning/outside-click mechanics of `portfolio-tracker/import/_content.tsx`'s `TypeMultiSelect` (adapted from multi-select/pill-removal to single-select pick-and-close), with a leading "All Projects"/"All Users" row to clear the filter. Same underlying `projectFilter`/`employeeFilter` state, options sources, and consuming effects/memos as before — filter-control UX only, no behavior change. During the quality-gate pass, extracted a shared `OptionRow` sub-component to remove duplication between the clear-row and each option row; the first attempt defined it inside `SearchableSelect`'s render body and tripped a real `react-hooks/static-components` ESLint error ("Cannot create components during render"), fixed by hoisting it to module scope. `npx tsc --noEmit`/`pnpm lint` clean throughout. Smoke-tested against the already-running dev server (clean 307 redirect to login, no server error). Full authenticated browser verification of the dropdowns' search/select/clear/outside-click/Escape interactions and the User dropdown's role gating was not run in this session (no test credentials/browser access), same documented gap as tasks 226/227 — recommended before shipping → `_docs/task/228-timelogs-searchable-project-user-filters.md` (completed: 2026-08-12)

- [227] Timesheet PDF Export Overhaul + User Filter (Follow-up to Task 226) — rewrote the Time Logs page's PDF export end-to-end and added a User filter for Super Admin/Admin/PM/HR (widened from the user's stated Super Admin/Admin/PM to match task 226's own "view every employee" role group). Export title changed to "Timesheet"; the redundant per-row Employee column is gone, replaced by an "Employee: <Name>" heading per employee with a page-break check before each new employee's block (starts a fresh page instead of squeezing in when the remaining page space drops below a threshold, otherwise continues with a gap); "Project: <name>" / "Project: All Projects" line with the Project column dropped only when one project is filtered; "Period: <date>" / "Period: <from> - <to>" line with the Date column (always first when shown) dropped only for a single-day filter; multi-day exports now subtotal by day (new shared `groupByDate()` helper, mirroring the task-detail tab's own local one) before each employee's existing grand total; a repeating page header (both WebriQ logos + "WebriQ Central Hub" text + "Exported On: <timestamp>") drawn via `jspdf-autotable`'s `didDrawPage` hook so it reliably repeats on every page including ones the table adds itself; and a new `timesheet_<date>_<employee>.pdf` filename scheme. Logos are decoded client-side via an `<img>` → offscreen `<canvas>` → PNG data-URL pipeline rather than handed to jsPDF as raw `.webp`/`.png` paths, sidestepping any uncertainty about jsPDF's own WEBP format support. Caught and corrected a mistake in the task doc's own filename-employee-segment description before implementing it — traced the given example ("Brandon Dwite Cobacha" → "Brandon_D") by hand and found "D" comes from the *second* name token ("Dwite"), not the last one ("Cobacha") as the doc had claimed; verified the final `filenameEmployeeSegment()`/`filenameDateSegment()` logic against both of the user's worked examples by hand and both traced to an exact match. User filter is applied client-side against the already-fully-fetched (period+project-scoped) entry set — no new endpoint. `npx tsc --noEmit`/`pnpm lint` clean. While the dev server was running for a smoke test, an already-authenticated real browser session (not one of my own — confirmed no browser-automation tab access existed) was observed hitting the page and its API with clean 200s, left running rather than killed. One documented Minor gap: a PDF-generation failure (e.g. a blocked logo asset request) would silently reset the export button without showing the user an error message, unlike this codebase's usual `setError(...)` pattern elsewhere — flagged, not blocking. Full authenticated visual verification of the actual generated PDF (page-break placement, column sets across all four period modes, real filenames on disk) was not run in this session — no credentials/browser access — left for the user's own pass before shipping → `_docs/task/227-timesheet-pdf-export-user-filter.md` (completed: 2026-08-12)
- [223] Project Detail — "Status Summary" Drawer (Stacked Phase Cards, reuses Status Report page's derive data) + Progress Overview Polish — added a "Status Summary" button to the end of the progress-overview stat-chip row on `/v2/portfolio-tracker/[projectId]`, opening a right-side drawer (mirrors the notification-bell drawer pattern) with one stacked card per phase (Onboard → Optimize) showing the same status/health/dates/used-allotted/assignee/notes data as the Status Report page (task 221) for that one project, computed via the identical `buildPhaseBreakdown()` derive path — no parallel logic. `GET /api/onboarding/projects/status-report` gained an optional `?projectId=` filter (additive; no-param behavior unchanged); extracted the Status Report table's `NoteCell` into its own file so the drawer's cards and the table's row-detail share one fetch/edit component instead of duplicating it. Progress-overview row's 3 stat chips (`StatChip`) gained icons (`Clock`/`CheckCircle2`/`ListChecks`). Drawer shell deviated from the plan's `notification-bell.tsx`-copied `mounted`-state/`useEffect` sketch — hit this repo's `react-hooks/set-state-in-effect` ESLint rule, fixed by reusing `_onboarding-detail.tsx`'s own `typeof document !== "undefined"` portal guard and a derived `loading` boolean instead, same behavior with no lint violation. Same-session follow-up round on direct visual feedback (four iterations against screenshots): wrapped the progress-bar block and stat-chip/button group in one flex container so they sit side-by-side in a single row on large screens (`lg:flex-row`) instead of stacked; fixed the stat chips' height to be flush with the bar block's top/bottom text (`h-[52px]` → `h-full`, which required dropping the row's `lg:items-center` in favor of the default `align-items: stretch` — centering was the actual reason a fixed pixel height still fell short); bumped the stat values to `font-mono` and increased size twice (`text-sm` → `text-lg` → `text-xl`); kept the Status Summary button at its normal unstretched height and pinned it to the top of the row with `self-start`. `npx tsc --noEmit` clean throughout, including after the polish round. Full role-based browser walkthrough (comparing every card's data against the Status Report page's expanded row across `DETAIL_ROLES`) not run in this environment — no login credentials for the different roles — flagged for the user's own live pass before shipping → `_docs/task/223-project-detail-status-summary-drawer.md` (completed: 2026-08-10)
- [219] Onboarding Workspace v2 Follow-Up — Programme Track Overdue Redesign, Checklist Alert Dot, Notes File Card Upgrade — follow-up to task 217, four requirements plus a new migration. Programme track: real per-day calendar dates in the footer (via the existing `formatDate()` util) replacing the "Migrate & Rebrand starts" milestone label; "N DAYS OVERDUE" replacing "DAY N OF M" once the phase itself is overdue; a "REVIEW CHECKLIST" link (switches to the Checklist tab) replacing the old plain "N SECTIONS OVERDUE" text; a red card border on overdue. Checklist tab gained a red alert dot when `overdueCount > 0`. Business facts RTE: placeholder copy change + one more row of height (192px → 216px). Notes file cards: file-size line, uploader attribution, a type-aware icon, and an in-app eye/download button pair replacing "Open". Investigation found `customer_assets` has no uploader column (task 217's own documented gap) — asked the user via `AskUserQuestion` whether to omit the name again or add a migration; user chose to track it properly. New migration `096_customer_assets_uploaded_by.sql` adds `customer_assets.uploaded_by` (set on every new upload going forward, no backfill possible for historic rows) and widens `profiles`' SELECT RLS to include `pm`/`marketing`/`developer`/`hr` (mirrors the existing `projects_staff_read` policy shape) so the new `uploader_name` embed resolves for every `WIZARD_ROLES` role, not just admins — a considered alternative to the `adminClient`-per-read pattern `_load-detail-data.ts` used for this exact same gap elsewhere, kept because it fixes the root cause and, as a side effect, also fixes `/api/staff-directory`'s identical pre-existing bug (flagged, not separately fixed). Two same-session live-QA follow-up rounds: (1) four items — replaced the first-pass bespoke preview modal with one matching the already-shipped `_onboarding-wizard.tsx` `FileViewerModal` exactly (size, `framer-motion`, Office Online embed for Word/Excel) and fixed a reported click lag by opening the modal before the signed-URL fetch resolves instead of after (mirrors that file's own `handleViewOutcomeFile` pattern); swapped the Notes-card icon badge for the real shared `FileTypeTile` (same component already driving the Files tab, so the two can't visually drift); shortened the displayed uploader name to "First I" form via a new `shortUploaderName()` helper (full name still stored); reworked the Checklist tab's count into a neutral pill (matching this codebase's existing `PermissionBadge`-style pill convention) with wider label spacing and the alert dot repositioned onto the pill's corner with a white ring for a "clipped" notification-badge look. (2) A markdown file in the new preview modal showed a scrollbar that didn't respond to wheel/drag — root cause was `pointer-events-none` hardcoded on `HtmlPreview`/`MarkdownPreview`'s iframes (correct for their original inert-thumbnail use in `_file-tile.tsx`, wrong once reused as the full modal preview); both gained an opt-in `interactive` prop (default off, thumbnails unaffected) that the modal now passes. `npx tsc --noEmit`/`pnpm lint` clean after every round. Not yet applied: migration `096` — needs `supabase db push` against the live instance before `uploaded_by`/the widened RLS policy exist; full manual/browser acceptance (overdue states, checklist dot, RTE height, Notes card across representative file types including a Word/Excel file, cross-role uploader-name visibility) not run in this session — left to the user's own pass → `_docs/task/219-onboarding-workspace-v2-followup-track-checklist-notes.md` (completed: 2026-08-06)
- [215] Time Logs Tab — hh:mm Formatting, Time Period, Timer Timeline Popover, Notes Column, Source Column — deepened the task 214 Time Logs tab to match Zoho's time-tracking detail, per two `AskUserQuestion` decisions locked before implementing: break "why" reuses the existing preset labels (Meal/Coffee/Few Minutes Break) rather than a new free-text field, and the timer session history is stored as a JSONB event log appended by every timer route rather than a normalized events table. New migration `095_timer_timeline_and_time_logs_period.sql` adds `active_timers.timeline` (jsonb, appended by `start`/`pause`/`resume`/`break/start`/`break/cancel`/`stop` — one line each via a shared `appendTimerEvent()` helper) and `time_logs.start_time`/`end_time`/`timeline` (copied over on stop, then left immutable even if the entry's displayed period is later corrected). Every hours value now renders `hh:mm` via the pre-existing but previously-unused `formatHoursAsHHMM()`; added a "Time Period" value (`hh:mm am/pm - hh:mm am/pm`); a hover popover on timer-sourced entries (`_timer-timeline-popover.tsx`, built on the shared `Tooltip` primitives) showing total hours, start/end timestamps, and a colored started/paused/break/stopped timeline; a role-gated Source pill (Manual/Timer, admin/pm/hr/super_admin only, driven by a server-computed `canSeeSource` flag rather than a client-side role check); and the add/edit form (`_time-log-form.tsx`) now takes Date + Start Time + End Time instead of a raw hours number, with hours always computed server-side. Same-session live-QA follow-up, three rounds: (1) row alignment was broken because notes rendered as a wrapped second line under an `items-start` row — reworked to a single always-one-line row (`items-center` + `leading-none`); (2) the timer-details and notes affordances were invisible (hours text was the only, uncued popover trigger; notes were permanent inline text) — replaced with dedicated `Timer`/`MessageSquare` icon-button triggers that only render when applicable; (3) a direct question about whether "3h ago" ever converts to an exact timestamp led to confirming `formatRelativeTime()` (shared util, used by comments elsewhere) never does — added a tab-scoped `formatLoggedAt()` instead of touching the shared one, so this tab specifically shows an exact date+time once an entry is from a prior day; (4) the new Time Period span was rendering near-invisible (no explicit text color, inheriting instead of using a real token) — fixed with an explicit `text-[#5F6A88]`. `npx tsc --noEmit`/`pnpm lint` clean after every round. Two migrations (094 from task 214, then 095 from this task) still need to be applied to the live Supabase instance before the new columns/policy exist; full manual/browser verification of the live start→pause→break→resume→stop timer cycle was not run in this session — left to the user's own pass → `_docs/task/215-time-logs-timer-timeline-time-period-source-column.md` (completed: 2026-08-05)
- [208] Restrict Developer Project Visibility to Member/Assigned Projects Only (List + Detail-Page Guard) — reported by the user (screenshot as `developer`): `/v2/projects` showed all 234 projects with zero role/membership filtering, and the detail routes (`tasks`/`issues`/`milestones` + their `[id]` sub-pages) had no guard at all, so a non-member developer could also open any project directly by URL. Two scope decisions confirmed via `AskUserQuestion` before implementing: restriction applies to `developer` role only (`client` left unfiltered, separate concern), and it covers both the list and the detail routes (list-only would still leak via direct URL). A third decision resolved what "member/collaborator" means for a developer — the actual `project_members` table (migration 073) is only populated by starting the onboarding programme or an explicit "Add Collaborators" call, not by task work, so most of the 234 mostly-legacy Zoho-imported projects have zero rows there; using it alone would leave developers looking at an almost-empty list. Decided on `project_members` OR having an assigned task (`tasks.assignees` contains the user's id) — `issues.assignee_name`/`assignee_email` excluded as a signal since they're free-text Zoho-import fields, not linkable to `profiles.id`. New shared `src/app/v2/(hub)/projects/_project-access.ts` exports `getDeveloperAccessibleProjectIds(userId)` (list-page `.in("id", ...)` filter, reusing the existing zero-match-sentinel convention when empty) and `isProjectVisibleToCurrentUser(projectRowId)` (detail-page guard; non-`developer` roles always return `true`, unrestricted). The guard lives in one choke point (`_get-project-detail-data.ts`, shared by the three list-tab pages via their existing `if (!data) notFound()`) plus three standalone `[id]` detail pages (`tasks/[taskId]`, `issues/[issueId]`, `milestones/[milestoneId]`) that each do their own project fetch. `npx tsc --noEmit`/`pnpm lint` clean; re-verified clean again after unrelated concurrent task 209 edits landed in two of the same files, confirming no interference. Marked completed directly after the quality-gate pass at the user's request (test/document/ship stages skipped for this change) — full browser-based acceptance verification (developer-role narrowing, non-member direct-URL 404, pm/admin no-regression check) was **not** run in this environment; left to the user's own live pass → `_docs/task/208-developer-project-visibility-member-only.md` (completed: 2026-08-05)
- [213] Attachments/Comments Panel — Persist Tab State (Eliminate Refetch/Flicker) + Supabase Realtime Auto-Refresh — root cause of the reported "keeps re-fetching/flickering on every tab switch" bug was `_task-attachments-comments-panel.tsx` conditionally rendering either `TaskAttachments` or `TaskComments` (a ternary), fully unmounting the inactive tab and forcing a fresh fetch + loading skeleton every switch. Fixed by keeping both tabs always mounted and toggling visibility with a `hidden` class instead. Added Supabase Realtime (mirroring `_project-detail.tsx`'s existing `tasks`/`issues` subscription pattern, no new infra — explicitly not QStash or a custom websocket) so the two tabs auto-refresh when another user posts elsewhere: the Attachments tab patches state directly from the `postgres_changes` payload; the Comments tab re-fetches the already-batched comments endpoint on change (a bare payload lacks the resolved author name/attachments a comment row needs), with a `commentsRef`-based guard skipping the redundant refetch for the current user's own optimistic post. New migration `093_enable_realtime_comments_attachments.sql` enables Realtime replication for `task_comments`/`attachments` (idempotent, existence-guarded, since no migration in this repo explicitly enabled it for `tasks`/`issues` either — likely done via the Supabase Studio Replication UI directly, an assumption this task deliberately didn't repeat). Two user-reported follow-ups fixed in the same session: (1) the attachment preview modal's "Failed to load file preview." error, root-caused via live Chrome-automation reproduction to React Strict Mode's dev-only double-effect-invocation firing two real requests per "View" click (one intermittently hitting a transient dev-server 503) with no guard against a stale/superseded response overwriting the current one — fixed by adding the same `cancelled`-flag pattern `_task-attachments.tsx`'s existing `AttachmentThumbnail` already used, to `_task-attachment-viewer-modal.tsx`; (2) comment attachments widened to accept HTML/Markdown/plain text/MP4 (previously images/PDF/Office only) — required fixing both the server route's allow-list *and* a second, independent client-side allow-list in the shared `_task-attachment-picker.tsx` (also used by the New Task modal, whose own server route was deliberately left unchanged), resolved via a new `allowedMimeTypes` prop rather than widening the shared default. All fixes verified live via Chrome browser automation (reproduced each bug first, then confirmed resolution), not just `tsc`/`lint` (both clean throughout) → `_docs/task/213-attachments-comments-panel-persist-tabs-realtime.md` (completed: 2026-08-05)
- [209] Developer Role Permissions (Hide New Project, Task Creation/Edit Rules) + Task Timer Pause/Resume + Floating Break Widget — three answered `AskUserQuestion` decisions locked scope before implementation: assigned-but-not-creator developers keep the ability to move status to `in_progress`/`ready_for_qa` (not fully read-only, overriding a literal reading of the request), the break widget is a hub-wide floating overlay (not dashboard-page-scoped), and timer/break state is server-persisted (new `active_timers` table) rather than client-only, since a hub-wide widget and a per-task-row control need to share and control the same running timer across navigation/refresh. Hid "New Project" behind a new `canCreateProject` prop (admin/pm/super_admin); added the missing `tasks_developer_insert` RLS policy that was silently blocking developer task creation even though the "New Task" button was already unrestricted client-side; widened `tasks_developer_update` to creator-or-assignee at the row level with field/value restriction enforced in the PATCH route via one new shared `getTaskEditPermission()` helper (creator = full edit, assignee-only = status-only limited to two values + `position` for board drag-and-drop, neither = read-only), reused identically across the PATCH route, list view, board view, and task detail page. Rebuilt the existing local-`useState` start→stop-only timer into a server-persisted model (`active_timers` table, nullable task/break columns so a break can exist independently of a running timer, 7 new API routes) behind a new `TimerProvider` context, replacing the deleted `POST /api/v2/tasks/[taskId]/timelog`. New hub-wide `TimerFloatingWidget` (developer-role only, mounted in `V2HubShell`) with Meal/Coffee/Few-Minutes break toggles (durations resolved server-side, never client-supplied), mutual exclusion, and a live countdown; ending or expiring a break leaves the timer paused, never auto-resumes. Quality-gate pass caught and fixed one real issue before shipping: a dead `BREAK_LABELS` export sitting unused alongside a fragile regex-derived label in the widget, replaced with the proper shared constant. Four follow-up fixes from the user's own live-browser pass, same session: break-button tooltips were rendering behind the floating panel (widget's `z-[9999]` sat above the shared `Tooltip` component's portaled `z-50` — lowered the widget to `z-40`, the design system's documented `--z-popover` level); the row's Pause/Resume/Stop/Start timer controls used native `title` attributes instead of the app's styled `Tooltip` component (two of six states already had it right — brought the rest in line); the task list's "Hours logged" column reformatted from `1.5h` to zero-padded `hh:mm` with a hover tooltip showing the plain-language total ("1 hour and 30 minutes"); and the enabled "Start timer" icon switched from `Play` to `Timer` (lucide-react), scoped only to that specific state per the literal request. `npx tsc --noEmit`/`pnpm lint` clean after every round including all four follow-up fixes. Not yet applied: migration `092_developer_task_permissions_and_active_timers.sql` — no local Supabase CLI/config exists in this repo, so it needs to be run against the live Supabase instance manually before any of this is functional; full manual/browser acceptance beyond the user's own spot-checks not run in this session → `_docs/task/209-developer-permissions-timer-pause-break-widget.md` (completed: 2026-08-05)
- [203] Portfolio Tracker & Onboarding Wizard — PM Membership Access, Member-Name RLS Bugs, Read-Only Field Indicators — live, user-driven debugging session testing the app as `pm`, five issues fixed in sequence. (1) Portfolio Tracker list card was non-clickable for `pm` regardless of project membership — `_onboarding-list.tsx`'s `editable` was purely role-based (`marketing`/`admin`/`super_admin`); replaced with a per-card `canOpenProject()` that also opens the card for a gated role (`pm`/`marketing`) who's a member of that specific project (task 154's member union), with `page.tsx` now passing `currentUserId` down and the footer disclaimer's copy corrected (no longer claims access is "restricted to Marketing"). (2) Fixing (1) surfaced a second, stricter gate: the Wizard's own "Restricted" screen (`_onboarding-detail.tsx`) checks `phase_members` (Phase 1 specifically), a separate table from `project_members` — a PM who was a project member but never explicitly added to `phase_members(phase_number=1)` still got blocked opening the Wizard. Per direct user clarification ("PM is project manager"), added `hasPhase1Access = isPhase1Member || (role === "pm" && isProjectMember)` — project membership alone now suffices for `pm`; `marketing` is untouched and still requires explicit Phase 1 membership per task 153's original requirement 4. (3)+(4) "Unnamed"/"Unassigned" member names on both the list's avatar stack and the detail page's Owner/Collaborators row traced to a `profiles` RLS gap, not a data bug: `profiles_read_own` (migration 048) only lets a caller read their own row, or every row if `admin`/`super_admin` — every embedded `profiles(full_name, role)` PostgREST sub-select silently returned nothing for teammates when the caller was `pm`/`marketing`. Fixed by switching six read sites to `adminClient` (matches the existing precedent for the same gap in the contacts lookup): `api/onboarding/projects/route.ts`, `_load-detail-data.ts` (phase1Members/projectMembers/creator-name queries), and the GET handlers in `api/projects/[projectId]/members/route.ts` + `api/projects/[projectId]/programme/phases/[phaseNumber]/members/route.ts`. (5) Wizard fields the user couldn't edit gave no explanation — controls just silently disabled or vanished; added a new shared `ReadOnlyHint` component (`_onboarding-wizard.tsx`: `Lock` icon + "Read-only — you don't have edit access to this step" message, `cursor-not-allowed`) wired into every shared field component (`ContactsField`, `TagField`, `RichTextField`, `FileUploadBox`, `HtmlMockupFileList`, the raw website-URL input, and the Phase 1 checklist toggle's existing hover-Tooltip) so it covers every gated step (1–5/7) uniformly, not just the step first reported on. No migration involved — all application-code fixes. `npx tsc --noEmit`/`pnpm lint` clean after every fix; manual/browser verification not run in this session (no live `pm` test account in this environment) — flagged for the user's own live pass, especially a `pm` account that's a project member but not a Phase-1 member → `_docs/task/203-pm-access-member-names-readonly-indicators.md` (completed: 2026-08-04)
- [201] Portfolio Tracker — Notifications for Set Project Owner & Add Collaborators — wired the gear-menu "Set Project Owner"/"Add Collaborators" actions on `/v2/portfolio-tracker/[projectId]` into the existing `notifications`/`createNotification()` infra (no migration needed — `actor_id`/avatar join already shipped in migration 082). `PATCH /api/projects/[projectId]/members` now notifies the new owner ("{Actor} set you as the project owner of {Project}.") and every other existing member ("{Actor} has changed the ownership of {Project} to {NewOwner}."), with the acting user always excluded (including the self-transfer edge case). `POST /api/projects/[projectId]/members` changed from single `user_id` to batch `user_ids: string[]` so one confirm can add several people and produce one combined notification for other members with correct singular/dual/Oxford-comma grammar via a new local `formatNameList()` helper, plus a "you" notification to each newly-added person. Per two `AskUserQuestion` decisions during planning: batching required reworking `CollaboratorsPanel` from click-to-add-immediately to a stage-then-confirm multi-select UI (chips + "Add N" button), and the actor never receives a notification about their own action. Hit and fixed one real TypeScript narrowing bug along the way: `Array.isArray(body?.user_ids)` (optional chain) doesn't narrow the differently-shaped `body.user_ids` reference used in the branch, silently leaving `userIds` typed `unknown[]` — fixed via a single local `const rawUserIds: unknown`, matching the working non-optional-chain `Array.isArray(body.xxx)` pattern already used elsewhere in the codebase. Post-implementation copy fix requested directly by the user: renamed the menu item and panel header from "Add Collaborators"/"Add collaborators — ..." to "Manage Collaborators"/"Manage collaborators — ...", since the panel already manages (adds and removes) existing collaborators, not just adds. `npx tsc --noEmit`/`pnpm lint` clean throughout; full manual/browser verification (Set Project Owner + Add Collaborators end-to-end, including the singular/dual/Oxford-comma notification phrasing across batch sizes) deferred to the user's own live pass — not run in this session → `_docs/task/201-portfolio-tracker-owner-collaborator-notifications.md` (completed: 2026-08-03)
- [200] Onboarding Wizard — Checklist-Based Step Navigation + Step-Gate Alert Redesign — fixed `handleStepIndicatorClick` (`_onboarding-wizard.tsx`), which used to hard-cap forward step-indicator clicks at exactly `stepIdx + 1` regardless of completion (reported repro: HTML mockup → Client sign-off blocked even with Storage folder + KB already done) — rewrote it to walk the full chain from the current step to the target and only block on a genuinely incomplete step, naming the specific blocker. Step-indicator checkmarks fixed from position-based (`i < stepIdx`) to real per-step `localDeliverables` completion. Step-gate popup redesigned with a `--warn-bg` tinted icon container and an outcome-named "Go to '{step}'" action instead of a bare icon + generic "OK". Three rounds of same-session live-QA follow-up: (1) active step's circle always shows its number rather than a checkmark, even when done — checks reserved for non-active completed steps; (2) live QA surfaced a second navigation bug — an overdue-but-incomplete step could still be skipped past via the indicator (an inherited overdue bypass whose "matches Continue's gate" comment was never actually true, since `handleContinueClick` has no such bypass) while Continue on the same step correctly hard-blocked — removed the overdue auto-clear entirely so "cleared" means real completion on both paths; also audited all three wizard popups' button styling against `_final_design/guide/central-hub-design-system.md` and found the wizard's own modals were the actual outliers (`rounded-lg`/opacity-hover/borderless-ghost) versus the guide-compliant `rounded-full`/`hover:bg-[#0063D6]`/bordered-ghost pattern already shipped in sibling files (`_onboarding-detail.tsx`, `portfolio-tracker/new` and `import` wizards) — brought all three in line, and swapped a stray radio-like `Circle` icon in the incomplete-checklist modal for the real checkbox square the live checklist uses; (3) clicking a step-indicator item blocked by the currently-viewed step now runs the same field-level validation Continue does (red field border + inline error, or the Incomplete-checklist-items modal) instead of only a generic popup — via a behavior-preserving extraction of Continue's validation body into a shared `validateCurrentStepForAdvance()`. All changes confined to `src/app/v2/(hub)/portfolio-tracker/[projectId]/_onboarding-wizard.tsx`. `npx tsc --noEmit`/`pnpm lint` clean after every round; full end-to-end manual browser re-verification of all four rounds together not run in this session — flagged for the next live QA pass → `_docs/task/200-onboarding-wizard-step-indicator-checklist-based-navigation.md` (completed: 2026-08-03)
- [199] Onboarding Wizard — HTML Mockup Auto-Generated MD Build Spec — every HTML mockup upload in the Onboarding Wizard's HTML Mockup step now auto-generates a paired Markdown build spec (1:1, section/component breakdown + interactive-elements + responsive notes + a ready-to-paste "Build Prompt" block for AI coding assistants), via a new `mockup_spec` Sonnet orchestration layer (`generateMockupSpec()` in `src/lib/ai/generate-mockup-spec.ts`, logged through `logLLMInvocation()` like every other layer) and a new `source_asset_id` self-referencing FK on `customer_assets` (migration 090, `on delete cascade`) linking each spec back to its source HTML row. `POST .../assets/[assetId]/generate-md` doubles as both "generate" (no paired spec yet) and "regenerate" (overwrites the existing paired spec's storage content + `file_size` in place) — one endpoint for both, so a Regenerate action next to the existing Edit/View/Remove ones on the spec row can refresh a spec that's gone stale after editing the HTML in-app. Upload UI got a two-phase progress bar (upload % → indeterminate "Generating build spec…", new `indeterminate-bar` CSS keyframe) that never blocks or rolls back the already-successful HTML upload on a generation failure — surfaces as a small inline Retry instead. `DELETE .../assets` now also cleans up the cascaded spec's storage object (the DB cascade alone only drops the row, not the file). Two real bugs found and fixed during user-driven manual QA in the same session, neither caught by `tsc`/lint since both were data-flow gaps, not type errors: (1) the Storage/KB step's server-side folder auto-filing map (`LABEL_TO_SYSTEM_FOLDER`, task 141) had no entry for the new "Mockup Build Spec" label, so a generated spec would've silently landed in the "Other" folder forever instead of alongside its HTML mockup — added the missing mapping; (2) confirmed pre-existing (present in `handleBusinessFactsUpload` too, not introduced here) but exposed by this feature: the Storage/KB File Explorer's asset list is fetched once on wizard mount and isn't kept in sync by any other step's own upload handler, so newly uploaded files across the wizard stay invisible there until a full page reload — closed specifically for the HTML Mockup step (resolves and assigns the already-provisioned "HTML Mockup" folder id at creation time instead of leaving it unfiled, and keeps the Storage/KB list synced live on upload/generate/regenerate/remove), left unfixed for the other five steps and flagged as a candidate follow-up task rather than silently expanded into here. `npx tsc --noEmit`/`pnpm lint` clean throughout, including after both post-review fixes; full manual browser verification (upload → progress → paired MD → view/edit/regenerate → delete-cascade → correct Storage/KB folder placement) still requires migration 090 applied to a live Supabase instance before first use → `_docs/task/199-onboarding-wizard-html-mockup-auto-md-generation.md` (completed: 2026-08-03)
- [197] Onboarding Wizard File Viewer Modal — Widen + HTML Desktop/Tablet/Mobile Viewport Toggle — widened `FileViewerModal` (was `max-w-4xl h-[85vh]`, visibly cramped) to match `HtmlEditorModal`'s near-fullscreen footprint, and for `text/html` files added the same `PREVIEW_SIZES` scale-to-fit Desktop/Tablet/Mobile rendering already used by the Edit HTML modal — "Desktop" renders the page's real 1280px desktop layout rather than tripping its own mobile breakpoints. Fixed a real bug found during manual testing: copying the reference implementation's conditional-early-return-before-mounting-the-ref pattern into the new `HtmlFilePreview` left the `ResizeObserver` permanently unattached (preview stayed blank forever) — fixed by keeping the ref'd wrapper div always mounted and moving loading/error/iframe into conditional children instead. Four rounds of live user-driven refinement in the same session, all re-verified in-browser (`npx tsc --noEmit`/`pnpm lint` clean after each) against the real project's own HTML mockup plus a purpose-built responsive test fixture: (2) relocated the toggle out of a separate toolbar row into the modal header — centered, icon-only buttons with real `Tooltip`s, navy-fill active state copied from the Projects list's Grid/List view switch — and shrank the modal to `w-[1360px] max-w-[96vw]` so Desktop mode fills the pane without excess side padding; (3) added an "Edit HTML" button to `FileViewerModal`'s header (HTML files only) that closes the viewer and opens `HtmlEditorModal` for the same file, first giving both modals `AnimatePresence` + `motion.div` fade/scale transitions (neither had any before) so the hand-off crossfades instead of hard-cutting — also brought `HtmlEditorModal`'s own viewport toggle to the same header-centered design, restyled its Save button as the orange pill CTA per `_final_design/guide/central-hub-design-system.md` (reused the exact classes from the app's other "+ Add client"/"+ New Project" CTAs), and added a `<>` code-editor show/hide toggle (editor pane unmounted rather than CSS-hidden, so the preview's `flex-1` naturally fills the row); (4) repositioned that code toggle into its own pill immediately before the viewport pill and switched its active-state color from light-blue to the same navy fill as the viewport buttons, per direct user feedback on the exact visual. Known accepted limitation, documented rather than fixed: since `FileViewerModal`/`handleOpenHtmlEditor` were originally wired only for the HTML Mockup step, editing-then-saving an HTML file surfaced from a *different* step's own file list won't live-refresh that step's local file-size display until reload (the save itself always succeeds). Test artifacts (`test-mockup.html`, `test-plain.txt`) uploaded for verification were removed from the real project afterward → `_docs/task/197-file-viewer-modal-widen-html-viewport-toggle.md` (completed: 2026-07-29)
- [196] Import Project — No Duplicate-Name Guard (Companion Gap to Task 195) — companion fix: `POST /api/onboarding/projects/import` derived the same auto-generated project name as the wizard but had zero duplicate check anywhere (no client pre-check, no server check), exploitable both via two identical rows in one CSV batch (each row is its own committed insert, not one transaction) and via simply re-uploading an already-imported file. Added the same case-insensitive `ilike` duplicate-name check per row, immediately before insert, following this route's existing per-row `errors.push({row, error}); continue;` convention (partial-batch success/failure, not an all-or-nothing 409 like task 195's route) — uses `adminClient` to match this file's existing pattern rather than the RLS-scoped client task 195 used. `npx tsc --noEmit`/`pnpm lint` clean; quality gate PASS with no deviations. Verified via direct API calls (bypassing the CSV-upload UI, exercising the same endpoint/payload): same-batch duplicate rows → row 1 imported, row 2 rejected; re-running an already-imported row → rejected, nothing created; a normal distinct-row import → unaffected. Test data (3 throwaway `ZZTEST-`-prefixed customers/projects created across both 195 and 196's verification) flagged for the user's own DB cleanup, cleanup SQL provided in both test reports → `_docs/task/196-import-project-duplicate-name-guard.md` (completed: 2026-07-29)
- [195] New Project Form — Duplicate Project Creation on Resubmit (No Server-Side Duplicate-Name Guard) — traced a reported 3-duplicate-project incident (M&I Interiors) to a check-then-act gap: the wizard's `check-name` uniqueness check ran once, client-side, at the Step 2→3 transition, never re-validated at actual submission, and `POST /api/onboarding/projects` had zero duplicate check of its own (no DB unique constraint on `projects.name` either). Live DB evidence (timestamps 9s/15s apart, same `created_by`) confirmed three separate submissions, not a single-request bug. Fix: added the same case-insensitive `ilike` duplicate-name check to `POST /api/onboarding/projects` (409 on match) plus a synchronous `submitLockRef` guard on the wizard's `submit()`/`startAtPhase()` (the existing `submitting` state alone is async and can be outrun by a fast second click). Deliberately did not add a DB-level unique constraint (would need a customer-scoping decision not requested) or fuzzy-name matching. `npx tsc --noEmit`/`pnpm lint` clean; quality gate PASS with no deviations. Verified end-to-end in-browser: double-click on "Just save" produced exactly one project (client lock confirmed); a direct duplicate POST bypassing the wizard returned 409 (server guard confirmed in isolation). Live-data incident cleanup performed in the same session: identified and deleted the two stray duplicate rows plus their `customer_products`/cascaded programme data, confirmed a separate pre-existing legacy Zoho-imported project (`PROJ-01`) was unrelated, and deliberately left the surviving project's `project_id` gap (`PROJ-04`, not renumbered to `PROJ-02`) since `project_id` is a live routing-key exception for `/v2/portfolio-tracker/[projectId]`/`/v2/projects/[projectId]` and the generator intentionally never reuses a deleted project's number (migration 088/task 187) — full incident trail recorded in the task doc. Follow-up gap found and spun out separately: the Import Project bulk route had the identical missing-guard problem → task 196 → `_docs/task/195-new-project-form-duplicate-submission-guard.md` (completed: 2026-07-29)
- [192] Project Detail — Activate Issues Tab (List/Board/Calendar Parity with Tasks Tab) — built the full Issues tab on `/v2/projects/[projectId]` (previously a "Issues coming soon." placeholder): `IssueListView`/`IssueBoardView`/`IssueCalendarView` mirroring the Tasks tab's toolbar (search, Status + new Severity `FilterMultiSelect`, sort, List/Board/Calendar toggle), reusing `BOARD_COLUMNS`/`STATUS_STYLE`/`normalizeStatus` from `_pm-shared.tsx` since `issues.status` is normalized through the same `mapTaskStatus()` as tasks at import time (confirmed in `zoho-import/issues/route.ts`) — only `severity` needed a new 5-value Zoho vocabulary (`SEVERITY_OPTS`/`SEVERITY_STYLE`/`normalizeSeverity`). New `GET/POST /api/v2/projects/[projectId]/issues` and `PATCH/DELETE /api/v2/issues/[issueId]` routes; realtime channel + optimistic mutations wired into `_project-detail.tsx` alongside a tab-aware "+ New Task"/"+ New Issue" header CTA and `CreateIssueModal`/`EditIssueModal` (edit-in-modal chosen over a new nested detail-page route — subtasks/labels/milestone/links don't apply to issues' data model, judged disproportionate for the ask). Board view supports cross-column drag (status change) only, not in-column reorder, since `issues` has no `position` column (tasks does) — cards sort by due date within a column instead. Assignee reassignment is a single-select picker (unlike tasks' multi-select) writing `assignee_name` from the chosen member's `full_name`; `assignee_email` is cleared rather than resolved, since `profiles` has no `email` column and resolving one per member would need extra admin-API calls for a picker that's mostly for display. Verified end-to-end in-browser against real imported Zoho issue data (not just empty-state) — created, edited every field, deleted, filtered/sorted/searched, and switched views. Live testing surfaced and fixed a real, pre-existing bug not introduced by this task: `CreateTaskModal`/`MilestonePanel`'s POST call sites pass `projectId={project.id}` (UUID) to routes that expect the `project_id` slug (`.eq("project_id", projectId)`), so "New Task"/milestone creation has been silently broken on every project — left those two untouched per the Tasks-tab-unchanged boundary (flagged for a follow-up task) but fixed the equivalent `CreateIssueModal` site so the new feature actually works. Two rounds of user-directed follow-up work in the same session, explicitly overriding the original task doc's "don't touch `_list-view.tsx`" boundary since the user asked for it directly in chat: (1) added bulk-select checkboxes + a working bulk-delete action bar to the Issues list (Tasks' equivalent bar exists but was never wired to a delete handler — left as-is, only reskinned) and changed both tabs' bulk-action "Trash" text button to an icon + `Tooltip`, matching the pattern used elsewhere for icon-only buttons; (2) discovered and fixed the column headers weren't actually sticky at all — `sticky` was applied inside a parent with `overflow-hidden`, which creates its own clipping/scroll-container box that defeats `position: sticky` against the real scrolling ancestor; restructured both list views so rounding/clipping lives on a rows-only wrapper instead, letting the header stick against the true scroll container. A second screenshot-driven follow-up caught a further gap/floating artifact — `py-5` top padding on the scroll container is part of that container's own scrollport padding box, so a `sticky top-0` child can never rise above it; moved the top spacing to a `mt-5` margin on the card instead (margin isn't part of the scrollport, scrolls away completely) — plus a `IntersectionObserver`-driven `headerStuck` boolean (zero-height sentinel div) toggling the header's top corners between rounded (at rest) and square (once actually flush/stuck), duplicated in both list-view files rather than extracted to a shared hook, matching this codebase's established page-scoped-duplication convention for 2-call-site utilities. `npx tsc --noEmit`/`pnpm lint` clean throughout all three rounds → `_docs/task/192-project-detail-issues-tab-parity.md` (completed: 2026-07-27)
- [191] Project Detail Page (`/v2/projects/[projectId]`) — Design System v2.0 Redesign, Task Search/Filter/Sort/Collapse-All Toolbar, Straight-Line View Toggle — full v2.0 token pass across the project detail/task-listing page (`_project-detail.tsx`, `_list-view.tsx`, `_board-view.tsx`, `_calendar-view.tsx`) plus the shared `_pm-shared.tsx` style constants (confirmed via grep that every remaining consumer of its color tokens lives inside this task's own subtree, unlike task 185's page which stopped importing from it — safe to retone here). Replaced the `ViewDropdown` with a straight-line List/Board/Calendar segmented toggle copied structurally from `/v2/projects`'s Grid/List toggle. Added a new toolbar: search (matches task title or containing-tasklist name), status/priority `FilterMultiSelect` dropdowns, a `SortSelect` driving lifted `sortKey`/`sortDir` state, and a collapse/expand-all button (List view only) driving lifted `collapsedGroups` state — all client-side, no server round-trip, since tasks are already fully loaded. Filtering is evaluated at the root-task level (no `parent_task_id`, `depth === 0`); a matching root's entire subtree is included unconditionally, a deliberate simplification flagged in the task doc to avoid orphaning a matching subtask under a non-matching, never-rendered parent. Retoned `STATUS_STYLE`/`PRIORITY_STYLE`/`PROJECT_STATUS_STYLE` onto DESIGN.md's ok/warn/late/neutral/blue vocabulary (8 task statuses re-derived since they don't map 1:1 onto 3 semantic states), collapsed `PROJECT_TYPE_STYLE`'s 4-hue table (which collided with reserved phase hues) into one neutral constant, and swapped the avatar palettes to DESIGN.md's fixed 6-color rotation. Found and deliberately left untouched: `_task-drawer.tsx` is dead code (not imported anywhere in `src/`, orphaned since task 188/190 moved task navigation to a full route) — flagged for a possible separate cleanup task. Milestones tab/`_milestone-panel.tsx` and the full task-detail page (`tasks/[taskId]/_task-detail.tsx`) stay v1-styled by deliberate scope call (gantt/timeline redesign and a separate page redesign, neither part of "task listing page"), though the latter two inherit the new `_pm-shared.tsx` colors for free. One implementation-time deviation: the GitHub-PR label in `_board-view.tsx` was recolored to neutral `#5F6A88` rather than the original `violet-600`, since that Tailwind color maps to `#6A48E0` — DESIGN.md's reserved Publish phase hue. `npx tsc --noEmit`/`pnpm lint` clean; Impeccable's font-size findings (9–12px toolbar/table/badge text) classified as false positives — same sizes already shipped throughout `_projects-index.tsx`/`_onboarding-list.tsx`, same stale-`.impeccable/design.json`-sidecar condition logged in every prior v2.0 task. Manual in-browser QA not run in this environment (no test session available) — flagged for the user's own live pass, especially the filter/sort/collapse-all interactions and drag-and-drop/realtime regressions → _docs/task/191-project-detail-design-system-v2-redesign-task-toolbar.md (completed: 2026-07-24)
- [185] Projects Page (`/v2/projects`) — Design System v2.0 Redesign, Multi-Member Avatars, Task/Issue Progress, Legacy/Version 2 Classification Filter — restyled the native Projects list/grid page onto Design System v2.0 following `/v2/portfolio-tracker`'s shipped `_onboarding-list.tsx` reference, without touching `_pm-shared.tsx` (still shared by the not-yet-migrated Projects kanban/detail views — deliberate blast-radius boundary). `page.tsx` gained a Legacy/Version 2 classification bucket (`external_project_id` null-ness — Legacy = Zoho-imported, Version 2 = created natively, traced through the Zoho import route rather than confirmed against live rows), a `project_members`→`profiles` member map (mirrors `/api/onboarding/projects`'s pattern), and a real `issues` count fetch. Grid cards rebuilt with a page-scoped multi-member `AvatarStack` (copied from `_onboarding-list.tsx`, falls back to a single owner bubble) and two `ProgressStat` rings (tasks + issues, count text below each ring per the request) replacing the single-owner/single-ring footer; deleted the rotating conic-gradient hover wrapper entirely. List view got the same token pass plus a real Issues column (was a static "No Issues" placeholder). Post-Testing revision (screenshot feedback): the Status/Type filter pills were squeezing onto one line, replaced with two checkbox multi-select dropdowns (`FilterMultiSelect`, portal-positioned like the Import wizard's `TypeMultiSelect`) where an "All" checkbox syncs to/from the individual checkboxes (checking All selects everything; unchecking any option un-checks All; zero-selected intentionally shows zero results rather than silently falling back to "All") — required switching `status`/`classification` filtering from `.eq()`/single-value to `.in()`/list-based server-side. Added a sort selector (newest/oldest/name/due-date/recently-updated, restricted to DB-sortable columns since progress is computed client-side post-fetch). View toggle's active state changed from gray to navy-fill (`#071133`, the design system's selection-state token, not literal black) + white icon, with real `Tooltip`s replacing bare `title=""` attributes. `npx tsc --noEmit`/`pnpm lint` clean throughout both rounds; manual in-browser QA not run in this environment (no test session available) — flagged for the user's own live pass, especially confirming the Legacy/Version 2 bucketing against real project names → _docs/task/185-projects-page-design-system-v2-redesign.md (completed: 2026-07-24)
- [184] Customers Page — Design System v2.0 Redesign + Customer ID Search — restyled `/v2/customers` (`_customers-index.tsx`, `loading.tsx`) onto Design System v2.0 following `/v2/portfolio-tracker`'s shipped `_onboarding-list.tsx` reference: `Chip`-based status badges (`ok`/`warn`/`neutral` tones — deliberately never a reserved phase hue for the coarse `customers.status` lifecycle flag), CTA-orange "New Customer" button, navy-active filter pills, `#FAFBFE`/`line-soft`/`blue-50`-hover table restyle, `font-heading` page title, `font-mono` on customer ID/day-phase/progress%/pagination-range, and a discoverable-by-ID search placeholder (`customer_id.ilike` search already worked server-side — UI-surfacing only, no backend change). Kept the list as a table rather than converting to cards, a deliberate scope call (dense column-scannable content vs. Portfolio Tracker's richer per-card summary). Post-Testing live feedback surfaced a real pre-existing bug: `page.tsx`'s "hide customers still gated behind Phase-1 handover" exclusion applied unconditionally to every role, including exact-ID/name search — a Super Admin searching for a known customer ("Test 2" / `WRQ-CUST-BDD824C5`) got a blanket "no results" with no indication the customer existed but was Phase-1-hidden. Fixed by fetching the caller's role (mirroring `layout.tsx`/`dashboard/page.tsx`'s pattern) and skipping the hidden-customer exclusion for `marketing`/`admin`/`super_admin` — the same privileged role set Portfolio Tracker's own `editable` gate already trusts to manage Phase 1 — including a secondary fix so those roles see the true project count/Day-badge instead of a misleading "0 projects" for a now-visible Phase-1 customer. `npx tsc --noEmit`/`pnpm lint` clean throughout both rounds; manual in-browser QA not run in this environment (no test session available) — flagged for the user's own live pass → _docs/task/184-customers-page-design-system-v2-redesign.md (completed: 2026-07-24)
- [182] Remote MCP Server — Expose Ops Chat Tools for External Clients — extended task 181's public `/api/mcp` OAuth server with 11 more tools (`list_tasks`, `create_task`, `update_task`, `update_task_status`, `assign_task`, `delete_task`, `list_assignable_users`, `list_classifications`, `update_classification_status`, `list_tickets`, `run_orchestration`), ported from the cookie-authenticated `ops-chat-tools.ts` used by the in-app Ops Chat (which stays unmodified — parallel implementation, not shared code). Retired the never-used `tasks:write` scope and added 6 new scopes (`tasks:manage`, `tasks:delete`, `classifications:read`, `classifications:write`, `tickets:read`, `orchestration:run`) in `src/lib/mcp/scopes.ts`, granted only to `admin`/`super_admin`/`pm` — confirmed via `AskUserQuestion` that `developer` stays staff-excluded from all 11 new tools this pass (even read-only ones) and that both blast-radius tools (`delete_task`, `run_orchestration`) should ship now rather than deferred. Traced the actual RLS policies for `tasks`/`classification_records`/`tickets`/`profiles` (migrations 026/048/084) to decide per tool whether it could run on the real RLS-scoped session client vs needing `adminClient`: only `list_assignable_users` genuinely needs `adminClient` (profiles RLS blocks `pm` from reading other users' rows); `assign_task` and `delete_task` turned out to be safely RLS-coverable, tighter than Ops Chat's own `adminClient` usage for those two. Added `maxDuration = 300` to the MCP route — first `maxDuration` set anywhere in this app — since `run_orchestration` chains multiple sequential LLM calls with no prior timeout override. `npx tsc --noEmit`, `pnpm lint`, and `pnpm build` all clean; live OAuth reconnect/tool-call testing against a real external MCP client was not run in-session (requires a deployed instance). Follow-up: created `_docs/mcp-tools.md` as a maintained tool inventory (name/scope/role-access table) per user request, plus a `CLAUDE.md` Key Conventions rule requiring any future `server.registerTool(...)` addition to update that file in the same change → `_docs/task/182-mcp-server-expose-ops-chat-tools.md` (completed: 2026-07-24)
- [179] v2 Auth Pages — Design System v2.0 Redesign (Lottie Hero, Logo Reposition, Testimonial Removal) — full token pass across all four `/v2/(auth)` pages (login, register, verify, change-password) and their shared shell/components against `_final_design/guide/central-hub-design-system.md`: new auth-scoped `--color-auth-*` tokens (navy `#071133`/blue `#007BFF`/orange `#FB914E`, additive alongside the existing `--color-brand-orange` used elsewhere in the hub, not repointing it), Space Grotesk page titles, pill-radius buttons, orange reserved for the single primary CTA with every other accent (links/focus rings/icons/theme toggle) moved to blue, and a late/warn/blue/ok semantic progression replacing the old ad hoc `brand-orange`/`green-500` password-strength colors. Replaced the static hero photo with the "Team Work" Lottie animation (`@lottiefiles/dotlottie-react`, dynamic-imported `ssr:false`; asset renamed `Team Work.lottie`→`team-work.lottie` for URL safety) and removed the testimonial quote entirely per instruction. Per an `AskUserQuestion` design decision confirmed with the user, the logo moved off the decorative hero panel entirely onto the form panel (above the heading, both breakpoints) rather than just being restyled in place. First `pnpm build` attempt failed with an unrelated stale-`.next`-cache error (`TypeError: Cannot read properties of null (reading 'hash')` in the PWA/service-worker build step); reproduced identically with the new Lottie usage and assets both temporarily removed, confirming it predated this task — resolved via `rm -rf .next`. Three rounds of live user testing/iteration in the same session: (1) illustration too small (canvas had no sized container — was defaulting to the browser's 300×150 canvas fallback — fixed with an explicit `aspect-square` wrapper), logo not left-aligned with the heading below it (`logo.png` has built-in transparent padding around the mark — compensated with `-ml-1.5`), label/field spacing too tight after an earlier pass shrank the label font without adjusting its gap (also caught a stale label className in `PasswordInput`'s non-`headerAction` branch missed by that earlier pass), field icons not highlighting on focus (added a `peer`/`peer-focus` pairing — first attempt silently failed because the icons were positioned *before* their `.peer` input in the DOM, which Tailwind's `~`-combinator-based `peer-focus` can't match backwards; fixed by reordering to input-then-icon, still visually on the left via `absolute`), and a real keyboard-accessibility bug where Tab went Email → "Forgot password?" → Password instead of Email → Password (root cause: the header-action link shared the label's DOM row, ahead of the actual input; fixed with a CSS grid that keeps it visually pinned to the label row while placing it last in DOM/tab order); (2) illustration bumped larger again (`max-w-lg`→`max-w-2xl`); (3) label/field gap increased further (`space-y-2`→`space-y-3` uniformly across all four label+field pairs, including the grid-based Password+"Forgot password?" row), after a prior edit had been reverted by the user/a linter back to a still-too-tight value. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` clean after every round; live-tested in Chrome (dark + light mode) including typed input to confirm the password-strength progression and Tab-key traversal → `_docs/task/179-v2-auth-pages-design-system-v2-redesign.md` (completed: 2026-07-23)
- [176] Security Sweep #2 — #1-3: Unauthenticated adminClient Routes + Remaining Permissive RLS — deep OWASP re-sweep covering the ~75 API routes not reviewed in sweep #1 plus every RLS policy not touched by migration 084, surfacing a more severe pattern: several customer/product/project routes (`POST /api/customers`, `PATCH /api/customers/[customerId]`, `POST`/`DELETE .../products`, `GET`/`POST .../projects`, `PATCH .../projects/[projectId]`) called `adminClient` (bypasses RLS) with zero authentication check — anyone, no account required, could create/tamper with any customer's record, add/remove products, or create/rename projects (including triggering real Zoho API calls). Added `pm`/`admin`/`super_admin` (and staff-read for the projects GET) checks to all five. Also found `customer_assets`/`customer_asset_folders` (which store literal credentials in a `fields` jsonb column for `type: 'credential'` rows) carried a blanket `auth.role() = 'authenticated'` RLS policy by prior explicit design (task 118 — "enforcement is application-level, not RLS"), meaning any signed-up `client` user could bypass the app's `canSeeAsset()`/`canSeeFolder()` checks entirely via direct Supabase REST calls and read/write every customer's secrets; migration 085 replaces it with row-level policies mirroring that JS logic exactly. Also caught and fixed 4 of migration 003's original 9 Phase-1 "tighten in Phase 2" tables that migration 084 (task 174) missed: `customer_products` (role-matrix, same pattern as `customers`), `playbooks`/`llm_invocation_logs`/`digest_logs` (staff-read-only, confirmed via grep that all writes go through `adminClient` already). `npx tsc --noEmit` clean throughout; migration written but not applied by the assistant (user applies directly, confirmed after task 174's migration). Findings #4-9 (authenticated-but-not-authorized gaps: products PATCH missing auth, reopen-onboarding/primary-contact/reply-send missing role checks, assets POST missing ownership check, customers GET missing explicit auth) deliberately deferred per user's "Handle #1-3 first" → `_docs/task/176-security-sweep-2-unauthenticated-routes-remaining-rls.md` (completed: 2026-07-22)
- [175] Security Sweep #1 — Medium/Low: Webhook Auth, Upload Validation, ops-chat Tool Gating, Dependencies — second half of the first OWASP sweep's findings, scoped as a deliberate follow-up from the Critical/High fixes (task 174) per user direction. Made Zoho webhook HMAC signature verification mandatory (was silently skipped if `ZOHO_WEBHOOK_SECRET` was unset, allowing unauthenticated requests to trigger paid LLM classification calls and flip `implementation_plans.status` via a guessed `zoho_task_id`) and stopped logging the full raw webhook body. Hardened `/api/upload` (public by design): dropped `image/svg+xml` (stored-XSS vector) and added a product-name allowlist + customer-existence check to close anonymous storage abuse under arbitrary paths. Gated ops-chat's Sanity MCP write tools (`create_documents`/`patch_documents`) to `admin`/`pm` in code — previously merged into every authenticated user's tool set with the restriction existing only as an unenforced system-prompt instruction. Resolved all 17 HIGH `pnpm audit` findings: `xlsx` had no npm-registry fix available (SheetJS moved off npm past 0.18.5) so it was repointed to SheetJS's own CDN build (resolved 0.20.3, past both the prototype-pollution and ReDoS fix thresholds); `sharp`/`serialize-javascript`/`brace-expansion`/`js-yaml`/`fast-uri` pinned via `pnpm.overrides` (`brace-expansion` needed a blanket override after range-scoped overrides left some transitive instances unpatched). Via `AskUserQuestion`: confirmed onboarding-ID widening as the fix approach for the brute-forceable 4-char customer ID (code change lives in task 174, same finding family), confirmed the SheetJS CDN approach for xlsx, and confirmed `role-access.ts`'s fail-open default should stay as-is (only gates legacy v1 page nav; v2 does its own role checks; sensitive prefixes already listed) rather than be flipped to deny-by-default without a full route audit. `npx tsc --noEmit` clean throughout; `pnpm audit --prod` HIGH/CRITICAL: 17 → 0 → `_docs/task/175-security-sweep-1-medium-low-webhook-upload-deps-onboarding-id.md` (completed: 2026-07-22)
- [174] Security Sweep #1 — Critical/High: Pipeline RLS + Missing Authorization — first full OWASP sweep of the app (all API routes, auth/session layer, RLS policies, AI orchestration pipeline, Zoho/Sanity/GitHub integrations, dependencies), Critical/High findings. Root cause: `customers`/`classification_records`/`requirements_assessments`/`implementation_plans`/`execution_records` still carried migration 003's Phase-1 `using (true) with check (true)` RLS policy explicitly flagged "tighten in Phase 2" but never revisited — any signed-up user, including the default `client` self-signup role, could read/write every customer's data directly via the Supabase REST API, bypassing the Next.js app entirely. New migration 084 replaces those with a role-matrix pattern matching migration 026's established convention (staff read, pm/admin/super_admin write, client-read-own on `customers`). Also added `pm`/`admin`/`super_admin` role checks to `/api/assessment`, `/api/execution`, `/api/plan` (POST+PATCH), `/api/orchestrate` — all four previously checked only `if (!user)`, letting any authenticated user trigger Sanity/GitHub execution or approve/reject implementation plans. Fixed `/api/orchestrate` trusting client-supplied `sanity_project_id`/`github_repo`/etc. instead of re-deriving them from the DB row for `project.id` — closes a path where a caller could redirect the AI executor's writes (shared `SANITY_GLOBAL_TOKEN`) to an arbitrary Sanity project. Upgraded Next.js `16.2.4` → `16.2.11`, resolving 6 HIGH advisories including a Middleware/Proxy bypass directly relevant to this app's `proxy.ts`-based auth-guard model. Widened `generateCustomerId()` from 4 to 8 hex characters (65,536 → ~4.3B combinations) in both the shared generator and a duplicate implementation in the Zoho customer-import route — the ID is the sole guard on the public, unauthenticated onboarding endpoints and was brute-forceable in seconds at 4 chars. Mid-implementation, user requested `super_admin`/`marketing` (real `profiles.role` enum values the stale docs omitted) be included in the new RLS — added `super_admin` everywhere `admin` appears (strict superset) and `marketing` read-only on `customers` only, per user's explicit scoping via `AskUserQuestion`. `npx tsc --noEmit` clean throughout; migration written but not applied by the assistant — user confirmed application before follow-up work continued → `_docs/task/174-security-sweep-1-critical-high-pipeline-access-control.md` (completed: 2026-07-22)
- [171] Onboarding Wizard — Design System v2.0 Full Step Redesign, Upload-First RTE Toggle, Deadline Badges — full v2.0 token pass across all 7 Wizard steps and every sub-component (`StorageFileExplorer`, `AddCredentialLinkModal`, `FileViewerModal`, `HtmlMockupFileList`, `HtmlEditorModal`, `PhaseAccessPanel`, `RichTextField`, `FileUploadBox`), `isDark`/dark mode dropped entirely (fixed-light, matching tasks 166–168's precedent for this surface — `isDark` prop removed from `OnboardingWizard` and its dead computation removed from `_onboarding-detail.tsx` too). Status-tinted deadline badges on every step header and mono `DAY N`/`PENDING` tags on every checklist row, per the design guide's own "Checklist row" spec. Upload-first, RTE-optional layout (new `UploadFirstField`, upload box primary/full-width with a redesigned `CloudUpload`-icon dropzone, RTE hidden behind a toggle defaulting open only when existing text is present) on Outcome Target, Migration Checklist, Content Map, and — added in a later round — Client Sign-off. Five rounds of live user feedback in the same session, all live-tested against a real project with an authenticated session (not skipped like tasks 166–168, since a session was available this time): (2) removed the redundant full-width `WizardDeliverableRow` status section in favor of a compact chip beside the step title, moved the checklist into a sticky right-side column instead of a full-width bottom section, redesigned the upload dropzone (thinner border, taller, `CloudUpload` icon), fixed a real checklist-validation gap (Migration Checklist's/Content Map's items were markable done with nothing filled in), and fixed a real navigation bug (the step indicator let you skip past incomplete steps, now capped to one step forward at a time); (3) added a light divider between the content and checklist columns, then: matched the upload box's error styling to the RTE's (red border/glow) across every field including two steps that had no error state at all before this, and root-caused + fixed a real bug where the status chip could get permanently stuck on "Pending" after a deep-link-style page load (the Wizard's local state could desync from the server with nothing to re-sync it — fixed via a render-time prop-sync, not a `useEffect`, to avoid this file's known `set-state-in-effect` lint constraint) plus three new "in progress" status triggers (scheduled day reached / field has content / partial checklist completion) computed client-side as a display-only overlay on the real persisted status; (4) converted Client Sign-off to the same upload-first layout as the other three, made HTML Mockup's dropzone full-width (was capped at `max-w-xl`); (5) moved the "Complete Phase 1" context messages out of the step panel into their own equal-width, distinctly-toned (amber/blue) card row above the footer nav, instead of two stacked identical banners inside the step's own box. `npx tsc --noEmit`/`pnpm lint` clean after every round → `_docs/task/171-onboarding-wizard-design-system-v2-redesign-upload-first-deadlines.md` (completed: 2026-07-22)
- [168] Portfolio Tracker Detail Page — Design System v2.0 (Timeline + Wizard Shell), "Jump to Today" Z-Index Fix — fixed the reported z-index bug (`notification-bell.tsx`'s two `z-99999` classes were unbracketed, so Tailwind silently emitted no CSS for them at all — the drawer rendered at default stacking while the floating button had a real `z-40`; bracketed to `z-[99999]`), and restyled the Timeline (`_onboarding-detail.tsx`, 1750 lines, read in full) to v2.0 as a strictly visual-only pass — `PHASE_VISUALS`/`PHASE_HEX` remapped to the app's fixed phase-hue vocabulary (Onboard=orange/Migrate=blue/Publish=violet/AI Visibility=teal/Optimize=green, correcting an unrelated old blue/violet/teal/amber/slate mapping), all `isDark` ternaries in the Timeline's own chrome collapsed to fixed-light while the `isDark` value itself stays threaded into the Wizard unchanged for its still-dark-mode-aware step content, and every v1 hex swapped to its v2.0 token. Wizard shell (7-circle step indicator, 2 lines) restyled light-branch-only; all 7 step contents (~5000 lines) explicitly untouched, matching this codebase's established per-step-task precedent. `DeliverableCard` drag/resize logic, phase-jump/access-gating/membership logic all verified unchanged. Six user-driven follow-up rounds in the same session, all logged in the task doc's Implementation Notes: (2) 7 buttons across the Timeline fixed to pill radius per DESIGN.md's button spec, "Onboarding Wizard" dropped its gradient fill for flat blue; (3) a full compliance audit against `_final_design/guide/central-hub-style-guide.html` found and fixed the list page's status-filter pills (were a segmented control, rebuilt as individual floating pills per spec), 6 more buttons at the wrong radius, and 5 form inputs missing their documented focus ring; (4) the shared `OnboardingStatusPill` (list page cards, PM dashboard's Clients table, Marketing dashboard) was rebuilt on the already-compliant `Chip` primitive — was `rounded-full`/500-weight/no-dot/blue, now 5px radius/700-weight/dot/`ok`-or-`warn`-or-`neutral` tone; (5) added a real `"completed"` project status end-to-end (`GET /api/onboarding/projects` never detected Phase-5 completion before this — a finished 120-day programme showed "In progress" forever; now checks Phase 5's `customer_phases` row), colored `ok`-green same as "In progress" (by design — `--ok` and `--ph-optimize` are the identical hex in this system) but distinguished by a checkmark instead of the status dot, plus a card-spacing fix; (6)+(7) the member-avatar stack on both the list page and the Timeline (`AvatarStack`/`CollaboratorAvatars`) — raised the "+N" overflow threshold from 3/4 to 5, replaced native `title` tooltips with the real shadcn/Base UI Tooltip (`AvatarCircle` converted to `forwardRef` on the Timeline page since Base UI's trigger needs to attach a ref a plain function component can't receive), and added a hover slide-up animation exclusively for multi-member stacks (single-member cards/rows get the tooltip only, no motion, deliberately skipped the tooltip on 2 call sites that already show the name as adjacent visible text). `npx tsc --noEmit`/`pnpm lint` clean after every round (repo-wide count held at 1 pre-existing, unrelated error throughout); manual in-browser QA not run this session (no test credentials) — flagged for the user's live-testing pass → `_docs/task/168-portfolio-tracker-detail-design-system-v2-zindex-fix.md` (completed: 2026-07-21)
- [166] V2 Dashboard — WebriQ Design System v2.0 (Navy/Blue/Orange) + Real 120-Day Programme Data — replaced `DESIGN.md`'s v1 dark-first oklch "Night Ops Console" system with Design System v2.0 (`#071133` navy / `#007BFF` blue / `#FB914E` orange, sourced from `_final_design/guide/central-hub-design-system.md`), and rebuilt `pm-dashboard.tsx` to match the mockup's full section set (`_final_design/dashboard/webriq-central-hub-dashboard.html`) with real Supabase data throughout, no fabricated numbers. Removed the old ticket/plan cards (`DecisionCard`/`TasksTable`/`DeskPulse`/`DailyDigestCard`); added 4 real stat tiles, the phase-colored 120-Day Programme board, a Clients table, Reminders (`GET /api/notifications`), a Developer queue (Phase 2 deliverables), a Phase 1 intake checklist (soonest-Day-15-gate client, `onboarding_internal_deliverables`), and Publish-phase progress (Phase 3 deliverables, honestly labeled as coarser than the mockup's page-level "weekly waves" since no real per-page data exists). New shared `Chip`/`PhaseChip`/`ProgrammeTrack`/`OnboardingStatusPill` primitives in `dashboard-shared.tsx`; `globals.css`'s `.pm-light` tokens updated to v2.0 hex so Dev/Marketing/Admin dashboards inherit the new palette automatically (none hardcode brand colors outside the `--c-*` token system, confirmed by reading all three in full — zero code changes needed there). Found and corrected a stale research premise mid-implementation: migration 070 (task 146, applied after the 060 the planning research cited) had already re-widened `customer_phases`/`customer_deliverables`/`onboarding_internal_deliverables` read access to `pm`/`developer`, so the plan's original "PM-limited vs. Admin-only-full-fidelity" split was unnecessary — corrected in the task doc before writing code, giving PM's dashboard the full section set and reducing `admin-dashboard.tsx` to a 10-line pass-through (previously also home to an "Admin-only extras" LLM-spend/event-bus block, since removed per live-testing feedback below). Live-testing round 2: user caught that the rewrite dropped the mockup's page-head (greeting stats subline + "Export weekly report" button) — restored with real `inProgress.length`/`lateProjects.length` counts and a working client-side CSV export of the loaded programme roster (no new API); user also asked to remove `WorkspaceCard` ("Your workspace") and Admin's LLM Spend/Event Bus Health block "for now" — both deleted along with their now-orphaned queries/imports. Round 3: user reported the greeting still invisible — traced to `use-greeting.ts`'s single app-wide `sessionStorage` key (`hub_greeting_ts`) already having faded from earlier same-tab testing, not a code bug; user ran the suggested `sessionStorage.removeItem` reset and confirmed it renders correctly. `npx tsc --noEmit`/`pnpm lint` clean throughout every round (1369 problems/77 errors baseline unchanged from before this task, all pre-existing and outside its touched files) → `_docs/task/166-v2-dashboard-design-system-v2-real-programme-data.md` (completed: 2026-07-21)
- [150] Portfolio Tracker — Nav Rename, `project_id` Route Param, Query-Param Wizard Step Addressing — retitled in scope partway through from the original "120-Day Programme" naming after user feedback during Testing; original plan + first revision both completed together in this doc. Original scope (3 parts, all shipped): (a) sidebar/page-title rename off "Onboarding," (b) `[projectId]` route now keyed by the human-readable `projects.project_id` instead of the UUID (a documented, scoped exception to CLAUDE.md's UUID-routing-key convention), (c) wizard step state made URL-addressable instead of pure `useState`. Found and fixed two latent bugs along the way not named in the original task doc: `GET`/`POST /api/onboarding/projects` both mis-labeled the UUID as `project_id` in their response shape (same bug category, only the GET route was originally flagged) — left unfixed, the "New Project" success screen's View button would have 404'd once the detail route required the real `project_id`; and `[projectId]/page.tsx`'s `phase_members`/`project_members` queries had to be switched from the raw route param to the fetched `project.id`, since those child tables are still UUID-keyed and the route param stopped being the UUID. Follow-up revision (post-Testing, same doc): user asked for a different URL shape than first shipped — route segment/label `programme` → `portfolio-tracker`, and the nested `/wizard/[stepKey]` route replaced with `?phase=&deliverable=` query params (1-based deliverable index into `getPhaseByNumber(phase).deliverables`, not the string key, so future deliverable additions/reorders in `customer-phases.ts` don't need another URL redesign) — one shared `stepIdx`-watching effect in the Wizard keeps Continue/Back/Steps-indicator navigation and the URL in sync without touching each of the 6 internal step-change call sites individually. Also added a breadcrumb-map entry (the route previously fell through to the header's default "WebriQ / Hub" since no entry existed for it) and relabeled the detail page's back-link "Back to Onboarding" → "Back to Projects" (label only, same destination — no new target was specified). Implemented the new path as `/v2/portfolio-tracker/[projectId]` rather than a literal root-level `/portfolio-tracker/[projectId]` as the request's example URLs showed, since everything in this app's v2 build (sidebar, header, auth guard) lives under `/v2/*` and a true root-level route would need its own duplicated layout — flagged to the user rather than assumed silently. Final round: user confirmed the one item flagged as still open — the list page's `<h1>🚀 Onboarding</h1>` heading now reads "🚀 Portfolio Tracker" too. `npx tsc --noEmit` passes clean throughout every round; `pnpm lint` has one pre-existing, unrelated failure (`_onboarding-wizard.tsx`'s `checklistValidationError`-clearing effect, `react-hooks/set-state-in-effect`) that predates this task and was never touched by it → `_docs/task/150-onboarding-programme-rename-route-project-id-wizard-routes.md` (completed: 2026-07-17)
- [161] Onboarding Wizard — Bert Label Removal, Checklist Redesign, shadcn Tooltips, Clickable Steps Indicator & Preview Fixes — retitled from the original "Remove Static 'Bert' Owner Label, Checklist Checkbox Redesign & 'Mark All as Done' Button," which shipped as originally scoped, but the same session kept surfacing follow-up work on the same Wizard file across 7 rounds. Round 1 (original scope): removed the static "Bert"/"PM + Bert" owner pill from `WizardDeliverableRow` (Phase 1 step header); redesigned internal-checklist items from `Circle`/`CheckCircle2` icons to a custom checkbox (outlined `rounded` square unchecked, `bg-green-500` filled square + white `Check` when done — sized `h-5 w-5` since this project's `--radius-lg` token is 12px and fully circularizes a 16px box); added a hover tooltip right of each item's label ("Mark as Done"/"Uncheck"); added a per-step "Mark All as Done" button reusing the existing incomplete-items/force-confirm validation modal via a new `markAllAdvance` flag so it never auto-advances the step (unlike the Continue-gate's own use of the same modal). Round 2: swapped the hand-rolled hover-reveal tooltip for the real shadcn Tooltip (`npx shadcn add tooltip`, Base UI-backed per this project's `base-nova` style; `TooltipProvider` added to the root layout) — found and fixed it rendering as a near-invisible white-on-white pill, since `<html>` carries a permanent `dark` class app-wide while v2 pages theme independently via a JS `isDark` prop that defaults to light; hardcoded `src/components/ui/tooltip.tsx`'s Popup/Arrow to a solid dark pill instead of the theme tokens. Round 3: rolled the same Tooltip out across the entire Wizard — the 7-circle step indicator (bottom placement, shows the full untruncated step name) and ~25 icon-only Add/Remove/X/View/Edit/Actions buttons across every step and the Storage/KB file explorer, via a small local `IconTip` helper, replacing native `title` attributes. Round 4: fixed two unrelated visual bugs found along the way — the active step's `ring-4` glow was clipped at the top because the steps-indicator scroll container only padded horizontally (`px-1 -mx-1`; `overflow-x-auto` forces `overflow-y` non-visible per spec too), fixed to `p-1 -m-1`; and CSV/Excel file-preview columns were unreachable because the modal's preview flex item had `min-h-0` but not `min-w-0`, letting it silently grow past the modal width instead of its own `overflow-auto` div ever registering overflow — added `min-w-0`. Round 5: made that now-functional CSV/Excel scrollbar actually visible at rest — discovered Tailwind v4 generates zero CSS for `[&::-webkit-scrollbar-thumb]:bg-*` arbitrary-variant syntax (confirmed via the page's own compiled stylesheet rules), so the fix from Round 4 was scrollable but still invisible (the app-wide thumb color is a translucent white tuned for the Hub's permanently-dark surfaces); replaced with a plain `.scrollbar-light` class hand-written in `globals.css`, matching how the app's existing scrollbar styling is already raw CSS there, not a Tailwind utility. Round 6: made the step indicator clickable — already-reached steps always navigate; jumping to an unreached step is blocked with a custom modal ("'{step}' needs to be completed first before continuing to the other step") unless the currently-viewed step is done or overdue (`currentDay > step.dayEnd`); pm role bypasses the gate entirely, mirroring `handleContinueClick`'s own existing pm bypass. Round 7: restored a real pre-existing validation gap the user caught while re-testing — `handleValidatedInternalToggle` only ever gated 4 of the 5 field-backed checklist keys; `outcome-target-filed` (Outcome Target's "Agreed measurable outcomes…" item) could be checked with the field completely empty, no error, since it fell through to the function's own documented no-op passthrough for unlisted keys (predates this session, never touched by rounds 1-6). Added the missing `outcome-target-filed && !isOutcomeFilled` branch (mirroring the existing 4), plus the same key to `handleMarkAllDone`'s force-confirm check and `handleReview`'s field-highlight reset, so all three entry points (direct checkbox click, "Mark All as Done", the Continue-gate's own modal) now correctly block/highlight instead of silently completing. `npx tsc --noEmit`/`pnpm lint` pass clean throughout all 7 rounds; every round live-tested in-browser (checklist toggle/tooltip/Mark-All-as-Done including the force-confirm bypass path, step-indicator and Add/Remove tooltips matching reference screenshots pixel-for-pixel, ring-glow fix via zoomed screenshot comparison, CSV overflow+visible-scrollbar fix via a real uploaded wide test file, clickable-steps blocking/allowed cases, Round 7's restored validation on a fresh empty Outcome Target step) — no console errors → `_docs/task/161-onboarding-wizard-remove-bert-label-checklist-redesign-mark-all-done.md` (completed: 2026-07-17)
- [160] Onboarding Timeline — Fix Jump-to-Phase "Day N" Indicator + Read-Only Previous Phases — fixed two related bugs in the manual "Jump to phase" override (`PATCH .../programme/phase` + `OnboardingWizard`). Bug 1: the already-started branch of the phase route left `projects.programme_started_at` untouched, so the calendar-day-driven "Day N" marker/header text/progress bar never moved after jumping an in-progress project to a different phase — now backdates it the same way the not-started branch already does (`adminClient`, matching `seedProgrammeAtPhase`'s convention for that table). Bug 2: `OnboardingWizard` had no concept of whether the phase it renders is still the DB's active phase, so a jumped-past Phase 1 stayed fully editable/uploadable — added a required `isPhaseActive` prop folded into `isStepReadOnly`/`canEditChecklist` (now `|| !isPhaseActive`, extending storage-kb's existing PM-only carve-out to also require an active phase, no carve-out once inactive) and into the completion-flow gating (banners, error text, Complete-Phase-1 button/message, new "Phase 1 is no longer active" copy). The Timeline's "Onboarding Wizard" CTA button now stays visible after a jump (relabels to "View Onboarding Wizard") instead of disappearing once Phase 1 is no longer active; `isPhaseActive` is computed inline from `phases` state rather than the later `phaseStatusMap` to avoid a `const` temporal-dead-zone error in the wizard's early-return render branch. `npx tsc --noEmit`/`pnpm lint` pass clean; no live browser verification run this session. Marked complete for now at the user's request — a follow-up task is planned for tomorrow covering further Onboarding Phase/jump-to-phase work, including an explicit in-Wizard message telling the user *why* a phase is read-only (currently fields are just disabled with no explanatory copy) → `_docs/task/160-onboarding-jump-to-phase-fix-active-indicator-readonly-previous.md` (completed: 2026-07-16)
- [159] Onboarding List — "Import Project" CSV/Excel Bulk Intake — bulk CSV/Excel import for onboarding projects, completed together with 5 rounds of live user testing/iteration all bundled into this one doc. Initial build: full-page wizard (`/v2/onboarding/import`, replacing an earlier modal design after round 1 feedback that its columns clipped and rows couldn't be fixed) with an Upload → editable Review & Fix table → Import flow; new `POST /api/onboarding/projects/import` route (partial-failure-tolerant, per-row customer/contact/phase seeding) reusing a newly-extracted `seedProgrammeAtPhase` helper shared with `PATCH .../programme/phase`'s not-started branch. Round 2: converted the review table's static preview into fully editable rows (`Customer`/`Project Type`/`Primary Contact`/`Kickoff Date`/`Current Phase`), reusing the New Project wizard's exact StackShift-grouping rule for Type selection. Round 3: renamed `Account`→`Customer`/`Type`→`Project Type`, replaced the 6-chip Type picker with a searchable multi-select combobox (portaled dropdown to escape the table's scroll-container clipping), added placeholder-value detection for Primary Contact ("To be confirmed"/"Unknown" clear the field but show the original text below). Round 4: quoted the Primary Contact placeholder hint, added a per-pill remove `×` button, fixed invisible search-box input text. Round 5: fixed a real Kickoff Date bug — SheetJS was silently reinterpreting day-first `DD-MM-YYYY` sheet text as US month-first, corrupting dates like "09/07/2026" into September instead of July; rewrote the parser to explicitly handle day-first strings and genuine Excel date-serial numbers. Confirmed the Timeline's existing `getCurrentProgrammeDay()` math already derives the correct active day/sub-phase once `programme_started_at` is set correctly (no new logic needed there beyond the date-parsing fix), and generalized `seedProgrammeAtPhase`'s per-deliverable seed status to mark whichever deliverable's day range contains the current elapsed day as "in_progress". Added an "Overdue" warning (Phase 1 still active past its 15-day window) in both the import review table and the Timeline's existing reminders-strip system. Verified end-to-end via a real completed import (not just previewed) matching a user-supplied worked example (Kickoff July 9, Day 8, Migration Checklist active) and an overdue case (Kickoff June 1, Day 46, "Phase 1 Overdue" banner). Also found and fixed one unrelated schema bug as a byproduct of cleaning up test data: `customer_asset_folders_project_id_fkey` (migration 065) was the only `project_id` foreign key in the schema without `ON DELETE CASCADE`, blocking test-row deletion — added migration 081 to match every sibling FK's convention (not yet applied by the user). `npx tsc --noEmit`/`pnpm lint` pass clean throughout all rounds → `_docs/task/159-onboarding-import-project-csv-excel.md` (completed: 2026-07-16)
- [158] New Project — Scheduled Start Redesign: Date/Time Picker, Phase-Aware Scheduling, QStash + Cron Reliability — retitled from the original "Move 'Scheduled Start' from Project Details to Review & Create," which shipped as originally scoped, but the same live-testing thread kept surfacing follow-up work on the exact same feature across 9 rounds, all bundled into this one doc rather than opened as separate tasks (same convention as task 155). Round 1: fixed a pre-existing duplicate "Company name is required" error (a leftover standalone error span meant only for the existing-company search view rendering unconditionally). Round 2: selecting an existing company now fetches and pre-fills its primary contact (new `GET /api/customers/[customerId]/primary-contact`, adminClient-backed since marketing isn't covered by `contacts_staff_read` RLS) with a loading state on the three contact fields. Round 3: server-side duplicate-name validation for company (new-company only) and project name (new `check-name` endpoints), blocking `goNext()` with an inline error. Round 4: added a "Start at phase" dropdown (admin/super_admin/marketing only, mirroring `_onboarding-detail.tsx`'s `canManagePhases` exactly) so immediate starts can jump straight to Phase 2-5 via the Timeline's existing "Jump to phase" route, plus a first-pass date-range constraint on the scheduled-start field. Round 5: hid "Start Now" while scheduling, restyled "Confirm & schedule" to match "Start Now"'s design, and made "Just save" clear the schedule state. Round 6: replaced the native `datetime-local` input (inconsistent across browsers/OS) with a custom `react-day-picker`-based `DateTimePicker` matching the app's own design tokens, plus auto-flip positioning so it never opens off-screen. Round 7: scheduled starts became phase-aware end-to-end (migration 076's `scheduled_start_phase`, `seedAndStartProgramme` generalized to accept a target phase, unifying the Phase-1 and Jump-to-phase seeding paths) and the Timeline's not-started card got a matching scheduled-state redesign ("Start Phase N Anyway" / "— OR —" / "Select Phase" + "Proceed"). Round 8: root-caused a real production bug — **all 5** cron-triggered jobs in the app (not just this one) had been sitting on unfilled `REPLACE_WITH_APP_URL`/`REPLACE_WITH_DIGEST_SECRET` placeholders since their original migrations and had never actually fired; fixed via Supabase Vault-based secret lookup (migrations 077/078, so real values never touch a git-tracked migration) plus an app-wide rename of the shared secret (`DIGEST_SECRET`→`CRONJOB_SECRET_KEY`, `x-digest-secret`→`x-cron-secret`) and a tightened 5-minute interval for just this job (migration 079); live-diagnosed and fixed a second bug where the Vault secrets themselves hadn't been created yet, which nulled out `net.http_post`'s url/headers and violated a `NOT NULL` constraint. Round 9: added Upstash QStash as a one-shot exact-time trigger (new `src/lib/qstash/index.ts`, migration 080's `qstash_message_id`, a signature-verified idempotent callback route) with the cron poll deliberately kept as a fallback safety net; fixed a build-breaking bug where `verifySignatureAppRouter` throws at module-load time without signing keys configured, and a live region-routing bug where QStash's global endpoint resolved to the wrong region for the user's account (fixed via `QSTASH_URL` env var, no code change). Verified via schema inspection that a QStash/cron race can't corrupt data (`project_id`-scoped unique constraints on all three seeded tables turn a losing racer into a caught, harmless Postgres error). `npx tsc --noEmit`/`pnpm lint`/`pnpm build` pass clean throughout all 9 rounds; user confirmed live in production that both the cron fallback and QStash successfully auto-started real scheduled test projects → `_docs/task/158-new-project-move-scheduled-start-to-review-step.md` (completed: 2026-07-16)
- [156] New Project — Optional Primary Contact + Horizontal Contact Email/Phone Layout — dropped step 1's client-side requirement that Primary contact name/email be filled in, since the same contact can be entered later at Kickoff (`ContactsField`, task 129/151's `upsertPrimaryContact` sync) — confirmed via code reading that the server (`POST /api/onboarding/projects`) already treated the whole `contact` object as optional, so this was purely a client-side validation change (`goNext()`'s step-1 check, `_content.tsx`). Kept the email-format regex check but gated it on non-empty input; removed the `required` prop/asterisk from Primary contact and Contact email; wrapped Contact email + Phone in a `grid grid-cols-1 sm:grid-cols-2 gap-3` row so they sit side-by-side on desktop and stack on mobile, Primary contact staying its own full-width row above. Confirmed by reading the code (not live-tested in browser, extension unavailable this session) that leaving contact fields blank at intake doesn't lose data — the Kickoff step's `ContactsField` pre-fill reads live from the `contacts` table (`page.tsx`'s `is_primary` query), and its own autosave (`wizard-data/route.ts`) calls `upsertPrimaryContact` again when the PM fills it in there instead, so the redundant early-entry point is simply removed, not a data path. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/156-new-project-primary-contact-optional-horizontal-layout.md` (completed: 2026-07-16)
- [155] Project Ownership Transfer, Wider Collaborator Pool & Search-to-Add Picker — third task in the 153→154→155 project/phase-membership arc, completed together after 4 rounds of live user testing/iteration on this doc (informally tracked as "task 156"/"task 157" in code comments — no separate task doc files exist for those, everything landed here). Round 1 (task 156): relocated Phase 1's member-management UI out of the Timeline page's `AccessPanel` and into `_onboarding-wizard.tsx` itself (new `PhaseAccessPanel`, toggled via an "Access" button in the Wizard's persistent header), since managing who has Phase 1 access belongs inside that phase's own workspace — `_onboarding-detail.tsx` still owns the `phase1Members` state and the Wizard-entry restriction gate, only the management UI moved; adding a phase member now also auto-adds them as a `project_members` row (non-owner), so anyone with phase access can find the project on the list too. Round 2 (task 157): added a real, transferable project-level owner (`project_members.is_owner`, mirroring `phase_members.is_owner` from task 153, migration 074) — the project creator becomes owner at creation, Super Admin/Admin/creator can transfer it afterward (including on legacy pre-153 projects with zero members); replaced the static "Owner: Bert" header text (a hardcoded per-phase config label never connected to any real user) with the actual owner's avatar, falling back to the project creator's name for legacy projects; added a "Collaborators: [avatars]" row (includes phase-added members automatically); replaced the "Access" text button with a Gear "Project Settings" icon opening a 2-item dropdown ("Set Project Owner" / "Add Collaborators"), each expanding its own focused panel — `canManageProjectMembers`/new `canSetProjectOwner` were re-keyed off "is this caller the project creator" rather than plain membership, per the user's explicit final role list (Super Admin/Admin/PM/creator can add collaborators; Super Admin/Admin/creator can set the owner) — this deliberately dropped `marketing` from collaborator-adding rights, flagged clearly to the user since it had those rights in every earlier round; also fixed an unrelated wizard step-indicator clipping bug (active step's `ring-4` glow was being cut by its `overflow-x-auto` container's edge). Round 3: user's first live test surfaced a real `PGRST201` PostgREST error — `project_members`/`phase_members` each have two FKs into `profiles` (`user_id` and `added_by`), so a bare `profiles(...)` embed was ambiguous everywhere either table joined to `profiles` (4 queries across `page.tsx` and both members API routes, including code originally from task 153) — fixed by qualifying every embed with its explicit FK constraint name. Round 4: minor layout polish (search field above collaborator chips instead of beside them, added bottom margin so the expanded panel doesn't touch the progress bar below it). `npx tsc --noEmit`/`pnpm lint` pass clean throughout all rounds; user live-tested each round in the browser before approving → `_docs/task/155-project-owner-search-picker-collaborator-refinement.md` (completed: 2026-07-15)
- [154] Onboarding List — Member Avatar Chips on Project Cards — second task in the 153→154→155 arc, completed together. `GET /api/onboarding/projects` now returns a deduped `members` array per project (union of `project_members` + Phase 1 `phase_members`, resolved to `profiles.full_name` via one batched lookup), and each list card renders up to 3 overlapping initials avatars + a `+N` overflow chip in its bottom metadata row, reusing `OwnerChip`'s (Projects module) exact initials/color derivation for visual consistency but as a smaller overlapping stack with overflow — behavior `OwnerChip` itself doesn't have. No changes were needed to this task's own files during the 4 rounds of follow-up work under task 155 — its query pattern (`profiles` queried standalone via `.in("id", ids)`, not embedded through `project_members`/`phase_members`) was never subject to the `PGRST201` ambiguity the other two tasks in this arc hit. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/154-onboarding-list-member-avatar-chips.md` (completed: 2026-07-15)
- [153] Project & Phase 1 Membership — Ownership, Access Gating & Project Visibility — foundation task for the 153→154→155 arc, completed together. Built a two-tier membership model: `project_members` (gates Onboarding-list visibility for marketing/pm; admin/super_admin always see everything) and `phase_members` (gates entry into Phase 1's Wizard, with a single transferable owner via a partial unique index, migration 073) — both tables use a "zero membership rows = unrestricted" backward-compatibility rule so shipping this never locked out any already-in-progress onboarding. Widened project creation and "Start Onboarding" to include `pm` (previously admin/super_admin/marketing only, in three separate `CREATE_ROLES`/`WRITE_ROLES` lists); `seedAndStartProgramme` (shared by 3 callers, including a session-less cron discovered mid-implementation) gained an optional starter-user parameter to seed Phase 1 ownership. Read the requirements as meaning `marketing` — not just `pm` — is also phase-membership-gated (today any marketing user has unrestricted Wizard access; flagged as the one non-literal interpretation in the task doc for the user to confirm). Split the planned single membership-logic file into `membership-rules.ts` (pure, client-safe) + `phase-membership.ts` (adminClient-backed mutations) after discovering `_onboarding-detail.tsx` (a Client Component) needed the pure permission checks — CLAUDE.md forbids importing `adminClient` client-side. User live-tested this task together with 154/155 across 4 follow-up rounds (see task 155's Implementation Notes) and found one real bug that traced back to this task's original code: a `PGRST201` PostgREST error from `project_members`/`phase_members` each having two FKs into `profiles`, fixed under task 155's Follow-Up 3 by qualifying the ambiguous embeds. Also superseded later: this task's original `AccessPanel`/`AddPersonSelect` Phase 1 UI was relocated into the Onboarding Wizard under task 155's Follow-Up 1 — the restriction gate and `phase1Members` state this task built remain unchanged, only the management UI moved. `npx tsc --noEmit`/`pnpm lint` pass clean → `_docs/task/153-project-phase-membership-access-control.md` (completed: 2026-07-15)
- [152] Onboarding Wizard — Real-Time Upload Progress Bars + Kickoff/Business Facts File Preview — follow-on to task 149. Added a shared `uploadFileWithProgress` helper (`XMLHttpRequest` wrapped in a Promise, since `fetch` exposes no upload-byte progress) and wired it into all 6 `FileUploadBox`/`HtmlMockupFileList` upload handlers (Business Facts, Outcome Target, Migration Checklist, Content Map, Client Sign-Off, HTML Mockup), each tracking a per-file progress array rendered as a live bar in the two shared components; as a side effect this also fixed a pre-existing race from task 149 where concurrent uploads shared one step-level `uploading` boolean (now derived from the progress array's length instead). Also closed a real gap: Kickoff/Business Facts was the one `FileUploadBox` call site never wired to the shared `onView`/`FileViewerModal` preview every other step already had — added `handleViewBusinessFactsFile`, an exact mirror of the existing pattern. Three rounds of live user testing surfaced and fixed real issues beyond the original plan: (1) the `finishing` state meant to bridge the gap between byte-upload completion and full server response was itself set too late (only after the second DB-insert request resolved), so it never covered the actual XHR-to-response wait — fixed by moving the transition into the progress callback itself, flipping the instant `upload.onprogress` hits 100%; (2) the pulsing-bar "Finishing…" treatment was replaced with a spinning `Loader2` icon (this codebase's established loading-state convention, per `_milestone-panel.tsx`/`_task-drawer.tsx`/etc.) after the user pointed out a bar visually implies measured progress that doesn't exist for that phase. Also fielded and declined (with reasoning, no code change) a question about switching to Server-Sent Events for that phase — traced the upload route (`assets/upload/route.ts`) to confirm the server has no granular progress to stream in the first place under its current buffer-then-atomic-`supabase-js`-upload approach; real end-to-end percentage would require a chunked/resumable protocol (e.g. TUS) first, a materially bigger change flagged as a possible future task rather than bundled in here. `npx tsc --noEmit`/`pnpm lint` pass clean throughout all 4 rounds (initial implementation + 3 follow-up fixes); every fix was live-tested by the user in the browser (including real 7.8MB and 25.4MB file uploads) before being marked complete → `_docs/task/152-onboarding-wizard-upload-progress-bars-kickoff-preview.md` (completed: 2026-07-15)
- [151] Primary Contact — Migrate from customers Columns to contacts.is_primary — moved the write-side source of truth for a customer's primary contact off the two loose `customers.contact_name`/`contact_email` columns (plus phone, previously stashed in `projects.source_meta.primary_contact_phone` since `customers` never had a phone column) onto a proper `contacts.is_primary` flag, since a customer can have many contacts and `contacts` (task 117) already models that. This directly reverses task 120's earlier decision to reject exactly this approach — revisited because phone finally needed a real home and task 147 (this session) added a third awkward `source_meta` read path. Kept `customers.contact_name`/`contact_email` as a synced read cache so the 15+ existing read-only call sites (search, list views, public onboarding pre-fill, `reply.ts`) needed zero changes; only the four write paths (v1/v2 create, v1/v2 edit, New Project form, Kickoff autosave) route through one new shared helper (`src/lib/customers/primary-contact.ts`), plus task 147's Kickoff pre-fill switched from reading `source_meta` to reading the customer's `is_primary` `contacts` row. New migration 072 adds `full_name`/`is_primary` columns, a DB-enforced partial unique index (one primary per customer), and backfills existing `customers` contact data. Also fixed two real bugs found only by tracing actual role access (not visible from the plan alone): the `marketing` role can call two of the affected write/read routes but isn't covered by `contacts` table RLS, which would have silently dropped its writes/reads — fixed with `adminClient` for just those specific operations, no RLS policy touched; and a uuid-vs-empty-string comparison bug in the demote-primary query that Postgres would have rejected. Also caught and fixed a duplicate-insert risk this migration would otherwise have introduced in the existing Phase-1 hand-off bulk-insert (task 129). `npx tsc --noEmit`/`pnpm lint` pass clean. User applied migration 072 live; first re-test still showed a blank Kickoff pre-fill on a reused test project — not a code bug, but the pre-fill's by-design "never overwrite an already-autosaved Kickoff" behavior colliding with a stale blank snapshot from earlier testing (task 147). Resolved with a one-line SQL fix clearing just that project's stale `wizard_data.kickoff.contacts` key; user confirmed the pre-fill then worked correctly → `_docs/task/151-primary-contact-migration-contacts-table.md` (completed: 2026-07-15)
- [148] 120-Day Timeline — Real Day-Span Scheduling + Drag Resize/Move — widened every Phase 2-5 `DeliverableConfig` day span in `customer-phases.ts` from the old single-day pins to real multi-day ranges (Phase 1 untouched); added migration 071 (nullable `day_start_override`/`day_end_override` on `customer_deliverables`, check-constrained, no RLS change needed) for per-project schedule edits that never mutate the shared static config; new role-gated (`admin`/`super_admin`/`marketing`) `PATCH .../deliverables/[deliverableKey]/schedule` route mirroring the sibling status-PATCH route's shape, validated against the deliverable's own phase day range. `_onboarding-detail.tsx`'s `Swimlane` merges `override ?? default` before rendering; `DeliverableCard` gained a custom pointer-capture drag implementation (no new library) with resize-left/resize-right/move modes, whole-day snapping, phase-range clamping, and a 4px movement threshold to distinguish a Phase-1 click (opens the wizard, unchanged) from a drag (resizes/moves, suppresses the click) on the same card body; drag-end optimistically updates state and calls the new PATCH, rolling back via the page's existing error banner on failure. `npx tsc --noEmit`/`pnpm lint` pass clean; migration 071 created but not applied, manual/browser verification skipped (no live environment this session). One flagged pre-existing limitation (not introduced or fixed by this task): the override lookup is keyed only by `deliverable_key`, matching the established `deliverableStatusMap` convention — but that key repeats across Phase 4 and Phase 5 (`updated-publishing-plan`, `gap-publishing`), so an override on one could currently apply to its same-keyed sibling in the other phase → `_docs/task/148-timeline-day-span-drag-resize-move.md` (completed: 2026-07-15)
- [144] Storage/KB File Explorer — Folder Permissions, Folder Rename/Delete, File Rename — added `allowed_roles`/`allowed_user_ids` to `customer_asset_folders` (migration 068) with server-side visibility filtering via a new `canSeeFolder()` mirroring the existing asset-level `canSeeAsset()`; new `PATCH`/`DELETE /api/customers/[customerId]/assets/folders/[folderId]` route (rename allowed on any folder including the 6 system folders + 3 pre-seeded sub-folders — cosmetic, folder membership is `folder_id`-based since task 141; delete blocked on system folders and on any non-empty folder, both confirmed defaults from the user rather than silent assumptions, to avoid the `ON DELETE CASCADE`/`ON DELETE SET NULL` FK orphaning edge cases); extended `PATCH .../assets/[assetId]` for file rename (display-name only, `file_path`/Storage object untouched). Folder kebabs gained Permissions/Rename/Delete, file kebabs gained Rename, both sharing one rename modal and the `renderPersonPicker` search-to-add UI (task 141). Live-run follow-ups after the user applied migration 068 and tested: rename modal's Save button now shows "Saving…" while busy; the new folder Permissions panel got the same explicit ✕ close button added to task 141's file panel, so neither requires re-clicking "Permissions" to close → `_docs/task/144-storage-kb-folder-permissions-rename-delete-file-rename.md` (completed: 2026-07-14)
- [143] Storage Bucket Path — Use Public project_id Instead of Project UUID — swapped the Supabase Storage path's project segment from the raw `project.id` UUID to the new human-readable `project_id` (task 142), so new uploads land at `{customerId}/{project_id}/{timestamp}_{filename}` instead of a UUID-shaped path; existing files keep their old path unchanged (`file_path` is always read as-is, per task 137's established precedent). Required widening the `project` prop type through all three files in the render chain (`page.tsx` → `_onboarding-detail.tsx` → `_onboarding-wizard.tsx`) — the middle one wasn't anticipated in the original file list, added as a necessary pass-through dependency. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean; the specific Storage-path change itself wasn't independently re-verified against the Supabase dashboard this session → `_docs/task/143-storage-path-human-readable-project-id.md` (completed: 2026-07-14)
- [142] Projects — Rename zoho_project_id → external_project_id + New Public-Facing project_id — confirmed with the user before proceeding: task 117 had deliberately kept `zoho_project_id`'s Zoho-specific name since it was still live-synced, but with Zoho being decommissioned that reasoning no longer applies, so the rename went ahead across all 90 genuine references in 30 application files + `database.ts`, carefully preserving the unrelated `_zoho_project_id` (underscore-prefixed) convention — a completely different, still-untouched thing used only for tagging raw Zoho IDs onto exported records for import correlation (verified via whole-tree greps before/after: 40 occurrences, unchanged). Migration 066 also added a new `project_id` column (`<last 4 chars of customer_id>-PROJ-<4 random chars>`, auto-generated by a `BEFORE INSERT` trigger + one-time backfill for existing rows) — display-only, the UUID `id` stays the routing key everywhere. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean. User applied the migration live; the one `NOTICE` in the `supabase db push` output (`trigger ... does not exist, skipping`) was the migration's own defensive `drop trigger if exists` running before first creation — expected, not an error. The generated `project_id` format was subsequently confirmed live via a real value (`2EBA-PROJ-04BA`) visible in a screenshot the user shared while testing task 141/144's File Explorer → `_docs/task/142-projects-external-project-id-rename-public-project-id.md` (completed: 2026-07-14)
- [141] Storage/KB File Explorer — Drive-Style Folders/Files, Custom Folder Tree, Bulk Actions — replaced the fixed, label-derived-only folder model (tasks 134/139) with a real folder tree (`customer_asset_folders`, migration 065, arbitrary depth via `parent_folder_id`) confirmed with the user to span the "whole explorer" (not just under Business Files), auto-provisioning the six former hardcoded folders plus three new `Branding`/`Proposals`/`Collateral` sub-folders that replace the removed free-text "Documents (branding / proposals / collateral)" note field. Redesigned folder tiles to a border-less pill+kebab look and files to Google-Drive-style cards with a kebab-only actions dropdown (View/Permissions/Move to folder/Remove), added multi-select with a Drive-style bulk toolbar (Share/Move/Delete — no Download, confirmed with the user since this app has no download feature anywhere by deliberate design), and a "New folder" control at every level. Live-run bug found immediately after the user applied migration 065: `customer_asset_folders` had no RLS policy (this Supabase project auto-enables RLS on new tables), so every insert 500'd — fixed with migration 067, an identically-shaped permissive policy to its sibling `customer_assets` (migration 021). Several rounds of live UI feedback followed and were applied directly: breadcrumb iterated from "hidden at root" to "always visible with the project's public ID + a 'Loading folders…' state" to "non-clickable and greyed specifically at root"; multi-select switched from checkboxes to Google-Drive-style highlight-on-select (card became a `<button>`, kebab repositioned as a sibling to avoid HTML's no-nested-`<button>` rule); a real bug where the bulk Share panel never closed (a standalone flag no other bulk action reset) was fixed with a `closeBulkSharePanel()` helper wired into every other action plus a `selectedIds.size > 0` gate as a second line of defense; "Share with specific people" switched from listing the entire staff directory to a search-to-add picker (`renderPersonPicker`, mirroring the Credentials & Links "Add" modal's existing pattern) shared by both the file Permissions panel and the bulk Share panel; and the file Permissions panel gained an explicit close button. `npx tsc --noEmit`/`pnpm lint`/`pnpm build` all pass clean throughout → `_docs/task/141-storage-kb-drive-style-folders-bulk-actions.md` (completed: 2026-07-14)
- [129] Kickoff Step Finalization — Multi-Contact Field, Business Facts Attachments, Validated Completion Checklist & Two-Column Layout — reworked the Kickoff step's data shape and completion mechanics on top of task 128's styling pass. Removed `seniorContact`/`customerData`; added a repeatable `ContactsField` (primary contact non-removable, per-row email/phone validation, icon-only outline add/remove controls) syncing the first contact to `customers.contact_name`/`contact_email` on every autosave; renamed `directAccess` → `additionalNotes` with a read-time fallback so already-saved content isn't lost; added URL validation to Website/Competitor fields; gave Business Facts a file-attachment capability (with a remove button, reusing the existing `DELETE /api/customers/[customerId]/assets` endpoint already used elsewhere) required alongside its rich text content. The 3-item completion checklist reuses task 127's `onboarding_internal_deliverables` mechanism verbatim (zero changes to its auto-derive-parent-status logic) via two new migrations (`061_contacts_external_id_nullable.sql`, `062_backfill_kickoff_internal_deliverables.sql` — not yet applied, user opted to apply personally) plus a `seedAndStartProgramme` change so Kickoff starts "in_progress" automatically at onboarding start. Checklist items are a direct pending↔done toggle (a mid-task correction from the initial 3-state cycle, since "in_progress" is reserved for the parent step). Continue now gates on any incomplete checklist item via a two-step confirmation flow (list incomplete items → "Mark all as done" → if that would bypass Kickoff's contacts/business-facts validation, a force-bypass "Yes, proceed" vs. "Review" — which red-glows the invalid field(s), self-clearing once fixed) built from scratch this task, including catching and fixing a real bug where the red-glow border was being visually overridden by the blue focus ring on the same element. On Phase-1 hand-off, kickoff contacts now bulk-insert into the `contacts` table as `match_method: 'manual'`. Verified extensively live in the browser across many rounds of user feedback, including creating a throwaway test project ("QA Test Co 129") to verify seed-time behavior unaffected by the still-unapplied migrations, and confirming the primary-contact sync against real DB data. `npx tsc --noEmit` and `pnpm lint` clean throughout; migrations still need to be applied by the user before the checklist/hand-off-contacts paths work on already-in-progress projects → `_docs/task/129-kickoff-contacts-checklist-two-column-redesign.md` (completed: 2026-07-10)
- [127] DeliverableCard — Progress-Ring Redesign, Auto-Status from Checklist, Click-to-Wizard-Step & Hover Detail Popover — redesigned the 120-Day Programme Gantt's `DeliverableCard` (task 125's view) from a status-icon pill into a progress-bar card: filled pie/ring indicator (outer ring + gap + wedge, checkmark at 100%) + title + `%`, two-tone solid-fill/diagonal-stripe-track background in the card's phase color. Clicking a Phase-1 card no longer cycles status — it opens the Onboarding Wizard directly on that deliverable's step (`initialStepKey` prop); non-Phase-1 cards stay inert. For sub-phases with an internal checklist, status is now derived automatically (new side effect on `PATCH .../internal-deliverables/[deliverableKey]`, which recomputes and returns the parent deliverable alongside the internal row) instead of manually toggled — `WizardDeliverableRow` goes read-only for those steps. Added a hover popover (title/description/owner/calendar dates/%, no chart) separate from the existing click-triggered internal-checklist badge popover, which itself was changed post-launch (live user feedback) to navigate to the owning wizard step on click instead of toggling status directly. Iterated the progress-ring visual through 6 rounds of live feedback (stroke ring → filled pie with outer ring, matched in-progress/completed lightness, per-character split text color at the fill boundary, fixed an invisible-ring bug from an unfilled transparent gap exposing the card's own same-hue background) plus a scroll regression fix: task 126's wheel-to-horizontal-pan listener was attached via a `useEffect` keyed to state unrelated to the scroll container's DOM lifecycle, so "Jump to phase" (or any re-render not touching that state) could leave it permanently unattached — moved attach/detach into the scroll container's ref callback so it's self-healing on every render. `npx tsc --noEmit` and `pnpm lint` both pass clean; every change was exercised live in the browser against the real "Acme Testing Co Website" project throughout, not just typechecked, with no console errors at any point → `_docs/task/127-deliverable-card-progress-ring-wizard-redirect.md` (completed: 2026-07-10)
- [126] Onboarding Detail Gantt — Badge Clipping Fix, Wheel-to-Horizontal-Scroll, Calendar-Day Programme Day Fix — fixed three bugs in the 120-Day Programme Gantt (task 125's view): (1) the Internal Deliverables `ListChecks` badge was clipped on track-0 cards because the Swimlane row was `overflow-scroll` with no top clearance — switched to `overflow-visible` plus an 8px `LANE_TOP_PADDING` (adjusts `laneHeight` to match); (2) horizontal scroll only worked via scrollbar drag, since nothing converted vertical wheel/trackpad input into `scrollLeft` — added a native (non-passive) `wheel` listener on `scrollRef` that pans the grid on wheel input while hovered, skips interception on `ctrlKey` (pinch-zoom) or when there's no horizontal overflow, and leaves the page's normal vertical scroll untouched outside the grid; (3) the "Day N" indicator was stuck a day behind because `getCurrentProgrammeDay` floor-divided the raw millisecond gap from a full `programme_started_at` timestamp — since that timestamp carries a time-of-day, the day only ticked over once today's clock caught up to the original start time, not at calendar midnight. Fixed by diffing midnight-normalized calendar dates instead. `npx tsc --noEmit` and `pnpm lint` both pass clean; manual browser verification of the wheel-scroll/badge-visibility/Day-N behavior was left to the user rather than independently re-verified this session → `_docs/task/126-onboarding-detail-badge-clipping-wheel-scroll-day-fix.md` (completed: 2026-07-10)
- [124] New Project — Full-Page Setup Wizard (replaces onboarding intake modal) — replaced the onboarding module's "New Project" intake modal (task 123) with a full-page, 3-step animated wizard at `/v2/onboarding/new` (Company & Contact → Project Details → Review & Create), styled to match `_design/customers/CustomerOnboarding.tsx`'s exact palette/font/Motion animations but reimplemented with Tailwind arbitrary-value classes instead of inline `style={{}}`, scoped Space Grotesk/Inter/JetBrains Mono fonts to this one route only (rest of v2 hub stays Sora/`bg-brand`). All form state, validation, debounced customer search, and the `POST /api/onboarding/projects` submit contract (`save`/`save_scheduled`/`start` modes) were ported verbatim from the deleted modal — no business-logic changes. Deleted the now-dead `fetchProjects` `useCallback` in `_onboarding-list.tsx` as a direct consequence of removing the modal's `onCreated` callback. Browser-verified live end-to-end (new-company creation → "Just Save" → success screen → project detail → "Draft" card on the list); `Save + Set Schedule`/`Start Onboarding` share the same submit code path but weren't independently live-submitted to avoid extra writes against what turned out to be live-looking Supabase data → `_docs/task/124-new-project-wizard-full-page-redesign.md` (completed: 2026-07-09)
- [121] Fix Lint Errors + Warnings (`pnpm lint`) — cleared all 44 original problems (8 errors, 36 warnings) plus a few more surfaced mid-implementation; `pnpm lint` and `npx tsc --noEmit` both pass clean. Ignored the PWA-generated custom-worker file in `eslint.config.mjs` instead of hand-editing generated code. Fixed all 6 `react-hooks/set-state-in-effect` errors (a newly-strict rule) with three different patterns depending on cause: `useSyncExternalStore` for hydration-safe one-time reads (theme, greeting, date), derived state instead of a stored loading flag (KB page), and an inlined fetch effect for the users page — one attempt (`TimerButton`) needed a second pass after tripping a *different* rule (`react-hooks/purity`, calling `Date.now()` during render). Found and removed a real bug in passing: `zohoPortalId`, a server-only env var per CLAUDE.md, was being serialized into a Client Component prop for no reason across three separate page.tsx callers (one of which — a cross-route-group import — wasn't in original scope, caught by `tsc`). Several unused-prop removals cascaded further up their call chains than originally planned (`v2-hub-shell.tsx`/`layout.tsx`'s `email`; `admin-dashboard.tsx`/`dashboard-view.tsx`'s `displayName` forwarding to `PMDashboard`; `tasks-tab.tsx`'s `customers` prop, which traced all the way back to a now-removed dead Supabase query in `dashboard/tasks/page.tsx`) — each verified fully dead before removal. Browser-verified the theme toggle (`useSyncExternalStore` rewrite, the riskiest change since it touches hydration) at `/auth/login`: toggles correctly, persists across reload, zero console errors. The remaining behavior-sensitive changes (Ops Chat greeting, `/kb` loading transition, `/v2/dashboard/users` load+refresh, project list view task timer) live behind the auth guard and were not independently browser-verified in this session (no test credentials available) → `_docs/task/121-fix-lint-errors-warnings.md` (completed: 2026-07-09)
- [120] v2 Customer Profile — Select/Deselect Primary Contact from Desk Contacts + Back-to-List Link — added Set/Remove Primary controls to each Desk Contacts row (task 119), writing straight into the existing `customers.contact_name`/`contact_email` fields rather than a new schema concept — resolved by research: those fields are already read by `src/lib/ai/reply.ts` for AI reply personalization and already power the Edit modal, so a parallel `is_primary` flag on `contacts` would have forked the source of truth for no benefit. Primary Contact block now shows Phone/Title when the current contact matches a Desk contact, resolved client-side with no extra fetch. Found and fixed a real pre-existing bug in passing: `validateCustomerUpdate` crashed (`null.trim()`) whenever `contact_email` was explicitly `null` — a path the existing Edit modal already hits on every save with a blank email field (true for the vast majority of customers before this feature existed), and one this task's "Remove Primary" action depends on directly. Also added a "← Back to Customers" link above the profile header for faster navigation. User-confirmed working in browser → `_docs/task/120-v2-customers-select-primary-contact.md` (completed: 2026-07-08)
- [119] v2 Customers — Show Matched Zoho Desk Contacts (List + Profile) — new `GET /api/customers/[customerId]/contacts` route (session-scoped, relies entirely on task 117's `contacts_staff_read` RLS policy, no manual role-filtering needed unlike the Assets route); v2 Customers list gets a scoped `contacts` count query alongside the existing `projects`/`customer_products` lookups, rendering a plain "+N Desk contact(s)" line under the Contact column; profile page's Contact tab gets a new "Desk Contacts" block (name, title, email, phone/mobile) below the existing Primary Contact section, lazy-fetched on tab-open mirroring the Assets tab's exact fetch-once pattern — no new nav tab or `NavSection` value needed. v1 untouched (list view is a structurally different, pre-task-116 component) and the 1,427 unmatched Desk contacts stay out of UI scope, per user decision. User-confirmed working in browser against real data → `_docs/task/119-v2-customers-show-desk-contacts.md` (completed: 2026-07-08)
- [117] Zoho Desk Contacts — Export, Import & Customer Matching — new Zoho Desk API client (`src/lib/zoho/desk.ts`, separate base URL + required `orgId` header, reuses `fetchZohoWithRetry`'s throttle handling) plus two export routes (Desk Accounts, Desk Contacts) and an import route into a new `contacts` table, matching each contact to a `customers` row by normalizing the Desk account name against `company_name`; unmatched contacts import anyway with `customer_id: null` as a review queue, by design (data-only scope — no UI this round). Post-review fix: renamed `zoho_desk_contact_id`/`zoho_desk_account_id` to generic `external_id`/`external_account_id` (migration 058) to match the `issues`/`milestones`/`tasklists` convention for one-time-import dedupe keys, since baking "zoho_desk" into a table meant to outlive the Zoho decommission was inconsistent (unlike `projects.zoho_project_id`, which stays live-synced and keeps its explicit name). That rename migration was originally numbered 057 and collided with an unrelated, concurrently-created migration (task 118's) claiming the same version — resolved by renumbering to 058 after confirming via a direct schema check that the rename hadn't actually applied yet. Live run on real data: 6,143 Desk accounts and 1,627 Desk contacts exported; import matched 200/1627 by account name (1,427 unmatched — mostly Desk's years of prospect/historical accounts beyond the Hub's 203 active customers, plus contacts with no Desk `accountId` at all), 0 errors — independently re-verified against the live table via direct PostgREST reads, not just the route's own log line → `_docs/task/117-zoho-desk-contacts-export-import-matching.md` (completed: 2026-07-08)
- [118] Customer Assets — Real File Upload, Multi-Field Credentials & Role Permissions — replaced the broken "File" type (previously just a mislabeled URL text box) with a real upload flow into a new private `customer-assets` storage bucket, viewed via on-demand short-lived signed URLs; "Credential" assets now support multiple `{label,value}` sub-fields (username/password/email/etc.) instead of one; every asset type gets an `allowed_roles` visibility pill group (All/Super Admin/Admin/PM/Developer), enforced via `profiles.role` API-level filtering on both GET and DELETE — chosen over introducing this codebase's first per-row RLS policy, after resolving a discovered conflict between the `hub_users.role` and `profiles.role` systems in favor of the latter (matches all existing RLS/CLAUDE.md convention). Applied identically to both the v1 and v2 (task 115) copies of the 2174-line customer profile client component, verified byte-identical throughout via direct file diffing. Post-handoff follow-up (user-tested, requested one refinement) made the modal's helper text dynamic per asset type: accepted file types + 25MB limit for File, example use cases for Link/Credential → `_docs/task/118-customer-assets-file-upload-multifield-role-permissions.md` (completed: 2026-07-08)
- [114] Zoho Issue Attachments Import: `issue` Entity Type + Manual-Match Upload Route — extended the `attachments` table's polymorphic entity_type constraint to include `'issue'` and built an issue-scoped sibling of task 106's manual-match uploader, matching on a `(name, size)` compound key (a real improvement over task 106's name-only matching, since the 629-file Issue dataset has genuine duplicate filenames the 40-file Task dataset never did). Live migration surfaced a chain of real bugs beyond the original implementation: `next.config.ts`'s upload body limit (50MB → 2GB) was far too small for the 1.6GB combined dataset; a matching-key bug where Chrome's local `" (N)"` dedup suffix broke lookups against bare Zoho names (fixed by stripping it) — followed by a second bug where the fix was *too* aggressive and mangled genuinely parenthetical real filenames like `"1 (6).jpg"` (fixed by trying the raw name first, falling back to stripped only if needed); the `project-assets` bucket's 50MB file-size limit (task 106's own setting) rejected several 60-120MB files, requiring both a bucket-level migration (055) and — discovered only after that still didn't work — a separate Supabase Dashboard-level "Global file size limit" project setting that migrations can't touch at all. The ~43 files originally scoped as an "accepted, unresolvable gap" (identical name *and* size across multiple issues) turned out to be fully resolvable by hand once the user identified which real issue each belonged to — three one-off scripts (`scripts/import-ambiguous-issue-attachments.ts`, `fix-line-card-electrical-linked-mixup.ts`, `import-batch2-fixes.ts`) inserted all of them directly, including catching and correcting a self-inflicted bug where the first script assumed same-name-within-a-group always meant same-bytes and briefly corrupted one already-correctly-imported file. Final state: 623 of 629 Issue Attachments fully correct end-to-end; 6 large files have DB rows but are pending a user-deferred manual re-upload once the Dashboard size limit is raised → `_docs/task/114-zoho-issue-attachments-import.md` (completed: 2026-07-08)
- [113] Zoho Issue Attachment Metadata Export: Per-Issue SSE Streaming (entity_type: "bug") — new export route, issue-scoped sibling of the Task attachment-meta export; the one functional delta (`entity_type: "bug"` instead of `"task"`) was user-directed and not independently doc-confirmed beforehand, so the spec required proving it against real data before trusting it. Live run across two slices (0-500, 500-1049 issues) confirmed it: 629 attachment records across 308 of 1,049 issues, every raw record's own `entity_type` field reading back `"bug"`, zero overlap between slices, 100% tagged with `_zoho_issue_id`/`_zoho_project_id`. Also surfaced the same known Zoho quirk task 106 already had to handle (sentinel `attachment_id: "-1"` on 50/629 records, real uniqueness via `third_party_file_id`) — flagged for whichever future task builds the Issue Attachments import/upload follow-up → `_docs/task/113-zoho-issue-attachment-meta-export.md` (completed: 2026-07-07)
- [112] Zoho Issue Time Logs Import: `issue_id` Column + Chunked SSE Upsert — reused the existing `time_logs` table with a new nullable `issue_id` column (migration 053) instead of a new dedicated table, since `time_logs.task_id` was already nullable and 0 `external_id` collisions were confirmed between the 14,533 Task time-log rows and 2,185 Issue time-log rows. New import route mirrors task 103's Task Timelogs importer exactly (multi-file scan, `stripHtml`/notes-fallback, chunked SSE upsert); caught during implementation (by reading the actual shipped Task Timelogs route rather than trusting the spec's sketch) that the `issues` lookup query needed the same `PAGE = 1000` pagination loop as `tasks`/`taskMap`, since `issues` has 1,049 rows — over Supabase's 1000-row default cap, the same bug class task 103/110 hit. Live run: 2,185 imported, 0 skipped, 0 errors → `_docs/task/112-zoho-issue-timelogs-import.md` (completed: 2026-07-07)
- [111] Zoho Issue Time Logs Export: Per-Issue SSE Streaming — near-literal mirror of the Tasks Timelogs export (task 102), diffed line-by-line against `zoho-export/timelogs/route.ts` to confirm byte-identical `fetchZohoWithRetry`/`sleep(700)`/`sleep(100)` throttle pacing, with the one functional delta (`module.type: "issue"`) confirmed via official Zoho API docs. No `log_hours` pre-filter exists on issues, so all 1049 issues get queried per run. Live run (two project-slice runs, `from=0&to=50` and `from=0&to=100`) completed cleanly with no unhandled errors: 2,185 total time-log entries across 73 of 78 projects (the other 5 simply logged zero time on issues), zero project-slice overlap between the two downloaded files, 100% tagged with `_zoho_project_id` → `_docs/task/111-zoho-issue-timelogs-export.md` (completed: 2026-07-07)
- [110] Zoho Issue Comments Import: New `issue_comments` Table + Import Route — dedicated table (migration 052, `issue_id` FK to `issues` not `task_comments`), `resolveIssueId()` helper, chunked-upsert import route with a first-of-its-kind `upsertChunkWithRetry` (bounded retry for transient Supabase write failures, unrelated to Zoho's throttle). Live run initially returned 2185 imported/100 skipped/100 errors ("no Hub issue found") — investigated by cross-checking a failing issue against the raw export (confirmed it existed, ruling out data corruption); root cause was an unpaginated `.select()` on the 1049-row `issues` table silently truncated by Supabase's 1000-row default cap, the same bug class already fixed once in task 103. Fixed by copying that established paginated-loop pattern; re-run imported all 2285 comments cleanly → `_docs/task/110-zoho-issue-comments-import.md` (completed: 2026-07-06)
- [109] Zoho Issue Comments Export: Per-Issue SSE Streaming — new SSE export route mirroring Task Comments export's per-entity/pagination mechanism, scoped to issues via `_from_zoho/issues-*.json`; upgraded to `fetchZohoWithRetry` instead of Task Comments' older inline 429-only retry. Live run tripped Zoho's rolling throttle at issue 200/1049 twice (first at `sleep(200)`, then again at `sleep(700)` after a proactive pacing fix that made no measurable difference to the trip point) — proved the "200 req/2 min" window assumption inherited from tasks 104/105 doesn't hold for this endpoint. Resolution required no further code change: `fetchZohoWithRetry`'s existing bounded backoff (9/12/15min) carried the run through the throttle automatically. Re-run: 2285 comments exported, 0 errors → `_docs/task/109-zoho-issue-comments-export-sse.md` (completed: 2026-07-06)
- [108] Zoho Issues Import: New `issues` Table + Import Route — new `issues` table (migration 051, `task_id` FK reserved for future linkage) + import route mirroring Tasks import's lookup-map/chunking pattern. Live run exposed an N+1 query bug (per-issue `resolveProjectId`+`upsert`, 282 sequential round-trips → 2.4min with zero UI feedback, looked hung); fixed by pre-building a project lookup Map (1 query) + chunked upserts (matches `tasks/route.ts`, not the `milestones/route.ts` pattern it was first modeled on). Also caught and fixed a missing `super_admin` RLS grant before shipping (same bug class migration 048 patched elsewhere). Re-run: 1049 issues imported, 0 errors → `_docs/task/108-zoho-issues-import.md` (completed: 2026-07-03)
- [107] Zoho Issues Export: Per-Project SSE Streaming (with From/To/Since) — added "Issues" export card mirroring Tasks; corrected mid-build from an assumed portal-wide endpoint to the real per-project `/projects/{projectId}/issues` shape (page/per_page required), added `fetchZohoWithRetry` + `failed_project_ids` visibility, then added From/To/Since inputs matching the Tasks card. Live run: 141 issues across 50 projects exported cleanly, verified same array+`_zoho_project_id` shape as `tasks-0-50-2025.json` → `_docs/task/107-zoho-issues-export-per-project-sse.md` (completed: 2026-07-03)
- [106] Attachments Bulk Upload: Manual-Match Uploader — replaced broken Zoho auto-fetch (OAuth-scope/CORS/IAM-ticket blocked, confirmed via live diagnostics) with file-picker + filename-match import; fixed external_id bug (third_party_file_id vs always-"-1" attachment_id); live run surfaced + fixed a missing proxyClientMaxBodySize config and a never-provisioned project-assets Storage bucket (migration 050) — 40/40 real attachments imported and verified linked → `_docs/task/106-attachments-bulk-upload-manual-match.md` (completed: 2026-07-03)
- [103] Timelogs Import: Field-Mapping Fixes, Multi-File Scan & SSE Upsert — fixed log_hour/module_detail/billing_status/notes mapping bugs, pre-built lookup maps (fixed Supabase 1000-row pagination gap), chunked SSE upsert + migrate page UI → `_docs/task/103-timelogs-import-multi-file-field-mapping-sse.md` (completed: 2026-07-02)
- [097] List View: Inline Subtask Expand/Collapse — tree rendering, depth indentation, pre-expand on load, count badge + import Pass 2 pagination fix → `_docs/task/097-list-view-subtask-inline-expand.md` (completed: 2026-06-30)
- [096] Tasks List View Redesign — Zoho-style table, inline pickers, timer, real name chips + assignees backfill migration → `_docs/task/096-tasks-list-view-redesign.md` (completed: 2026-06-30)
- [095] v2 HR > Users Tab: List Users, Manage Role/Status, Send Invite + Register Page → `_docs/task/095-v2-hr-users-tab-management-invite.md` (completed: 2026-06-30)
- [094] hub_users Schema: external_id rename, first_name/last_name split, role system, invite flow, trigger fix → `_docs/task/094-hub-users-schema-rename-roles.md` (completed: 2026-06-30)
- [091] List View: Status Normalization, Priority Fix & Zoho-Style UI Overhaul → `_docs/task/091-list-view-status-priority-fix-ui-overhaul.md` (completed: 2026-06-29)
- [087] Project Detail Overhaul: Primary Tabs, Tasklist Grouping, Milestone Panel & Skeleton → `_docs/task/087-project-detail-list-default-milestone-panel.md` (completed: 2026-06-29)
- [086] Fix profiles.role for Existing Staff Users — Seed from hub_users via migration 040 → `_docs/task/086-fix-profiles-role-seed-from-hub-users.md` (completed: 2026-06-29)
- [055] New `customer_projects` Table — Move Project Fields Off `customer_products` + Fix Auto-Zoho-Creation → `_docs/task/055-customer-projects-table.md` (completed: 2026-06-08)
- [044] Zoho Employee Pending Approval Flow → `_docs/task/044-zoho-employee-pending-approval-flow.md` (completed: 2026-06-02)
- [043] Auth Callback: Zoho Portal User Role Determination → `_docs/task/043-auth-callback-zoho-role-determination.md` (completed: 2026-06-02)
- [026] Sprint 4 Part 2 — Full Zoho Sync (M7 complete) → `_docs/task/026-sprint-4-full-zoho-sync.md` (completed: 2026-05-27)
- [025] Sprint 4 — Plan Generation (M5) → `_docs/task/025-sprint-4-plan-generation.md` (completed: 2026-05-27)
- [024] Sprint 3 Digest Gaps — Clarification Flag, Automation Queue, Unassigned Tasks → `_docs/task/024-sprint-3-digest-gaps-clarification-automation-queue-unassigned.md` (completed: 2026-05-27)
- [023] Dev Digest — Type-Aware Queries, Dev Prompt & Cliq Dev Channel → `_docs/task/023-dev-digest-type-aware-queries-cliq-channel.md` (completed: 2026-05-27)
- [022] Sprint 3 — Requirements Assessment (M3) + Daily Digest (M4) → `_docs/task/022-sprint-3-requirements-assessment-daily-digest.md` (completed: 2026-05-27)
- [021] Onboarding Submission Flow — PM Notification, Zoho Project Dialog & Status Transitions → `_docs/task/021-onboarding-submission-pm-notification-zoho-project-dialog.md` (completed: 2026-05-27)

- [019] CiteForge Badge — Customers Table + Customer Details Add-On Card → `_docs/task/019-citeforge-badge-display.md` (completed: 2026-05-25)
- [018] Fix Public Onboarding — CiteForge Filter + Product Sub-Routes → `_docs/task/018-public-onboarding-product-subroutes.md` (completed: 2026-05-25)
- [017] CiteForge as StackShift Add-On → `_docs/task/017-citeforge-stackshift-addon.md` (completed: 2026-05-25)
- [016] M1 Onboarding Completion — Missing Fields View, Product Metadata Edit, Add Product → `_docs/task/016-m1-onboarding-completion.md` (completed: 2026-05-25)
- [015] Wire Cliq Webhook Token into sendCliqNotification → `_docs/task/015-wire-cliq-webhook-token.md` (completed: 2026-05-25)
- [014] Sprint 2 — Live Stats, Customer Profile Classifications & Pipeline Classify Column → `_docs/task/014-sprint-2-live-stats-customer-classifications-pipeline.md` (completed: 2026-05-25)
- [013] Sprint 2 — Classification Engine, PM Tasks UI, Cliq & Zoho Stubs → `_docs/task/013-sprint-2-classification-engine-pm-ui.md` (completed: 2026-05-25)
- [012] PM Dashboard: Sub-Routes, Tailwind Cleanup & Active Indicator → `_docs/task/012-pm-routes-tailwind-cleanup.md` (completed: 2026-05-25)
- [011] Sidebar Cleanup, Inline Style Migration & Dynamic Greeting → `_docs/task/011-sidebar-cleanup-dynamic-greeting.md` (completed: 2026-05-25)
- [009] PM Dashboard: Apply WebriQ Design System → `_docs/task/009-pm-dashboard-design-system.md` (completed: 2026-05-25)
- [005] Per-Product Progress Bars (Real-Time) on PM Dashboard → `_docs/task/005-per-product-progress-realtime.md` (completed: 2026-05-25)
- [004] Fix: Public Onboarding Route Auth Wall → `_docs/task/004-fix-public-onboarding-route.md` (completed: 2026-05-25)
- [003] Sprint 1: Customer Creation & Onboarding (M1) → `_docs/task/003-sprint-1-customer-onboarding.md` (completed: 2026-05-25)
- [020] Auto-Transition Customer to Active + Create Zoho Project on Full Onboarding Completion → `_docs/task/020-auto-active-status-zoho-project.md` (completed: 2026-05-21)
- [010] Sidebar Nav Restructure + Header User Menu + AI Chat → `_docs/task/010-sidebar-nav-restructure-header-menu-ai-chat.md` (completed: 2026-05-15)
- [008] Admin Force-Logout (All Users Except One) → `_docs/task/008-admin-force-logout.md` (completed: 2026-05-15)
- [007] Fix Zoho Auth: Display Name & Zoho User ID → `_docs/task/007-fix-zoho-auth-display-name-user-id.md` (completed: 2026-05-15)
- [006] Sprint 1.1: Zoho OAuth + Email/Password Auth → `_docs/task/006-sprint-1-1-zoho-auth.md` (completed: 2026-05-07)
- [002] Resolve Sprint Plan Gaps Before Sprint 0 Kickoff → `_docs/task/002-resolve-sprint-plan-gaps.md` (completed: 2026-05-04)
- [001] Sprint 0: Infrastructure Foundation → `_docs/task/001-sprint-0-infrastructure.md` (completed: 2026-04-27)

## Cancelled
